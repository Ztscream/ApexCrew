# ApexCrew Agent Log

This chronological log records material agent work and human decisions. Future implementation entries must include the `PLAN.md` task, Superpowers skill, red/green evidence, commit or PR, and any manual correction. Never record credentials, private prompt text, or secret-bearing command output.

## 2026-07-25 / DISCOVERY-001

- **Skills**: `research`, `domain-modeling`.
- **Human context**: the user selected A-class Coding Agent Harness, named it ApexCrew, proposed repository-bounded action, HITL for dangerous operations, test-feedback correction, multi-agent cooperation, long context, and continuous cowork. Provider choice is deferred.
- **Agent work**: read both course requirement files; inspected the docs-only workspace and local toolchain; delegated primary-source GitHub landscape research and an independent scope review.
- **Finding**: generic multi-agent/worktree/persistence/evidence orchestration overlaps OpenHands, Vibe Kanban, Gas Town, h5i, Bernstein, Codex, and Ruflo. This invalidated novelty claims for individual durability features, not the user's authority to select the product direction.
- **Decision proposed, not accepted**: research suggested adversarial acceptance as an alternative mainline. It remained advisory.
- **Human intervention**: none during the research pass. User sign-off remained required before formal `SPEC.md` brainstorming.
- **Artifacts**: `docs/research/github-agent-landscape.md`, `INITIALIZATION.md`, `CONTEXT.md`, `docs/learning/README.md`, and revised `AGENTS.md`.
- **Lesson**: feature combinations that sounded distinctive were already implemented by direct competitors. Research must precede scope freeze, and provenance must not be confused with semantic correctness.

## 2026-07-26 / INIT-002 - Product direction accepted

- **Skills**: no Superpowers workflow was invoked; this was a human product decision informed by the completed research agents.
- **Key context**: choose one mainline, cap v1 at three Workers, use Python + TypeScript fixtures, and keep the assessed loop independent of host Coding Agent CLIs.
- **Human decision**: accepted **Evidence-Driven Durable Crew** as the only mainline; limited v1 to one repository and at most three Workers; selected Python + TypeScript fixtures and a public personal GitHub repository; prioritized backend and Agent-engineering interview value.
- **Core constraint**: real providers use low-level model APIs. Codex, Claude Code, Gemini CLI, and high-level agent frameworks cannot replace the self-built WorkerLoop.
- **Correction**: adversarial acceptance and weak-oracle challenge became supporting experiments only. The research report was marked advisory and its conflicting Codex-backend recommendation was removed.
- **Artifacts**: `docs/adr/0001-evidence-driven-durable-crew.md`, the decision callout in `docs/research/github-agent-landscape.md`, and reconciled initialization documents.
- **Lesson**: research may challenge novelty and refine claims, but it does not silently supersede an explicit human product decision.

## 2026-07-26 / INIT-003 - Superpowers setup

- **Skills**: plugin installation and filesystem/configuration verification; no newly installed Superpowers workflow was claimed in the installation session.
- **Key context**: the course requires the seven-step Superpowers workflow, so the complete official plugin must be present before formal brainstorming.
- **Installation target**: `superpowers@openai-curated` through the Codex plugin registry.
- **Verification**: enabled in Codex configuration; manifest version `5.1.3`; cache revision `11c74d6b`; required workflow skill directories are present.
- **Agent output**: installed plugin cache and enabled configuration outside the repository; package/version evidence is recorded here and in `INITIALIZATION.md`.
- **Human intervention**: the user authorized completing initialization; no credential or provider choice was required.
- **Operational note**: Stage 2 starts in a task/session where the newly installed plugin skills are loaded. No workflow step is claimed before it is actually run.
- **Lesson**: installation, activation, and actual workflow use are separate facts and must not be conflated.

## 2026-07-26 / INIT-004 - Local governance baseline

- **Skill**: `domain-modeling`, used to keep `CONTEXT.md` glossary-only and record the accepted direction in ADR-0001.
- **Key context**: finish a documentation-only initialization; do not create `SPEC.md`, `PLAN.md`, fixtures, source, tests, or CI before their gates.
- **Agent work**: reconciled product status, repository guidance, architecture boundaries, experiments, process records, and security-oriented ignore rules.
- **Environment verification**: Git author identity is configured; Docker daemon 29.6.1 responds; GitHub CLI is absent but optional.
- **Artifacts**: `README.md`, `SPEC_PROCESS.md`, `CONTEXT.md`, `INITIALIZATION.md`, repository policies, ADR-0001, system overview, experiment plan, research, and learning index.
- **Human intervention still needed**: GitHub/NJU remote details, course schedule, final WebUI constraint, and license choice.
- **Implementation state**: none. `SPEC.md`, `PLAN.md`, source, tests, and CI are deliberately absent.
- **Lesson**: an initialization baseline should make unknowns and gates executable for the next task rather than filling them with guessed decisions.

## 2026-07-26 / INIT-005 - Verification and baseline commit

- **Skills**: `domain-modeling` consistency review plus verification-before-completion discipline; the Superpowers skill itself was installed but not falsely claimed as loaded in this session.
- **Key context**: accept only a repository state that preserves the sole mainline, contains no implementation, leaks no likely credential, and has valid local document links.
- **Agent output**: maintained Markdown files passed local-link checks; all Markdown fences close; `AGENTS.md` is within its required 200-400-word range; common secret patterns and machine-local paths were absent; forbidden implementation paths were absent; all seven required Superpowers skill directories were verified; `git diff --cached --check` passed.
- **Independent review**: a fresh read-only subagent confirmed the mainline/scope/loop constraints and identified missing process-log fields, a cold-start gate deadlock, late provider selection, an incomplete Stage 2 checklist, and executable-looking archived research. All were corrected before commit.
- **Human intervention**: none during verification. GitHub/NJU remotes and license remain external follow-ups.
- **Commit**: `chore: initialize ApexCrew project governance`; the immutable hash is read from Git history because a commit cannot contain its own final hash.
- **Lesson**: process documents need the same evidence loop as code: audit found an omission, the log was corrected, and checks are rerun before completion.

## 2026-07-26 / GOAL-001 - GitHub publication and durable objective

- **Skill**: `domain-modeling`, used to record the hard-to-reverse public-license choice in ADR-0002 without adding implementation details to `CONTEXT.md`.
- **Key context**: publish through the user's personal repository, defer NJU/GitLab, choose a license autonomously, and establish a goal that persists through v0.1 delivery.
- **Human decision**: `https://github.com/Ztscream/ApexCrew.git` is the public repository; NJU/GitLab is out of current scope; license selection is delegated to the agent.
- **Agent output**: configured `origin`, verified the remote contained no refs, selected Apache-2.0 for its permissive terms and explicit patent grant, added `NOTICE` to exclude course-provided documents, and created the long-running v0.1 goal.
- **Safety**: publication uses an ordinary non-force push only after the committed tree passes link, secret, license-integrity, and Git checks.
- **Commit**: `chore: configure GitHub publication and licensing`.
- **Lesson**: a repository-level license must distinguish original project work from bundled reference material the project owner cannot relicense.

## 2026-07-26 / SPEC-R1 - Problem and scenario approval

- **Skill**: Superpowers `brainstorming`, loaded from the installed official plugin; its visual companion was accepted and initialized for later diagram/layout questions.
- **Key context**: define one primary user journey, failure, completion boundary, decomposition owner, and falsifiable comparison before discussing mechanisms.
- **Human decisions**: selected option A for all five questions and explicitly approved Round 1: one developer delegates one hours-long cross-module task; stale integration is the main failure; success ends at a human-approved Integration Candidate (the provisional term later split into Task Candidate and Run Candidate); the Coordinator proposes a bounded DAG; freshness-disabled ablation is the primary baseline.
- **Agent output**: recorded the full question/decision/consequence table and approved conclusion in `SPEC_PROCESS.md`; added `.superpowers/` to `.gitignore` so visual session state remains local.
- **Implementation state**: none. The Superpowers design hard gate remains active.
- **Commit**: `docs: approve brainstorming round 1`.
- **Lesson**: the main contribution is strongest when evaluated by a controlled ablation, not by broad claims that multi-agent execution is generally better.

## 2026-07-26 / SPEC-R2 - Mechanism and state approval

- **Skills**: Superpowers `brainstorming` with its accepted visual companion; `domain-modeling` for the approved vocabulary correction.
- **Key context**: turn the Round 1 freshness claim into a self-owned WorkerLoop, invalidation algorithm, evidence boundary, recoverable state model, and cross-language falsification fixtures without creating implementation.
- **Human decisions**: selected declared dependencies with conservative fallback; prepared-snapshot verification; one typed action per model turn; reconciliation-based recovery; private Run Branch promotion; action-boundary stale termination; and immutable Plan Revisions with bounded automatic correction. The user then explicitly approved the consolidated Round 2 design.
- **Independent review**: three parallel reviewers challenged integration identity, crash windows, state terminology, and fixture falsifiability. Their accepted corrections split Task Candidate from Run Candidate, separated immutable Evidence Receipts from Freshness Assessments, required a final run-wide gate, and bound integration to prepared commit and expected target OIDs.
- **Fixtures approved**: Python changes money units from cents to decimal dollars; TypeScript changes timestamps from milliseconds to seconds. Both preserve text-level mergeability while making old green evidence semantically invalid.
- **Rejected**: worker-tip receipts, merge-then-test, dynamic-only dependency inference, multi-action/free-form Worker execution, unconditional replay, hard process interruption, per-check human approval, and silent DAG/contract mutation.
- **Implementation state**: none. Round 3, consolidated `SPEC.md`, `writing-plans`, and independent cold-start review remain mandatory gates.
- **Commit**: `docs: approve brainstorming round 2`.
- **Lesson**: evidence is immutable history; current authority is a separate judgment over the exact prospective revision, dependency graph, contract, policy, and checks.

## 2026-07-26 / SPEC-R2A - Post-commit specification correction

- **Trigger**: an independent review of `e70b3e3..bdfc56c` found that Round 2 was recorded without the required `SPEC.md` diff or legal lifecycle/data-model definitions. It also found mixed freshness/grant terminology, Plan/Policy version coupling, ambiguous private promotion wording, and a stale README fixture TODO.
- **Correction**: created a not-implementation-ready `SPEC.md` containing only approved Round 1/2 behavior plus explicit Round 3 gaps; defined legal Run, Task, Attempt, Candidate, and lease transitions and logical entity relationships; separated Policy Revision and Grant Validation; clarified that automatic promotion touches only the private Run Branch.
- **Implementation state**: none. This correction closes documentation-process gaps and does not authorize source, tests, fixtures, or planning.
- **Commit**: `docs: add approved round 2 spec sections`.
- **Lesson**: approval of choices is not approval of an underspecified artifact. Each round must leave an inspectable normative diff, and independent review must check the process contract as well as prose consistency.

## 2026-07-26 / SPEC-R3 - Operations and acceptance approval

- **Skills**: Superpowers `brainstorming` and visual companion for consolidated architecture review; `openai-docs` for current model/API/pricing facts; `codebase-design` for deep-module seams; `domain-modeling` for canonical Round 3 language and qualifying ADRs.
- **Prompt/context**: the agent received the two course requirement files, approved Round 1/2 records, the A/B/C1 decision sequence, the documentation-only gate, and a request to consolidate all thirteen operations choices without creating implementation. The user reviewed the rendered Round 3 page rather than approving an unseen prose rewrite.
- **Human decisions**: trusted host control plane plus restricted Docker command executor; OpenAI Responses API with `gpt-5.6-terra`; keyring credentials with headless/CI environment injection; adaptive C1 budgets; CLI-only mutations and read-only WebUI; Open Design as a design-stage tool; two-tier observability; untrusted repository/Worker threat model; GitHub Pages fixture replay; Windows 11 and Ubuntu 24.04 x86_64; approved Run Check Set; balanced quantitative acceptance thresholds.
- **Schedule**: deadline supplied as 2026-08-10 and interpreted as 23:59 Asia/Shanghai; 25 hours/week yields approximately 53 hours. Optional weak-oracle, extra providers/platforms, writable WebUI, and hosted execution are cut before required core or course work.
- **Independent research**: a read-only Open Design agent confirmed that upstream is an application/workbench rather than a stable UI library. The accepted workflow pins `open-design-v0.16.1` at commit `276b4d8e970bc143d7ad060181a89a834e3d9caf`, authors the custom ApexCrew Operational system through `design-md`, uses disposable `dashboard` and `design-review` artifacts, keeps the design contract original, and excludes Open Design packages/daemon/generated source from runtime and CI.
- **Subagent evidence**: the Open Design review concluded, "Open Design is more suitable as ApexCrew's development-time design workbench; it should not become a runtime dependency," and identified its private workspace packages and fast-moving `0.x` surface as the reason to transfer only reviewed information architecture and original tokens.
- **Conservative defaults approved**: token-protected loopback UI; Tier 1 retained until explicit purge and Tier 2 retained 30 days/1 GiB; regular-file actions/checks allowed, protected mutations revision-bound and one-use approved, escape/shell/network/socket/push/destructive Git hard-denied; evidence bound to an exact Execution Fingerprint and model actions to immutable provenance.
- **Agent output**: replaced every Round 3 gap in `SPEC.md` with normative operations, security, delivery, non-functional, schedule, risk, and acceptance clauses; reconciled process, glossary, architecture, initialization, README, contributor guidance, and ADRs.
- **Implementation state at Round 3 close**: none. `PLAN.md` was gated on architecture comparison, independent spec review, and final written-spec sign-off; source, fixtures, tests, and CI additionally required planning and cold-start review.
- **Human approval**: the user explicitly approved the complete Round 3 review page. This approves the design round and documentation update, not implementation or push.
- **Lesson**: durability is only credible when operational limits, threat assumptions, evidence environment, redaction, public-demo authority, and delivery time are as explicit and testable as the state machine.

## 2026-07-26 / SPEC-R3A - Independent Round 3 correction

- **Skills and context**: `code-review` ran independent Standards and Spec axes over `git diff --cached HEAD` from fixed point `fa780b7`; standards were `AGENTS.md`, both course requirement files, and `SPEC_PROCESS.md`, while the approved Round 3 record was the spec source.
- **Subagent evidence**: Standards reported a deadlock between retaining `PLAN.md` and using it for cold-start review; Spec reported that Tier 2 could exceed 1 GiB during an active Run and that the public replay lacked objective behavior.
- **Corrections**: made `PLAN.md` the allowed post-sign-off review input; added candid process reflection and technical-choice rationale; bounded every Tier 2 payload and repository total; specified replay controls and acceptance; restored the inclusive ten-minute CI bound and major-change design review; clarified bootstrap/no-progress pauses, credential injection risk, check timeouts, and sanitized disposable executor snapshots.
- **Human intervention**: none in this correction pass. It applies the already approved Round 3 choices and course rules; it does not add a product decision, implementation authority, or push approval.
- **Implementation state**: none. The correction changes documentation and review evidence only; no source, fixture, test, CI, or `PLAN.md` was created.
- **Lesson**: a high-level safety choice is incomplete until retention overflow, malicious check writes, secret-bearing untracked files, retry accounting, and user-visible replay behavior have explicit failure-closed outcomes.

## 2026-07-26 / SPEC-ARCH - A-Hybrid architecture approval

- **Skills and context**: `codebase-design` Design It Twice compared module interfaces after Round 3; `domain-modeling` kept implementation names out of the domain glossary and recorded the qualifying decision in ADR-0006. Two design agents ran in parallel; the journey-facade brief ran as a read-only follow-up after the agent-thread limit rejected a third concurrent spawn. Each pass used `SPEC.md`, `CONTEXT.md`, `INITIALIZATION.md`, and the system overview.
- **Subagent evidence**: the minimal kernel offered `execute/read` but warned of a swollen kernel and blocking interruption semantics; the flexible dual reactor improved locality but exposed many ordering-sensitive interfaces; the journey facade simplified CLI use but warned its continuation token could become a generic command bus.
- **Agent recommendation**: combine the minimal external surface with internal dual-reactor locality: `CrewControl.handle`, `CrewRuntime.run_until_blocked`, and `RunQueries.get`, backed by internal Coordinator, WorkerLoop, Admission, Authority, EffectJournal/recovery, tools, and projection modules.
- **Human decision**: the user explicitly approved **A-Hybrid**. This selects implementation module ownership and interface placement; it does not approve the complete written SPEC, create `PLAN.md`, authorize implementation, or authorize push.
- **Output**: updated the normative module contracts, architecture maps, initialization/process status, contributor guidance, README, learning-note index, and ADR-0006. `CONTEXT.md` remains unchanged because the new names are implementation interfaces rather than domain language.
- **Implementation state**: none. No package directory, fixture, test, CI, or `PLAN.md` was created.
- **Lesson**: the strongest shape is not the smallest interface alone; depth also requires local internal ownership so freshness/admission and recovery changes do not turn one kernel into a maintenance hotspot.

## 2026-07-26 / SPEC-ARCH-R1 - Whole-spec cold-review correction

- **Trigger**: after A-Hybrid approval, a fresh implementer/state-safety read of the complete SPEC found nine blocking ambiguities that the earlier architecture-diff review did not expose.
- **Skills and agents**: `codebase-design` preserved the approved deep-module seams; `domain-modeling` synchronized changed meanings in `CONTEXT.md`; read-only lifecycle/durability and concurrency/authority agents independently specified failure cases and deterministic guards.
- **Blocking evidence**: bootstrap planning had no executable owner/sequence; provider calls preceded durable intent; Plan approval bypassed Policy/Budget/Model Configuration start guards; future writers could stale already-promoted work; consumed Grants, old-head leases, unscoped reads, moved targets, and human uncertainty resolution had contradictory or open semantics.
- **Correction**: added a bounded Coordinator planning protocol, explicit `PLANNING`/`READY_TO_START` gates, durable model request reservations, promotion-hazard validation, scoped `R`/`D`/`W`/`Q` access, classified-head lease admissibility, pinned-base behavior, exact-intent Grant settlement, and closed objective `INDETERMINATE` choices. Candidate CAS remains Admission-owned; bootstrap CLI flows have no Run/model/repository effect authority.
- **Implementation state**: none. The changes are documentation only; `PLAN.md`, source, fixtures, tests, and CI remain prohibited.
- **Review state**: correction prepared and awaiting a new whole-SPEC cold read. A-Hybrid approval remains valid, but this entry is not final written-spec approval or push authorization.
- **Lesson**: interface shape can pass a diff review while lifecycle ordering remains impossible; every external effect and pre-activation phase needs the same durable authority and failure semantics as the main Worker loop.

## 2026-07-26 / SPEC-ARCH-R2 - Authority and host-safety closure

- **Trigger**: continued whole-document implementer, safety, and course reads found that the first correction still allowed divergent implementations around CAS ownership, released leases, invalid Grants, approval-wait races, active Policy changes, returned model IDs, symlinks/secrets, purge recovery, checked-out target branches, and repository-controlled host Git execution.
- **Skills and agents**: `codebase-design` kept A-Hybrid at three Run-facing interfaces; `domain-modeling` synchronized only resolved domain terms; three read-only reviewer roles supplied lifecycle, host-security, and course-process counterexamples.
- **Correction**: Coordinator now schedules while Admission exclusively validates/prepares/issues CAS and a sanitized Git adapter executes it. Plan/Policy freeze at `ACTIVE`; Candidate promotion relies on proven lease provenance; bad Grants do not destroy fresh Candidates; pause/cancel and Grant-delivery crashes have ordered outcomes; returned-model IDs are allowlisted before loop release.
- **Security closure**: v0.1 denies all symlinks, binds fixed plus host-local secret rules without disclosure, rejects checked-out targets and unsafe/external Git storage, disables hooks/filters/config/network paths, defines tombstone-backed terminal purge, and requires tracked-tree plus full-reachable-history secret scanning with planted-secret negatives.
- **Process correction**: cold-start review is mandatory for a different agent type in a fresh session using only SPEC/PLAN, with 1-2 disposable tasks for about 1-2 hours and a required pause on ambiguity.
- **Implementation/review state**: documentation only; no `PLAN.md`, source, fixture, test, or CI exists. A new independent whole-document review must still find zero blockers before final written-spec approval is requested.
- **Lesson**: a typed adapter is not a containment guarantee until configuration, hooks, storage indirection, model routing, lifecycle races, and destructive-retention recovery all have explicit fail-closed contracts.

## 2026-07-26 / SPEC-ARCH-R3 - Runtime authority and reservation cleanup closure

- **Trigger**: continued fresh implementer, security, and course-document reads found that the second correction still allowed direct/old-command runtime replay, incomplete planning and multi-intent transitions, undefined known-CAS failure, ambiguous post-purge queries, and a Target Reservation that could not be safely removed or recovered.
- **Skills and agents**: `domain-modeling` captured Target Reservation and Runtime Permit language; `codebase-design` kept the approved three-interface Run surface; three read-only reviewers independently exercised lifecycle, Git/security, and course/process counterexamples.
- **Runtime correction**: accepted runtime-driving commands now issue one persisted one-use Runtime Permit, consumed with ownership before reconciliation or Coordinator work. Old begin/start/resume/Grant/integration replays cannot restart progress; only a new exact `continue` closes a genuine orphaned phase. State tables now cover planning stops, multi-intent barriers, and private/target CAS known-failure successors.
- **Git correction**: one reservation identity/ownership row is persisted before Git and reused across `DRAFT` retries. v0.1 rejects pre-existing linked worktrees and config includes before Git, never follows admin-record paths, and closes terminal cleanup with exact unlock, revalidation, one intent-bound forced remove, and explicit mixed-component crash recovery.
- **Retention correction**: purge waits for administrative cleanup, retains only its confirmation receipt/tombstone, returns deterministic outcomes for old request IDs, and exposes only minimal tombstone query state after Audit history is removed.
- **Implementation state**: none. No `PLAN.md`, source, fixture, test, or CI artifact was created; all work remains documentation-only.
- **Review state**: corrections are written and a new whole-document cold read is in progress. This entry is not final written approval or push authorization.
- **Lesson**: durable commands require separate delivery authority, and a safety guard is incomplete until both its normal teardown and every partial teardown state are executable without trusting repository-controlled routing.

## 2026-07-26 / SPEC-ARCH-R4 - Independent specification review passed

- **Review subject**: the complete `SPEC.md` frozen at SHA-256 `9751BEA572112994034AEA2AB265CFE6AD54195CFB513C18C587AB35AA2F3EB4`, together with its process, glossary, initialization, architecture, learning, and contributor companions.
- **Independent evidence**: a fresh implementer/state-machine cold read, a course/companion-document cold read, and a security/Git-containment cold read each reported `ZERO BLOCKERS`.
- **Closeout**: synchronized only review status, exact private-ref/Attempt-workspace terminology, and the documented rejection of unsupported linked-worktree, sparse/split, graft/shallow/partial, alternate, and externally routed Git layouts.
- **Implementation state**: none. No `PLAN.md`, source, fixture, test, build, or CI artifact exists; final written-spec approval, planning, and the separate implementation cold-start review still gate persistent implementation.
- **Authority state**: this review result is not final written-spec approval, implementation authorization, commit publication, or push authorization.
- **Lesson**: a frozen review result must identify both the artifact digest and reviewer perspective, while process-status edits must remain visibly separate from product approval.

## 2026-07-27 / SPEC-SIGNOFF - Final written specification approval

- **Human decision**: in Codex task `019f99cb-b307-75a3-8059-6e12908ff4b8`, the repository owner stated `批准最终 SPEC.md，哈希 2F1434AB29C3B7205B13CA96FE35D18C7666729F633EDF984F1DFCA54F0663BC`.
- **Approved artifact**: root `SPEC.md`, SHA-256 `2F1434AB29C3B7205B13CA96FE35D18C7666729F633EDF984F1DFCA54F0663BC`, 128,418 bytes, source commit `aabdd4fb664eb82dcc34c41391dbdb48872254c8`, Git blob `9d32355e610e08a75bf89a3a0fc23785441cda24`.
- **Review basis**: implementer/state-machine, course/companion-document, and security/Git-containment reviewers each reconfirmed `ZERO BLOCKERS` on the final digest and closeout delta.
- **Integrity rule**: `SPEC.md` was rehashed unchanged while the worktree was clean. Its embedded pre-approval status is superseded by the external sign-off record; any byte change requires a new digest and explicit approval.
- **Scope**: Stage 2 is complete. The approval authorizes `writing-plans` to create `PLAN.md`; it does not authorize persistent implementation, credentials, publication, push, release, or a Runtime Grant.
- **Planning state**: `writing-plans` 5.1.3 is installed at cache revision `11c74d6b` but is absent from this session's available-skills catalog, so no `PLAN.md` was created. Planning must resume in a session where the plugin skill is loaded.
- **Recorder**: Codex at 2026-07-27T09:26:27+08:00. SHA-256 identifies content but does not itself prove approver identity.

## 2026-07-27 / PLAN-R1 - Stage 3 implementation plan and document review

- **Skill and authority**: pinned personal `writing-plans` at upstream commit `f2cbfbefebbfef77321e4c9abc9e949826bea9d7`, normalized equal to repository-recorded Superpowers 5.1.3 cache revision `11c74d6b`; frozen approved `SPEC.md` SHA-256 `2F1434AB29C3B7205B13CA96FE35D18C7666729F633EDF984F1DFCA54F0663BC`.
- **Output**: created only root `PLAN.md` plus planning/process records. The plan covers the approved A-Hybrid interfaces, ApexCrew-owned Coordinator/WorkerLoop, low-level ModelPort and offline `ScriptedMockLLM`, SQLite recovery, typed admission/authority/tools, hostile Git/Docker containment, both fixtures, CLI/read-only WebUI, CI/public replay, evaluation, and delivery gates.
- **Schedule/cut line**: retained 2026-08-10 23:59 Asia/Shanghai, 25 hours/week, and approximately 53 hours; required core, safety, deterministic fixtures, delivery, public replay, and course evidence precede optional experiments/providers/platforms/UI scope.
- **Independent plan review**: found five blockers: missing wheel backend/source-package configuration; unreachable Policy/Budget/Model Configuration flows; fixture commands without deterministic provisioned imports/tools; no `dist/pages` producer; and task commits omitting `AGENT_LOG.md`. The plan was corrected before cold-start dispatch.
- **Implementation state**: none retained; no commit or push authorized or performed.

## 2026-07-27 / COLDSTART-1 - Stage 4 disposable attempt paused

- **Review isolation**: detached worktree `C:\Users\29119\.codex\worktrees\stage4-apexcrew-87fd-review` at `07f20aa92de97088d56d07c1a8528f93a220cf91`; fresh different-model reviewer received only `SPEC.md` and copied untracked `PLAN.md` as normative inputs.
- **Task 1 red evidence**: `uv run pytest tests/unit/test_package.py::test_package_exposes_initial_version -q` failed with the expected `ModuleNotFoundError: No module named 'apexcrew'`, but selected Python 3.11 rather than required Python 3.12.
- **Blocking probe**: `uv python find 3.12` failed because no CPython 3.12 interpreter was discoverable. The reviewer correctly paused instead of inferring authority for `uv python install 3.12`.
- **Task 2 result**: not attempted because Task 1 could not complete. Read-only inspection additionally found undefined canonical revision-document fields and lifecycle-dependent enforcement assigned to immutable type files.
- **Disposable changes**: only `tests/unit/test_package.py`, ignored pytest/bytecode caches, and the copied `PLAN.md`; no package source, configuration, lockfile, credentials, commit, push, or user-target effect.
- **Plan correction**: authorized an explicit conditional Python 3.12 host prerequisite; made the initial red command select Python 3.12; defined exact closed Policy/Budget/Model Configuration documents, digests, payloads, revision bindings, and confirmation codes; moved state-dependent revision rules to `CrewControlService` and the runtime action barrier.
- **Status**: Attempt 1 rejected as incomplete. Remove its disposable artifacts/worktree, then rerun Tasks 1-2 in a new detached worktree and fresh context. Stage 4 remains open.

## 2026-07-28 / PLAN-R2 - execution-readiness audit and correction

- **Authority and scope**: documentation-only correction under the frozen approved `SPEC.md` digest `2F1434AB29C3B7205B13CA96FE35D18C7666729F633EDF984F1DFCA54F0663BC`; no source, fixture, test, CI, lockfile, credential, commit, or push was created by this audit.
- **Independent findings**: dependency review reported no forward references or missing legacy task commit commands. Schedule review found the claimed 53-hour all-scope completion infeasible from 757 two-to-five-minute actions before TDD/review/commit overhead; it also found non-atomic Tasks 2, 7, 11, 12, and 27, Task 28's unsequenced provider work, a premature eight-job Task 35 release assertion, and a partial `TASK-*` ledger presentation.
- **Plan correction**: `PLAN.md` now records M0-M4 milestones, requires owner capacity review at every boundary, keeps M4 requirements deferred rather than waived, splits the five legacy groups into independent execution slices, designates Task 28 as the offline-contract-tested M4 provider profile, makes Task 35A report `PENDING_FINAL_CI_TOPOLOGY`, and adds Task 35B after Task 36C for the eight-job release verifier.
- **Cold-start scope**: the next disposable review is limited to the Python 3.12 probe, Task 1, and Task 2A. It stops before ledger/commit steps and cannot close retained implementation until an independent re-review accepts the R2 plan and its generated worktree is removed.
- **Observed host prerequisite**: `uv python find 3.12` passed and selected `C:\Users\29119\AppData\Roaming\uv\python\cpython-3.12.12-windows-x86_64-none\python.exe`.
- **Implementation state**: none retained. The old Attempt 2 worktree remains quarantined because it contains a stale untracked plan; it is not an authorized review input.
- **R2 independent re-review**: schedule, structural, and dependency auditors reported zero blockers for an M0 Stage 4 evaluator. They verified that only Task 1 and the exact `2A` procedure are executable, that `2A` has independent red/green/mypy/diff commands, that M1-M4 require future reviewed plan revisions, that 35B consumes committed CI definitions rather than completed remote jobs, and that the Python 3.12 host probe passes. The remaining 66-task roadmap is not current execution authority.

## 2026-07-28 / COLDSTART-2 - Stage 4 M0 attempt paused and corrected

- **Review isolation**: fresh user-owned evaluator in detached disposable worktree `C:\Users\29119\.codex\worktrees\bbcb\AI4SE` at documentation commit `6d219a91413e6de52e8f5b86ee87d15c149544a8`; normative inputs were only frozen `SPEC.md` and R2 `PLAN.md`.
- **Prerequisite and red evidence**: `uv python find 3.12` selected CPython 3.12.12. The initial and configuration-only package selectors both failed with the expected `ModuleNotFoundError: No module named 'apexcrew'`. The `.gitignore` scoped diff contained exactly the allowed added `.tmp/` line.
- **Pause**: `uv lock --python 3.12` exited 0, resolved 51 packages, and created `uv.lock`, but emitted previously unspecified nonfatal third-party metadata warnings about ignored legacy Jinja2/keyring/tqdm artifacts and corrected invalid version specifiers. The evaluator correctly treated unlisted output as an ambiguity and stopped before sync, green tests, `2A`, ledger/staging, or commit.
- **Disposable changes**: only the Task 1 files `pyproject.toml`, `.python-version`, `.gitignore`, `uv.lock`, `src/apexcrew/__init__.py`, `tests/conftest.py`, `tests/unit/test_package.py`, plus ignored pytest cache; no credentials, provider calls, source outside M0, staging, commit, push, or publication.
- **Plan correction**: Task 1 Step 8 and the Stage 4 gate now explicitly permit those exit-zero resolver metadata diagnostics while retaining stop conditions for lock failure, missing lockfile, unsupported Python, or missing direct dependency. Attempt 2 is rejected as incomplete; its worktree must be removed and a fresh evaluator must rerun M0 against the amended plan.
- **Cleanup state**: `git worktree remove --force` removed the disposable worktree registration but Windows returned `Permission denied` while deleting its directory. The completed evaluator task was archived; repeated exact-path removal and recoverable move both remain blocked by an external file handle. The orphaned directory is outside every Git worktree and contains only the listed disposable M0 files. No new cold-start attempt may begin until it is removed or the owner explicitly accepts a recorded cleanup exception.

## 2026-07-31 / CLEANUP-1 - Attempt 2 cleanup closed and a duplicate plan retracted

- **Session context**: Claude Code (Opus 5) was asked to assess the project's architecture, stack, and stage, then authorized to proceed on its own judgment.
- **Assessment error**: the initial reading ran `git log` on `main` only and never ran `git branch -a`. `main` was clean and its last commit was the 2026-07-27 spec sign-off, so the session incorrectly reported that no engineering had occurred since sign-off, that `writing-plans` had never produced `PLAN.md`, and that Python 3.12 was the outstanding host prerequisite. All three were false: PLAN-R1, COLDSTART-1, PLAN-R2, and COLDSTART-2 had already happened on unmerged branch `codex/stage4-m0-plan`, and `uv python find 3.12` had already passed on 2026-07-28.
- **Consequence**: acting on that wrong reading, the session wrote a separate 823-line `PLAN.md` and committed it to `main` as `08b067f`, together with status edits to `README.md`, `INITIALIZATION.md`, `SPEC_PROCESS.md`, and `AGENTS.md`.
- **Retraction**: the repository owner chose to discard that work. `main` was reset to `07f20aa` and fast-forwarded to `b1c1d47`, so the reviewed R2/R3 `PLAN.md` is the sole plan of record. The discarded draft was kept only outside the repository as a non-authoritative reference and is not a second plan truth.
- **Cleanup performed**: the orphaned Attempt 2 directory `C:\Users\29119\.codex\worktrees\bbcb\AI4SE` recorded in COLDSTART-2 as undeletable was observed empty on 2026-07-31, removed, and its empty parent `bbcb` removed with it. Git had already deregistered that worktree, so no Git operation was involved. The COLDSTART-2 precondition that blocked a new cold-start attempt is now satisfied without a cleanup exception.
- **Also removed**: a disposable review worktree this session had created at `C:\Users\29119\Desktop\apexcrew-coldstart-review` from the retracted plan. `git worktree list` now shows only the main worktree plus the two pre-existing Codex worktrees.
- **Host state**: `uv python install 3.12` was re-run this session and was a no-op; `cpython-3.12.12-windows-x86_64-none` was already present, consistent with the COLDSTART-2 probe.
- **Publication state**: unchanged and unauthorized. `origin/main` remains at `e70b3e3`; local `main` is ahead and was not pushed.
- **Implementation state**: none retained. No `src/`, `tests/`, `fixtures/`, or CI artifact exists on `main`.
- **Next authorized action**: a new user-owned Stage 4 M0 evaluator, different agent type, fresh session, disposable worktree at the current documentation commit, receiving only frozen `SPEC.md` and R3 `PLAN.md`, attempting Task 1 plus exact `2A` and stopping before ledger, staging, or commit.
- **Lesson**: a clean working tree on the default branch is not evidence that no work exists. Enumerate refs before judging project state, and treat an unexpectedly quiet history as a prompt to look wider rather than as a finding.

## 2026-07-31 / CLEANUP-2 - Stale Attempt 1 worktree removed

- **Target inspected before deletion**: `C:\Users\29119\.codex\worktrees\stage4-apexcrew-87fd-attempt2`, detached at `07f20aa`. `git status` showed exactly one untracked file, the superseded R1 `PLAN.md`; every other entry was tracked content already present at that commit.
- **Superseded artifact identified, not silently destroyed**: the R1 plan measured 7,385 lines at SHA-256 `835231CA29C880F875F3FE42C142B2B711CEEF46995448F5B780D0EB3165A676`. That digest is recorded in `SPEC_PROCESS.md` so the artifact Attempt 1 reviewed stays identifiable, while the repository keeps only one plan of record. Its substantive findings were already captured in PLAN-R1 and COLDSTART-1.
- **Removal**: `git worktree remove --force` deregistered and deleted it without the Windows file-handle failure seen in COLDSTART-2. `git worktree list` now shows only the main worktree and `C:\Users\29119\.codex\worktrees\87fd\AI4SE` on `codex/stage4-m0-plan`, whose commits are merged into `main`.
- **Rationale**: `SPEC_PROCESS.md` had recorded that this worktree held a pre-R2 plan that must not reach an evaluator. Removing it converts a documented prohibition into a structural impossibility before Attempt 3 is dispatched.
- **Implementation state**: none retained. `main` remains documentation-only at a clean tree.
- **Publication state**: unchanged. `origin/main` is still at `e70b3e3`; no push was authorized or performed.

## 2026-07-31 / COLDSTART-3 - Stage 4 M0 review passed with zero blockers

- **Dispatch**: fresh Claude Sonnet 5 evaluator, no inherited context, isolated disposable worktree at `d0ebc62`, inputs restricted to frozen `SPEC.md` (`2F1434AB...663BC`) and R3 `PLAN.md` (`93ADDFE7...90CE`). The Execution Gate's prohibition on loading `superpowers:executing-plans` was passed through explicitly and observed.
- **Result**: both authorized M0 slices completed with no pause. Task 1 red reproduced the specified `ModuleNotFoundError`; `uv lock --python 3.12` exited 0 resolving 51 packages; Task 1 and `2A` focused selectors turned green; `mypy` succeeded on `types.py` and `revisions.py`; `git diff --check` was clean; `commands.py` was confirmed absent. Verdict `ZERO BLOCKERS` with four non-blocking findings.
- **Independent verification**: the recorder re-ran the Task 1 and `2A` green selectors and `mypy` inside the evaluator's worktree and observed the same passing output before destroying it. The report was not accepted on its own assertion.
- **Process-strength qualification**: Attempts 1 and 2 were owner-dispatched Codex evaluators. Attempt 3 was dispatched by the assisting agent inside the same session. Cold context, a different model from both dispatcher and prior evaluators, worktree isolation, and the two-file input restriction all held, and the evaluator disclosed that an early directory listing incidentally exposed forbidden filenames without opening their content. It is still weaker evidence than an owner-opened session because the dispatcher was not independent of the plan. An owner-dispatched Codex confirmation run is recommended before this is cited as sole Stage 4 closure.
- **Unexercised amendment**: `uv lock` emitted zero warnings, so the R3 correction permitting resolver metadata diagnostics was never triggered. It is written but unvalidated by observation.
- **Cleanup**: `git worktree remove` reported the same Windows permission error seen in COLDSTART-2 after Git had already deregistered the worktree. The empty directory was removed on retry, `.claude/worktrees/` was removed, and the disposable branch `worktree-agent-adb25cb7a7b91acb4` was deleted. `main` contains no `src/`, `tests/`, `pyproject.toml`, `uv.lock`, or `.python-version`; `git status` is clean.
- **Implementation state**: none retained. Stage 4 success now authorizes retained re-execution of Task 1 and `2A` under M1, which itself still requires a new exact M1 `PLAN.md` revision, an independent document review, and the owner capacity decision below.

### M0 milestone capacity review (required by `PLAN.md` Milestones section)

| Field | Value |
|---|---|
| Elapsed against the planned budget | ~17.9 h of the ~53 h notionally available from 2026-07-26, at the stated 25 h/week |
| Hours available before the deadline | ~35.7 h (2026-07-31 to 2026-08-10 23:59 Asia/Shanghai at 25 h/week) |
| Completed retained evidence | None. M0 is disposable by definition; its artifacts were destroyed |
| Conservative remaining estimate for M1-M4 | Exceeds the hours available. `PLAN.md` already records that the 757 listed 2-5 minute actions alone total 25.2-63.1 raw hours **before** red/green reruns, full regressions, review, evidence recording, and 66 commit boundaries |
| Owner decision | **PENDING.** Not decided by an agent |

The remaining schedule does not close for the full signed scope. Per the Milestones section the only valid outcomes are a new approved scope revision or additional capacity/time; silently omitting a required signed item is prohibited. The owner must record an explicit `GO` or `HOLD` for M1, and if `GO`, state which signed deliverables move behind the deterministic offline core.

## 2026-07-31 / CAPACITY-M0 - Owner capacity decision and M1 readiness audit

- **Owner decision**: capacity raised from 25 h/week to **30-40 h/week**, with the instruction not to over-weight the deadline and to aim for full implementation. This supersedes the 25 h/week figure in the M0 capacity table above for planning purposes; it does not alter `SPEC.md`, which records the original assumption.
- **Recomputed budget**: 2026-07-31 to 2026-08-10 23:59 Asia/Shanghai is ~10 days, giving **~42.9 h at 30 h/week and ~57.1 h at 40 h/week**. `PLAN.md` records 25.2-63.1 raw hours for the 757 listed actions before red/green reruns, full regressions, review, evidence recording, and 66 commit boundaries. The full signed scope therefore closes only at the optimistic end of that range with negligible rework; M1-M3 is a defensible commitment and M4 remains stretch.
- **Host prerequisites re-verified 2026-07-31, all PASS**: Git 2.47.1 (SPEC requires 2.43+), CPython 3.12.12 via uv, uv 0.9.29, Node 22.14, Docker server 29.6.1 responding, and OS keyring resolving to `keyring.backends.Windows.WinVaultKeyring`. Keyring had not previously been verified and is required by SPEC 5.3 for the Secret Path Set installation key.
- **Blocking M1 start**: `PLAN.md` requires a new exact M1 `PLAN.md` revision plus an independent document review before retained M1 execution, and requires preserving the superseded M0 plan in a documented course-artifact location. Neither has been done. The R1/R2/R3 plans were produced with a pinned personal `writing-plans` under Codex; that skill is still not loadable in the Claude Code session, so the M1 revision should be produced the same way to keep plan provenance consistent.
- **Owner-only external enablement, none yet done**: GitHub Pages, GHCR publication, and any package trusted publisher require repository-owner action; no `.github/workflows/` exists yet; the `gh` CLI is not installed on this host. The NJU/GitLab remote is still unconfigured - `origin` is the only remote - and remains a hard final course-delivery dependency rather than an optional one.
- **Promised-but-absent artifacts**: `SECURITY.md` (named in the INITIALIZATION documentation table), `DESIGN.md` (named in SPEC 10.5), `REFLECTION.md` (final phase), `.gitlab-ci.yml` with its exact `unit-test` job, `Makefile`, `pyproject.toml`, `fixtures/`, and all CI workflows. All are expected to be absent at this stage except `SECURITY.md`, which the documentation table implies should already exist.
- **Carried design question**: the Budget Revision field-scope ambiguity raised as non-blocking finding 1 in COLDSTART-3 must be resolved before Task 10 implements `propose_budget`/`approve_budget`.
- **Implementation state**: none retained. `main` is documentation-only and clean.

## 2026-07-31 / COURSE-GAP-1 - Graded course requirements not carried by the current plan

A re-read of the course brief against the repository found requirements that `SPEC.md` and the R3 `PLAN.md` do not fully carry. These are graded deliverables, not preferences.

- **PR workflow is mandatory and is currently unmet.** Brief 4.6 requires one worktree per independent feature/large module, each corresponding to one PR; 4.7 rejects a single commit containing everything and requires a complete commit and PR history. All 12 commits to date went directly to `main` with zero PRs, and `.github/PULL_REQUEST_TEMPLATE.md` has never been used. Documentation-only commits are defensible, but M1 implementation must move to worktree-plus-PR boundaries.
- **Attribution format**: 4.7 requires commit or PR text to state which subagent performed the work and which parts a human changed. Existing commits carry `Co-Authored-By` but not that split.
- **Two-phase per-task review**: 4.6.4 requires a spec-compliance check followed by a code-quality check after every task, with critical issues fixed before the next task. The current sizing does not visibly include this cost.
- **Continuous `PLAN.md` updates**: 4.7 requires marking each task complete with its commit hash as it lands.
- **Submission channel**: deliverable list 5 submits through one NJU GitLab repository link; GitHub is development only. No NJU remote is configured - `origin` is the sole remote. Deliverable 6 requires `.gitlab-ci.yml` with a job named exactly `unit-test`, and deliverable 7 requires the **last CI run to be green**, which is execution work rather than a file drop.
- **Deliverable 9 tension with the signed scope**: the course requires a publicly reachable URL exposing an accessible WebUI. `SPEC.md` v0.1 excludes hosted backend, production public execution, and a writable WebUI, and its only public surface is a sanitized static fixture replay on GitHub Pages. Whether that satisfies deliverable 9 is a grader judgment. It must be decided explicitly and described accurately in `README.md` rather than left implicit.
- **`REFLECTION.md` authorship**: brief section 6 requires the student to write it and forbids AI ghost-writing, permitting disclosed AI polish. No agent may draft its content. Planning its schedule and required questions is permitted.
- **`SECURITY.md`** is named in the `INITIALIZATION.md` documentation table but does not exist; either create it or correct the table.

These findings are inputs to the M1 `PLAN.md` revision, not authorization to change `SPEC.md`. Where a requirement conflicts with the signed specification, the resolution is an explicit owner decision, recorded, and if necessary a proposed specification amendment with a new digest.

## 2026-07-31 / DELIVERY-1 - Delivery channel resolved to GitHub only

- **Owner decision**: no NJU/GitLab remote will be configured; GitHub is the sole remote and delivery channel.
- **Documented basis and its limits**: the course brief is internally inconsistent. Section 4.7 is titled "GitHub 仓库要求", mandates a public GitHub repository, and section 4.8's CI clause requires GitHub Actions on every push; section 5 instead describes submission "通过同一个 NJU Git 仓库链接" and names `.gitlab-ci.yml` as deliverable 6. The recorder searched both course documents exhaustively and found no clause resolving the conflict. The owner resolved it in favour of GitHub using course context not present in these files. This entry records the decision and its basis; it does not claim the brief itself authorizes dropping the NJU link.
- **Retained despite the decision**: `.gitlab-ci.yml` carrying a job named exactly `unit-test` is still produced. Deliverable 6 names the file directly, and frozen `SPEC.md` section 10.5 requires the file's existence and job name rather than a GitLab remote, so keeping it satisfies both without a specification amendment.
- **Residual risk, not closed**: if the grader requires the NJU link as the submission channel, GitHub-only delivery does not satisfy deliverable 5. Configuring a second remote later is a single command, so the cost of reversing this decision stays low; the cost of discovering it late does not.
- **Documents corrected**: `README.md`, `INITIALIZATION.md`, and `AGENTS.md` now state GitHub-only delivery, the passed Stage 4 review, the M1 plan revision as the next gate, and the 2026-07-31 host re-verification. `AGENTS.md` additionally carries the course workflow rules that bind from M1: worktree-per-module with one PR each, subagent/human attribution in commit or PR text, the two-phase per-task review, and continuous `PLAN.md` commit-hash updates.
- **`SPEC.md`**: unchanged and unaffected. Its `.gitlab-ci.yml` requirement remains satisfiable.

## 2026-07-31 / PLAN-M1-R1 - Candidate retained-implementation plan

- **Skill**: `writing-plans`, used to revise the signed R3 roadmap into an M1-only candidate with executable TDD slices, explicit review gates, and course workflow evidence.
- **Authority inputs**: frozen `SPEC.md` remained byte-unchanged at SHA-256 `2F1434AB29C3B7205B13CA96FE35D18C7666729F633EDF984F1DFCA54F0663BC`; superseded M0 R3 `PLAN.md` was verified at SHA-256 `93ADDFE784DC510E5D621E3CFABFD65814C5352B72D6DA3A79C876700C7490CE`.
- **Required archive**: before replacing the root plan, the M0 R3 bytes were copied to `docs/architecture/PLAN-M0-R3-2026-07-31.md`. The archive is 1,194,551 bytes and retains the exact R3 digest above.
- **Candidate artifact**: root `PLAN.md` is M1-R1 candidate SHA-256 `C19C351A877351214C9D915A6EE23A79AA9FC9EE6C52ADAA879B91F03B6EE5AD`, 1,228,522 bytes and 27,369 lines. It is review input, not retained-implementation authority.
- **M1 boundary**: retained re-execution of Tasks 1 and 2A, then 2B-17, is represented by 27 task labels in 8 serial module worktrees/GitHub PRs. Legacy Tasks 2, 7, 11, and 12 are replaced by 8 slices, each with its own failing selector, focused green selector, exact stage set, log label, and implementation commit; aggregate parent commits are forbidden.
- **Review workflow**: every task requires a fresh implementation subagent, then an ordered spec-compliance review and code-quality review, with critical fixes and re-review before the next task. Commit and PR text must identify the subagent and exact human changes. `PLAN.md` is updated immediately with every observed task SHA and persisted by the module ledger commit.
- **Budget decision**: selected the narrow reading of SPEC 10.2. Only its seven table resource categories, represented by eight scalar ceilings, plus the dated returned-model price mapping are Budget proposal fields. Per-Task tranche/call/Attempt/stale/resume/no-progress limits, action/check timeouts, provider retries, and the warning percentage are fixed code-owned `V01_MECHANISM_LIMITS`; they are rejected as Budget extras and not accepted from caller-controlled model requests. No SPEC amendment was applied. A reviewer rejection of this textual basis stops M1 and requires a separately hashed owner-approved SPEC clarification proposal.
- **Course decisions carried**: GitHub is the only remote; M3 still owes `.gitlab-ci.yml` with exact `unit-test` and GitHub Actions on every push. The sanitized GitHub Pages replay is the literal deliverable-9 WebUI response but remains an explicit grading risk. Task 6 creates `SECURITY.md`. The student reserves 3-5 hours and solely authors `REFLECTION.md`; no reflection prose was drafted here.
- **Capacity**: the fixed deadline remains 2026-08-10 23:59 Asia/Shanghai with approximately 42.9-57.1 hours at 30-40 h/week. M1-M3 are a conditional committed priority requiring near-upper capacity, low realized action time, prompt reviews/PRs, no SPEC amendment or critical redesign, healthy CI, and owner GitHub permissions. M4 is a sprint target; the plan makes no unconditional full-scope fit claim.
- **Verification**: all 1,034 Markdown fence lines close; `git diff --check` passed; counts are 27 unique M1 ledger labels, 8 unique module branches, and 8/8 split slices with red/green/stage/commit declarations. The R3 archive and frozen SPEC digests were rechecked. No `src/`, `tests/`, `fixtures/`, `.github/workflows/`, `.gitlab-ci.yml`, `pyproject.toml`, or `uv.lock` exists.
- **Human input**: the owner supplied the capacity decision, GitHub-only delivery decision, Budget ambiguity, Stage 4 qualifications, course requirements, and immutable deadline. No human-authored file edit was represented as agent work.
- **Implementation/publication state**: none retained or authorized. No staging, commit, worktree creation, credential access, push, PR, Pages change, or publication occurred. The next step is an owner-dispatched independent review of this exact M1 candidate; only a zero-blocker verdict plus explicit owner `M1 GO` can authorize the first worktree.

## 2026-07-31 / PLAN-M1-REVIEW - Independent M1 document review returns 10 blockers

- **Subject**: M1-R1 candidate `PLAN.md` at SHA-256 `C19C351A877351214C9D915A6EE23A79AA9FC9EE6C52ADAA879B91F03B6EE5AD`, repository at `60a7331`. Reviewer confirmed that digest, the frozen `SPEC.md` digest, and the M0 archive digest before starting.
- **Verdict**: `10 BLOCKERS`. **M1 stops before Task 1.** Module coverage passed cleanly: Tasks 1-17 map to 27 slices across 8 module PRs with no omission, duplication, or slice falling between modules.
- **Blocker 1, the consequential one**: the Budget narrow-reading decision cannot claim it needs no `SPEC.md` amendment. The recorder verified this independently and confirms the reasoning. `SPEC.md` line 481 states the table values "are also non-raiseable v0.1 administrative maxima", so a value can be a Budget Revision field **and** non-raiseable at the same time. Line 499 calls the per-Task limits "**likewise** non-raiseable" - pointing at that same dual status. The M1 plan's implicit premise, that non-raiseable implies not-a-Budget-field, is therefore defeated by line 481 itself. Line 493's enumeration of what a Budget Revision "may lower" still gives the narrow reading real support, and section 7 line 397's "allocation rules" plus "governs Run and Task allocation" gives the broad reading real support. Both readings are defensible, so the ambiguity is genuine and only an approved specification clarification can close it.
- **Blockers requiring `SPEC.md` text**: 1 (Budget field boundary), 2 (whether a table ceiling may be lowered to zero; the plan decided `ge=1` on its own), 3 (active Run time semantics - which lifecycle states bill, how approval waits and crash intervals count, start/stop transactions and restart rules; the plan has only a DTO and synthetic threshold tests, no trusted clock, persistent accumulation, or production check).
- **Blockers requiring plan correction only**: 4 (Task 9B calls undefined `_ceiling_for`, so its claimed red is unreachable, and seven of eight global usage metrics have no production caller), 5 (Task 9D calls three undefined functions and orders exhausted-lower-ceiling ahead of the approved-higher-Budget resume path that line 499 requires), 6 (Task 9C in M1-05 runs `tests/contract/test_executor.py`, created by Task 15 in M1-08, under strictly serial modules), 7 (Task 11B returns `GrantedActionIntent`, defined by Task 17), 8 (three stale post-split selector paths), 9 (7B Step 14 writes `test_no_follow_handles.py` while 7B's exact stage set omits it), 10 (GitHub Actions deferred to M3 Task 35A although `SPEC.md` line 561 and course section 4.8 require CI on every push, and M1 performs eight module pushes first).
- **Independent verification by the recorder**: blockers 1, 6, 9, and 10 were checked directly against the cited lines and all four are real. Blocker 6 and 9 are literal execution failures, not stylistic notes. Sampling stopped there; the report's rigor is established.
- **Non-blocking findings**: six directory-level `git add` pathspecs that would absorb unintended changes, an imprecise 7A red condition, and a commit-count statement that was true when written and is now stale.
- **Process observation, recorded rather than smoothed**: `SPEC.md` was signed after three independent reviews each reporting zero blockers, yet a fourth reader working from the plan found three genuine specification gaps. This is the review process working, not failing, but it means the frozen specification was complete enough to sign and not complete enough to implement from. Future gates should treat implementation-facing review as capable of reopening specification text.
- **Implementation state**: none. No worktree, branch, or source file was created. `M1 GO` was not given and cannot be given until a revised plan passes a new independent review.

## 2026-07-31 / SPEC-AMEND-1 - Specification revision 2 approved and applied

- **Human decision**: the owner reviewed `docs/proposals/0001-spec-clarification-budget-and-run-time.md` at SHA-256 `6AA24E1717835BAC2D1325E0CB140E5FFC2705F5A2912C124223799E298120C6` and approved all three gaps as written. Approval was given against the exact proposed wording, not against a summary or an unwritten intent.
- **Superseded artifact**: `SPEC.md` revision 1, SHA-256 `2F1434AB29C3B7205B13CA96FE35D18C7666729F633EDF984F1DFCA54F0663BC`, signed 2026-07-27. It remains the authoritative identity for every decision recorded before this entry. Its exact pre-amendment bytes were copied outside the repository before any edit.
- **New artifact**: `SPEC.md` revision 2, SHA-256 `97E9652D874B606C1673867923C97C29834F63B43ADB3F3E89779B13183E26D6`, 131,011 bytes, 636 lines.
- **Applied diff**: 12 insertions, 2 deletions, confined to sections 7 and 10.2. The recorder printed the complete diff and confirmed it matched the approved wording verbatim before computing the new digest. Nothing outside the proposed text was altered.
- **Substance**: the Budget Revision field boundary is closed in favour of the narrow reading; scalar table ceilings other than the cost reserve are positive integers; active Run time accumulates only while runtime ownership is held, measured monotonically, with crash intervals closed conservatively at the last Audit Event of that ownership generation.
- **Deliberately not updated**: the rejected M1-R1 `PLAN.md` still cites revision 1 at its lines 15 and 40. It was left untouched so its reviewed identity `C19C351A...E5AD` stays valid, and its own `Frozen input` gate now fails against revision 2, structurally preventing execution of a rejected plan. M1-R2 must cite revision 2.
- **Historical records unchanged**: earlier `AGENT_LOG.md` and `SPEC_PROCESS.md` entries continue to cite revision 1 because they record what was true when written. Only current-state references were updated, namely `README.md`.
- **Authorization boundary**: this approval authorizes applying the text and recording the digest. It does not authorize implementation, `M1 GO`, credentials, push, or publication. Review blockers 4-10 remain open and belong to the M1-R2 plan revision.
- **New work this creates**: gap 3 requires a monotonic clock port, a durable cumulative total, interval open/close inside the existing permit-consumption and `RunStop` transactions, recovery closure from the last Audit Event, and an action-boundary check. Estimated 1.5-3 hours; M1-R1 had none of it.
- **Lesson**: a digest freezes bytes, not completeness. Three specification-only reviews agreed the text had no blockers; the first reader who tried to build from it found three. Freezing should be treated as a checkpoint that survives amendment, not as a claim that amendment will never be needed.

## 2026-07-31 / PLAN-M1-R2 - Corrected candidate for independent review

- **Skill and authority inputs**: `writing-plans` was used against approved `SPEC.md` revision 2, SHA-256 `97E9652D874B606C1673867923C97C29834F63B43ADB3F3E89779B13183E26D6`, 131,011 bytes and 636 lines. No specification text was changed.
- **Rejected-plan archive**: M1-R1 SHA-256 `C19C351A877351214C9D915A6EE23A79AA9FC9EE6C52ADAA879B91F03B6EE5AD` was archived byte-for-byte at `docs/architecture/PLAN-M1-R1-2026-07-31.md`; the archive is 1,228,522 bytes and remains non-executable history.
- **Candidate artifact**: root `PLAN.md` is M1-R2 candidate SHA-256 `659A8097A712F06D086103A3D20B5E83A147A06AF300D5BDB3B1E08E8DAE70F0`, 1,351,845 bytes and 29,987 total lines. It cites SPEC revision 2 and preserves 27 unique M1 slices in 8 strictly serial module worktrees/PRs.
- **B4 closed**: Task 9B defines total ceiling and fixed-maximum mappings for all eight global metrics, fails closed on unknown metrics, names each trusted production producer, and distributes 80/100-percent tests across the first module in which each real producer exists.
- **B5 closed**: Task 9D defines `_remaining_task_calls`, `_table_or_task_hard_cap_reached`, and `_approved_higher_budget_restores_capacity` as total functions. Its decision order distinguishes immutable maxima from a lower current Revision ceiling before requiring an approved higher Budget plus exact resume.
- **B6-B7 closed**: Task 9C no longer forward-runs Task 15's absent executor file; Task 15 owns that later regression. Task 11B contains no `GrantedActionIntent` dependency; Task 17 owns the type, protocol extension, recovery branch, and runtime composition together.
- **B8-B9 closed**: the three split-task selector paths now name their owning files and identify the historical parent commands they replace. Slice 7B's exact stage set includes `tests/unit/adapters/repository/test_no_follow_handles.py`.
- **B10 closed**: Task 1 creates a minimal read-only-permission GitHub Actions workflow with `push` and `pull_request` triggers and offline lint/unit jobs before the first M1 push; Task 35A later expands that same file rather than creating the first workflow.
- **Revision-2 Gap 3 absorbed**: Tasks 9/9A add the durable cumulative/open-interval schema, state values, synthetic threshold red tests, and trusted active-time budget evaluation. Slices 11A/11B add the monotonic clock port/adapter, open the interval in the Permit-consumption transaction, close it in the `RunStop` transaction, seal orphaned generations at their last committed Audit Event, and check the persisted-plus-open duration at every action boundary.
- **First correction round, stated accurately**: the first verification correction fixed exactly the four Task 11 helpers supplied by that defect report, then reported a clean systemic private-helper sweep. That sweep claim was false: the check as performed did not cover the class. The Task 9D canonical-order and Task 17 Granted-Action recovery corrections remain genuine and unchanged.
- **Second private-helper verification round**: an exact CommonMark-fence sweep found 44 unresolved private calls: 11 already attributed in prose and 33 with neither definition nor mention. `_require_expected_sequence` was undefined on the first line of the same `_require_consumable_runtime_permit` function repaired in round 1; the original defect report itself named only four of that function's five missing helpers. Round 2 defines the safety-load-bearing subset in each owning task, removes Task 11's premature Task 24D unresolved-set call, and adds an exhaustive convention for the remaining prose-owned or safety-neutral factoring. The final sweep is 121 distinct definitions, 96 distinct calls, 13 unresolved, 13 prose-attributed, and zero silently undefined.
- **Zero-blocker pre-review polish**: after a zero-blocker verification pass performed by the assisting agent, this polish expanded line 30 from compressed ranges to the ledger's exact 27-slice order and documented why `ACTIVE_RUN_SECONDS` remains decimal despite its integer ceiling; that pass is not the independent review required by the Execution Gate because the assisting agent co-authored the correction rounds. The gate is unchanged: an owner-dispatched independent document review must still return zero blockers before the owner may write `M1 GO`.
- **Dispatch-closure repair basis**: what first appeared to be an owner design choice was resolved by rereading frozen SPEC revision-2 lines 299, 300, 302, 459, and 460. Those lines require dispatch closure before `PAUSED`, persistence through `INDETERMINATE`, closure after the current atomic action settles, and exact human resolution before reopening. Task 9 therefore owns a durable general Run flag plus canonical cause set; Task 9B closes it in the settlement transaction, Task 9D is the only Budget reopen CAS after approved higher capacity plus exact resume, and Task 11's barrier reason is only a derived copy. This is a specification-derived repair, not owner approval; the owner may reverse it if their reading of those frozen lines differs.
- **Capacity posture**: the revision/proposal/re-review round consumes an estimated 6-10 hours and leaves about 25 implementation hours. The revised M1 estimate remains 29.7-67.7 hours; M1 is a priority with explicit schedule risk, M2-M3 are unauthorized roadmap without a credible deadline claim, and M4 is post-M3 roadmap rather than a sprint promise.
- **Independent-review cross-check**: an owner-dispatched independent review by Gemini 3.1 Pro on 2026-08-01 returned four claimed blockers; three were not sustained on cross-check, because two arose from imprecise wording in the review prompt rather than plan defects and one is contradicted by the writers at `PLAN.md` lines 10947 and 11051, which already canonicalize before the readers validate; the fourth was genuine but was a test-diagnostics defect rather than a blocker and was fixed here together with two further instances the review did not find. The review verdict is not a pass and the gate is unchanged: a further owner-dispatched independent review must return zero blockers before the owner may write `M1 GO`.
- **Authority boundary**: this candidate is **review input only**. It grants no implementation authority. An owner-dispatched independent M1-R2 document review must return zero blockers, after which only the owner may record `M1 GO` and separately authorize creation of the first worktree. No implementation, worktree, branch, commit, push, credential access, publication, or `REFLECTION.md` content was produced in this revision round.

## 2026-08-01 / TASK-001 - Python 3.12 offline test spine

- **Base and task**: Task 1 (`TASK-001`) started from `8eb39c25487c5afc7bdeda92dc11ad6253c78637` on `codex/m1-01-foundation`.
- **Implementing agent**: OpenAI Codex (GPT-5), session `019fbd2f-d22c-7dc1-8efc-07e46701c850` completed the retained Task 1 slice. No human edited code.
- **TDD red evidence**: `uv run --no-project --python 3.12 --with pytest==8.3.5 pytest tests/unit/test_package.py::test_package_exposes_initial_version -q` exited 1 before package implementation with `ModuleNotFoundError: No module named 'apexcrew'`, `ERROR tests/unit/test_package.py`, and `1 error in 0.10s`.
- **CI red evidence**: `uv run --python 3.12 pytest tests/contract/test_bootstrap_ci.py::test_minimal_ci_runs_quality_and_offline_tests_on_every_push -q` exited 1 before workflow creation with `FileNotFoundError: [Errno 2] No such file or directory: '.github\\workflows\\ci.yml'` and the named selector failed.
- **Lock and focused green evidence**: `uv lock --python 3.12` used CPython 3.12.12 and reported `Resolved 51 packages in 3.15s`; `uv sync --python 3.12 --frozen --all-groups` used CPython 3.12.12 and reported `Installed 46 packages in 1.02s`. `uv run --python 3.12 pytest tests/unit/test_package.py::test_package_exposes_initial_version -q` and `uv run --python 3.12 pytest tests/contract/test_bootstrap_ci.py -q` each exited 0 and printed `.                                                                        [100%]`.
- **Boundary-gate blocker**: the first completion attempt stopped when `uv run --python 3.12 ruff format --check .` exited 1 under Ruff 0.16.1 with `3 files would be reformatted, 25 files already formatted`. Ruff had reached Python fences in `PLAN.md`, `docs/architecture/PLAN-M1-R1-2026-07-31.md`, and `docs/architecture/PLAN-M0-R3-2026-07-31.md`; rewriting those reviewed documents would have invalidated the pinned PLAN digest and recorded line-number citations.
- **Boundary-gate resolution and attribution**: an assisting Claude agent added `[tool.ruff] extend-exclude = ["*.md"]` to `pyproject.toml`, with the explanatory comment that the formatter owns source rather than reviewed documentation. This was an assisting-agent change, not a human change. OpenAI Codex independently re-ran and observed every gate below before recording completion.
- **Mechanical green evidence**: `uv run --python 3.12 ruff format --check .` exited 0 with `4 files already formatted`; `uv run --python 3.12 ruff check .` exited 0 with `All checks passed!`; `uv run --python 3.12 pytest -q` exited 0 with `..                                                                       [100%]`; `uv run --python 3.12 mypy src` exited 0 with `Success: no issues found in 1 source file`.
- **Reviewed-document integrity**: after the resolution, `PLAN.md` remained `659A8097A712F06D086103A3D20B5E83A147A06AF300D5BDB3B1E08E8DAE70F0`, `SPEC.md` remained `97E9652D874B606C1673867923C97C29834F63B43ADB3F3E89779B13183E26D6`, the M1-R1 archive remained `C19C351A877351214C9D915A6EE23A79AA9FC9EE6C52ADAA879B91F03B6EE5AD`, and the M0-R3 archive remained `93ADDFE784DC510E5D621E3CFABFD65814C5352B72D6DA3A79C876700C7490CE`. Task 1 did not instruct an in-task PLAN edit, and its exact stage set omits `PLAN.md`, so its checkboxes and reviewed bytes were left unchanged for the later module ledger commit.
- **Plan defect carried forward**: Task 1 pins `ruff>=0.8,<1`, which now resolves to a formatter whose repository-wide `format --check .` gate reaches reviewed Markdown outside the task's modification authority. The repository-level Markdown exclusion is required for every later local slice and for any M3 CI expansion; it must not be dropped or rediscovered as a host-specific issue.
- **Changed paths**: `.gitignore`, `.github/workflows/ci.yml`, `.python-version`, `pyproject.toml`, `src/apexcrew/__init__.py`, `tests/conftest.py`, `tests/contract/test_bootstrap_ci.py`, `tests/unit/test_package.py`, `uv.lock`, and `AGENT_LOG.md`.
- **Commit subject**: `chore: establish python test spine`.
- **Review boundary**: spec-compliance and code-quality reviews are deferred to the M1-01 module PR review, as required by the commit trailers. No push was performed.

## 2026-08-01 / TASK-002A - Exact immutable revision foundation

- **Base and task**: Task 2A (`TASK-002A`) started from `12d112ed4d7b937202d147794c9fc34d16347ba6` on `codex/m1-01-foundation`.
- **Implementing agent and skill**: OpenAI Codex (GPT-5), session `019fbd36-e7a7-7d62-8323-98e0dd7ceddf`, used the `tdd` skill and completed the retained Task 2A slice. No subagent or human edited code.
- **TDD red evidence**: `uv run --python 3.12 pytest tests/unit/domain/test_revisions.py::test_budget_revision_rejects_v01_mechanism_fields tests/unit/domain/test_revisions.py::test_revision_documents_reject_secret_globs_and_mismatched_origin -q` exited 1 before domain implementation. Pytest printed `ERROR collecting tests/unit/domain/test_revisions.py`, `E   ModuleNotFoundError: No module named 'apexcrew.domain'`, and two `ERROR: found no collectors` lines for the requested selectors. This matched the required red failure exactly.
- **Focused green evidence**: rerunning the exact red selector exited 0 with `..........                                                               [100%]`. `uv run --python 3.12 python -c "from apexcrew.domain.revisions import BudgetRevisionDocument, ModelConfigurationRevisionDocument, PolicyRevisionDocument, revision_digest; assert callable(revision_digest)"` exited 0 with no output. `uv run --python 3.12 mypy src/apexcrew/domain/types.py src/apexcrew/domain/limits.py src/apexcrew/domain/revisions.py` exited 0 with `Success: no issues found in 3 source files`. `git diff --check -- src/apexcrew/domain tests/unit/domain/test_revisions.py` exited 0 with no output.
- **Mechanical reconciliation**: the first `uv run --python 3.12 ruff format --check .` exited 1 with `2 files would be reformatted, 7 files already formatted`; only the displayed line-layout changes were applied manually. The next `uv run --python 3.12 ruff check .` exited 1 with five plan-snippet lint findings: two source and two test `FURB157` integer-string `Decimal` constructors plus one test `I001` import order. Only Ruff's displayed replacements and import ordering were applied manually; no field, constraint, assertion, or tested value changed. Ruff was never run in write or fix mode.
- **Final mechanical green evidence**: `uv run --python 3.12 ruff format --check .` exited 0 with `9 files already formatted`; `uv run --python 3.12 ruff check .` exited 0 with `All checks passed!`; `uv run --python 3.12 pytest -q` exited 0 with `............                                                             [100%]`; `uv run --python 3.12 mypy src` exited 0 with `Success: no issues found in 5 source files`.
- **Boundary and document integrity**: `src/apexcrew/domain/commands.py` and `tests/unit/domain/test_commands.py` remained absent. `PLAN.md`, `SPEC.md`, the M1-R1 archive, and the M0-R3 archive retained their recorded SHA-256 digests `659A8097A712F06D086103A3D20B5E83A147A06AF300D5BDB3B1E08E8DAE70F0`, `97E9652D874B606C1673867923C97C29834F63B43ADB3F3E89779B13183E26D6`, `C19C351A877351214C9D915A6EE23A79AA9FC9EE6C52ADAA879B91F03B6EE5AD`, and `93ADDFE784DC510E5D621E3CFABFD65814C5352B72D6DA3A79C876700C7490CE`.
- **Changed paths**: `src/apexcrew/domain/__init__.py`, `src/apexcrew/domain/types.py`, `src/apexcrew/domain/limits.py`, `src/apexcrew/domain/revisions.py`, `tests/unit/domain/test_revisions.py`, and `AGENT_LOG.md`.
- **Commit subject**: `feat(domain): define immutable revision values`.
- **Review boundary**: spec-compliance and code-quality reviews are deferred to the M1-01 module PR review. No push was performed.

## 2026-08-01 / TASK-002B - Closed command contracts

- **Base and task**: Task 2B (`TASK-002B`) started from `2a0f755` on `codex/m1-01-foundation`.
- **Implementing agent and skill**: OpenAI Codex (GPT-5), session `019fbd3f-f3a1-7920-939b-8ef5518baedd`, used the `tdd` and `writing-plans` skills and completed the retained Task 2B slice. No subagent, assisting agent, or human edited code.
- **TDD red evidence**: `uv run --python 3.12 pytest tests/unit/domain/test_commands.py::test_command_envelope_rejects_unknown_payload_fields -q` exited 1 before `commands.py` existed. Pytest printed `ERROR collecting tests/unit/domain/test_commands.py`, `E   ModuleNotFoundError: No module named 'apexcrew.domain.commands'`, and `ERROR: found no collectors for C:\Users\29119\Desktop\AI4SE\tests\unit\domain\test_commands.py::test_command_envelope_rejects_unknown_payload_fields`.
- **Focused green evidence**: `uv run --python 3.12 pytest tests/unit/domain/test_commands.py -q` exited 0 with `........                                                                 [100%]`. `uv run --python 3.12 mypy src/apexcrew/domain/types.py src/apexcrew/domain/limits.py src/apexcrew/domain/revisions.py src/apexcrew/domain/commands.py` exited 0 with `Success: no issues found in 4 source files`.
- **Mechanical reconciliation**: the first `uv run --python 3.12 ruff format --check .` exited 1 with `2 files would be reformatted, 9 files already formatted`; only the displayed line-layout changes were applied manually. The next `uv run --python 3.12 ruff check .` exited 1 with two `I001` import-order findings and four `UP037` redundant quoted-annotation findings; only Ruff's displayed import ordering and quote removals were applied manually. Ruff was never run in write or fix mode; no type, field, constraint, validator branch, assertion, or tested value changed.
- **Final mechanical green evidence**: `uv run --python 3.12 ruff format --check .` exited 0 with `11 files already formatted`; `uv run --python 3.12 ruff check .` exited 0 with `All checks passed!`; `uv run --python 3.12 pytest -q` exited 0 with `....................                                                     [100%]`; `uv run --python 3.12 mypy src` exited 0 with `Success: no issues found in 6 source files`.
- **Boundary verification**: before the ledger edit, `git status --short` listed only untracked `src/apexcrew/domain/commands.py` and `tests/unit/domain/test_commands.py`. Task 2A source/tests and reviewed documents were unchanged. The slice stage set is exactly `src/apexcrew/domain/commands.py`, `tests/unit/domain/test_commands.py`, and `AGENT_LOG.md`.
- **Changed paths**: `src/apexcrew/domain/commands.py`, `tests/unit/domain/test_commands.py`, and `AGENT_LOG.md`.
- **Commit subject**: `feat(domain): define closed command contracts`.
- **Review boundary**: spec-compliance and code-quality reviews are deferred to the M1-01 module PR review. No push was performed.

## 2026-08-01 / TASK-003 - Durable Audit state and command idempotency

- **Base and task**: Task 3 (`TASK-003`) started from `bdc6414a0202dbea57d812884cb632188893ae12` on `codex/m1-02-durable-model`.
- **Implementing agent and skill**: OpenAI Codex (GPT-5), session `0cd31cb6-42b1-48b4-a8f3-56eb0d49fd63`, used the `tdd` and `andrej-karpathy-skills:karpathy-guidelines` skills. No subagent, assisting agent, or human edited code.
- **First TDD red evidence**: `uv run pytest tests/contract/test_state_store.py::test_identical_command_replay_returns_committed_outcome -q` exited 1. Pytest printed `ERROR collecting tests/contract/test_state_store.py`, `E   ModuleNotFoundError: No module named 'apexcrew.adapters'`, and `ERROR: found no collectors for C:\Users\29119\Desktop\AI4SE\tests\contract\test_state_store.py::test_identical_command_replay_returns_committed_outcome`.
- **First focused green evidence**: rerunning that exact selector exited 0 with `.                                                                        [100%]`.
- **Journal TDD red evidence**: `uv run pytest tests/contract/test_state_store.py::test_sqlite_effect_intent_and_result_survive_restart tests/contract/test_state_store.py::test_state_and_audit_roll_back_together -q` exited 1 with `FFF                                                                      [100%]`. The restart case reported `AttributeError: 'SqliteStateStore' object has no attribute 'record_intent'`; the two rollback parameters reported that `InMemoryStateStore` and `SqliteStateStore` had no `fail_next_commit_after_state_write_for_test` method.
- **Journal focused green evidence**: rerunning that exact two-selector command exited 0 with `...                                                                      [100%]`.
- **Additional parity reds**: `uv run --python 3.12 pytest tests/contract/test_state_store.py::test_malformed_intent_payload_has_the_same_closed_error -q` exited 1 with `FF                                                                       [100%]`; both adapters leaked `json.decoder.JSONDecodeError` instead of `EFFECT_INTENT_PAYLOAD_NOT_CANONICAL`. `uv run --python 3.12 pytest tests/contract/test_state_store.py::test_sqlite_rejects_a_result_whose_typed_columns_do_not_match_json -q` exited 1 with `F                                                                        [100%]` and `Failed: DID NOT RAISE <class 'apexcrew.domain.effects.StateConflict'>` after the duplicated `result_class` column was changed. The corresponding selectors then exited 0 with `..                                                                       [100%]` and `.                                                                        [100%]`.
- **Focused state-contract green evidence**: `uv run pytest tests/contract/test_state_store.py -q` exited 0 with `...........................                                              [100%]`. This covers both adapters' replay/conflict behavior, contiguous and stale Audit CAS, copied-state/SQLite rollback, malformed and mismatched digest/binding rejection, deterministic unsettled ordering, SQLite restart, fully populated codec round trips, stored-column binding checks, and a two-connection expected-sequence race.
- **Mechanical reconciliation**: the first `uv run --python 3.12 ruff format --check .` exited 1 with `4 files would be reformatted, 12 files already formatted`; only Ruff's displayed layout changes were applied manually. The first `uv run --python 3.12 ruff check .` exited 1 with one `SIM102`; only the displayed behavior-preserving combined condition was applied. Ruff was never run in write or fix mode. The first `uv run --python 3.12 mypy src` exited 1 with three typing errors: two string-vs-`CommandStatus` arguments and one optional resulting sequence; the enum and explicit non-`None` narrowing fixed them without changing a constraint or tested value.
- **Final mechanical green evidence**: `uv run --python 3.12 ruff format --check .` exited 0 with `16 files already formatted`; `uv run --python 3.12 ruff check .` exited 0 with `All checks passed!`; `uv run --python 3.12 pytest -q` exited 0 with `...............................................                          [100%]`; `uv run --python 3.12 mypy src` exited 0 with `Success: no issues found in 10 source files`.
- **Plan interpretations**: the first import is `apexcrew.adapters.state.memory`, so the required red cannot first name the missing SQLite module while both adapters are absent. The pasted replay assertion names nonexistent `CommandOutcome.sequence`; the implementation uses `resulting_sequence`. The pasted test omits `make_pause_command`, `accepted_outcome`, and several imports. Its new database starts at `expected_sequence=3` despite requiring exact contiguous CAS; the retained test establishes Audit events 1-3 first, preserving the stated 3/4 values without weakening the exact sequence check. The receipt schema requires `repository_id`, but neither `CommandEnvelope` nor any Task 3 table/method supplies it and the `runs` table belongs to Task 8; Task 3 therefore stores the only schema-compatible temporary empty binding, while still binding request ID, Run ID, and full canonical envelope digest. The migration file layout and `_commit_state_and_event` callback signature are not specified, so the numbered migration ledger remains inside the declared `sqlite.py` path and both adapters use the same mutation-callback primitive. Step 3 lists effect tables, while Step 5 says they should still be absent; the tables were created in migration 1 and the observed Step 5 red was the genuinely missing typed methods/hooks.
- **Reviewed-document integrity**: `PLAN.md`, `SPEC.md`, the M1-R1 archive, and the M0-R3 archive retained SHA-256 digests `659A8097A712F06D086103A3D20B5E83A147A06AF300D5BDB3B1E08E8DAE70F0`, `97E9652D874B606C1673867923C97C29834F63B43ADB3F3E89779B13183E26D6`, `C19C351A877351214C9D915A6EE23A79AA9FC9EE6C52ADAA879B91F03B6EE5AD`, and `93ADDFE784DC510E5D621E3CFABFD65814C5352B72D6DA3A79C876700C7490CE`.
- **Changed paths**: `src/apexcrew/domain/effects.py`, `src/apexcrew/adapters/state/__init__.py`, `src/apexcrew/adapters/state/memory.py`, `src/apexcrew/adapters/state/sqlite.py`, `tests/contract/test_state_store.py`, and `AGENT_LOG.md`.
- **Commit subject**: `feat(state): persist audit and command receipts`.
- **Review boundary**: spec-compliance and code-quality reviews are deferred to the M1-02 module PR review. No push was performed.

## 2026-08-01 / TASK-004 - Durable scripted model completion

- **Base and task**: Task 4 (`TASK-004`) started from `627fda6e5a21485437da22d0136aaf4e3755d5e0` on `codex/m1-02-durable-model`.
- **Implementing agent and skill**: OpenAI Codex (GPT-5), session `019fbd6e-8e6e-7120-881d-2a5167a91978`, used the `tdd` and `andrej-karpathy-skills:karpathy-guidelines` skills. No subagent, assisting agent, or human edited code.
- **First TDD red evidence**: `uv run --python 3.12 pytest tests/unit/domain/test_model_requests.py::test_unapproved_returned_model_is_charged_but_not_released -q` exited 1 before the model boundary existed. Pytest printed `ERROR collecting tests/unit/domain/test_model_requests.py`, `E   ModuleNotFoundError: No module named 'apexcrew.domain.model'`, and `ERROR: found no collectors for C:\Users\29119\Desktop\AI4SE\tests\unit\domain\test_model_requests.py::test_unapproved_returned_model_is_charged_but_not_released`.
- **Scripted-adapter green evidence**: `uv run --python 3.12 pytest tests/contract/test_scripted_model.py::test_scripted_model_consumes_one_completion_without_network -q` exited 0 with `.                                                                        [100%]`; socket creation was replaced by a hard failure, one queued completion was returned, and a second call was rejected.
- **State-adapter TDD red evidence**: `uv run --python 3.12 pytest tests/contract/test_state_store.py::test_sqlite_model_reservation_survives_restart tests/contract/test_state_store.py::test_sqlite_worker_model_owner_survives_restart -q` exited 1 with `FF                                                                       [100%]`; both selectors reported `AttributeError: 'SqliteStateStore' object has no attribute 'reserve_model_request'`.
- **Focused green evidence**: `uv run --python 3.12 pytest tests/contract/test_state_store.py::test_sqlite_model_reservation_survives_restart -q` exited 0 with `.                                                                        [100%]`. `uv run --python 3.12 pytest tests/unit/domain/test_model_requests.py::test_unapproved_returned_model_is_charged_but_not_released -q` exited 0 with `.                                                                        [100%]`. `uv run --python 3.12 pytest tests/unit/domain/test_model_requests.py tests/contract/test_scripted_model.py tests/contract/test_state_store.py -q` exited 0 with `...............................                                          [100%]`. `uv run --python 3.12 mypy src/apexcrew/domain/model.py src/apexcrew/domain/effects.py src/apexcrew/adapters/model src/apexcrew/adapters/state/memory.py src/apexcrew/adapters/state/sqlite.py` exited 0 with `Success: no issues found in 6 source files`.
- **Mechanical reconciliation**: the first `uv run --python 3.12 ruff format --check .` exited 1 with `1 file would be reformatted, 20 files already formatted`; only the displayed boolean-expression, conditional-expression, and digest-field layouts were applied manually. The first `uv run --python 3.12 ruff check .` exited 1 with three `I001` import-order findings; only the displayed import ordering was applied manually. A second formatter check refined two overlong boolean conditions; only that displayed layout was applied. Ruff was never run in write or fix mode, and no constraint, assertion, tested value, or field binding changed for lint.
- **Mechanical green evidence**: `uv run --python 3.12 ruff format --check .` exited 0 with `21 files already formatted`; `uv run --python 3.12 ruff check .` exited 0 with `All checks passed!`; `uv run --python 3.12 pytest -q` exited 0 with `...................................................                      [100%]`; `uv run --python 3.12 mypy src` exited 0 with `Success: no issues found in 13 source files`.
- **Plan interpretations**: Steps 15 and 16 call an undefined `_transaction` and then call Task 3's transaction-owning `_commit_state_and_event` from inside it; that literal structure cannot work because Task 3 rejects nested state transactions. Both SQLite model mutations therefore use Task 3's existing mutation-callback primitive directly. Step 13 does not assign a migration version; the new tables are migration 2 so databases created by Task 3 receive them. Step 12's literal memory settlement accepts a never-reserved or substituted intent and a caller-substituted allowlist; both adapters instead require the exact stored reservation and its allowlist before their settle-once compare-and-set. These are safety-preserving corrections required by the Task 4 prose, not Task 4A retry/recovery work.
- **Changed paths**: `src/apexcrew/domain/model.py`, `src/apexcrew/domain/effects.py`, `src/apexcrew/adapters/model/__init__.py`, `src/apexcrew/adapters/model/scripted.py`, `src/apexcrew/adapters/state/memory.py`, `src/apexcrew/adapters/state/sqlite.py`, `tests/unit/domain/test_model_requests.py`, `tests/contract/test_scripted_model.py`, `tests/contract/test_state_store.py`, and `AGENT_LOG.md`.
- **Commit subject**: `feat(model): add durable scripted completion port`.
- **Review boundary**: spec-compliance and code-quality reviews are deferred to the M1-02 module PR review. No push was performed.

## 2026-08-01 / TASK-004A - Retry only durably closed provider attempts

- **Base and task**: Task 4A (`TASK-004A`) started from `e7497d6fdddbcbc31405bea3116ddd5d85acfdfb` on `codex/m1-02-durable-model`.
- **Implementing agent and skill**: OpenAI Codex (GPT-5), session `019fbd7e-1520-7760-aeac-68720ec7f692`, used the `tdd`, `writing-plans`, and `andrej-karpathy-skills:karpathy-guidelines` skills. No subagent, assisting agent, or human edited code.
- **First TDD red evidence**: `uv run --python 3.12 pytest tests/unit/domain/test_model_retry.py::test_known_closed_rejection_retries_with_new_reserved_attempts -q` exited 1. Pytest printed `ERROR collecting tests/unit/domain/test_model_retry.py`, `E   ImportError: cannot import name 'ProviderAttemptResult' from 'apexcrew.domain.model'`, and `ERROR: found no collectors for C:\Users\29119\Desktop\AI4SE\tests\unit\domain\test_model_retry.py::test_known_closed_rejection_retries_with_new_reserved_attempts`. Zero test bodies executed, exactly matching the required first red.
- **Intermediate TDD evidence**: after the attempt-result and scripted-port contracts were added, the exact known-closed selector exited 1 with `TypeError: DurableModelClient.__init__() got an unexpected keyword argument 'backoff'`. `uv run --python 3.12 pytest tests/contract/test_state_store.py::test_sqlite_model_backoff_survives_restart -q` first exited 1 with `AttributeError: 'SqliteStateStore' object has no attribute 'begin_model_turn_and_reserve'`; after migration 3 and SQLite reservation were added, it exited 1 with `AttributeError: 'SqliteStateStore' object has no attribute 'settle_model_attempt'`. After the in-memory settlement methods were added, `uv run --python 3.12 pytest tests/unit/domain/test_model_retry.py::test_timeout_after_possible_dispatch_is_fully_charged_without_retry -q` exited 1 at the still-missing `backoff` constructor handling, before any provider call.
- **Focused green evidence**: `uv run --python 3.12 pytest tests/contract/test_state_store.py::test_sqlite_model_backoff_survives_restart -q` exited 0 with `.                                                                        [100%]`. `uv run --python 3.12 pytest tests/unit/domain/test_model_retry.py::test_known_closed_rejection_retries_with_new_reserved_attempts -q` exited 0 with `.                                                                        [100%]`. `uv run --python 3.12 pytest tests/unit/domain/test_model_retry.py::test_timeout_after_possible_dispatch_is_fully_charged_without_retry -q` exited 0 with `.                                                                        [100%]`. `uv run --python 3.12 pytest tests/unit/domain/test_model_retry.py tests/contract/test_scripted_model.py tests/contract/test_state_store.py -q` exited 0 with `.......................................                                  [100%]`. This includes memory/SQLite double-settlement rejection, unjournaled-settlement rejection, and reservation/counter/Audit rollback coverage.
- **Mechanical reconciliation**: the first `uv run --python 3.12 ruff format --check .` exited 1 with `5 files would be reformatted, 17 files already formatted`; only Ruff's displayed line-layout changes were applied manually. The first `uv run --python 3.12 ruff check .` exited 1 with two `I001` import-order findings, one `F401` unused import, and one `FURB157` integer-string `Decimal` constructor; only the displayed import ordering, unused import removal, and `Decimal(0)` replacement were applied. Ruff was never run in write or fix mode, and no constraint, assertion, tested value, or field binding changed for lint.
- **Final mechanical green evidence**: `uv run --python 3.12 ruff format --check .` exited 0 with `22 files already formatted`; `uv run --python 3.12 ruff check .` exited 0 with `All checks passed!`; `uv run --python 3.12 pytest -q` exited 0 with `............................................................             [100%]`; `uv run --python 3.12 mypy src` exited 0 with `Success: no issues found in 13 source files`.
- **Plan interpretations and defects**: Step 7 predicts a missing `begin_model_turn`, but its test calls and actually lacked `begin_model_turn_and_reserve`. Steps 12 and 13 use a transaction shape incompatible with Task 3's transaction-owning `_commit_state_and_event`; both SQLite paths use the existing mutation callback instead, avoiding `NESTED_STATE_TRANSACTION`. Step 13's pasted settlement updates a row without first proving the caller supplied the exact stored intent; the retained Task 4 binding check prevents substituted intents from corrupting durable counters. Step 17 requires the unchanged Task 4 scripted contract to pass although Task 4A's Files/Exact Stage exclude that test; a narrow client conversion accepts its legacy queued `ModelCompletion` while new calls use `ProviderAttemptResult`. The prose requires remaining-budget revalidation and an exact budget stop, but trusted Budget ceilings and reservation authority are explicitly Task 9 work and are not present in this slice; Task 4A can persist counters but cannot invent that authority.
- **Open SPEC/debt boundary**: Task 4A now persists provider-attempt number, provider response ID, reported usage JSON, returned model ID, charged amounts, result digest, and normalized result data. It still does not validate `ModelCompletion.requested_model_id` against the request, and the second transaction does not explicitly persist that completion field; only the pre-dispatch request JSON contains the requested model ID. This remains a live divergence from frozen SPEC section 5.4 before M1 can be complete. The existing `uuid4` sources remain the plan-prescribed durable turn/intent ID source; no additional `uuid4` call site was added, but no injectable deterministic source exists. `ScriptedMockLLM` still deliberately consumes by script order and ignores request content.
- **Changed paths**: `src/apexcrew/domain/model.py`, `src/apexcrew/domain/effects.py`, `src/apexcrew/adapters/model/scripted.py`, `src/apexcrew/adapters/state/memory.py`, `src/apexcrew/adapters/state/sqlite.py`, `tests/unit/domain/test_model_retry.py`, `tests/contract/test_state_store.py`, and `AGENT_LOG.md`.
- **Commit subject**: `feat(model): retry only closed provider attempts`.
- **Review boundary**: spec-compliance and code-quality reviews are deferred to the M1-02 module PR review. No push was performed.

## 2026-08-01 / TASK-004B - Recover a committed completion without redispatch

- **Base and task**: Task 4B (`TASK-004B`) started from `cbb44878467fe323880da9260f8c3cf8d2a971aa` on `codex/m1-02-durable-model`.
- **Implementing agent and skill**: OpenAI Codex (GPT-5), session `019fbd91-f062-7d01-908f-26f6d31925fe`, used the `tdd`, `writing-plans`, and `andrej-karpathy-skills:karpathy-guidelines` skills. No subagent, assisting agent, or human edited code.
- **First TDD red evidence**: `uv run --python 3.12 pytest tests/integration/test_model_restart.py::test_restart_releases_committed_completion_without_provider_redispatch -q` exited 1. Pytest printed `ERROR collecting tests/integration/test_model_restart.py`, `E   ImportError: cannot import name 'ModelRecoveryBinding' from 'apexcrew.domain.model'`, and `ERROR: found no collectors for C:\Users\29119\Desktop\AI4SE\tests\integration\test_model_restart.py::test_restart_releases_committed_completion_without_provider_redispatch`. The plan predicted a later missing-`recover_committed` `AttributeError`, but its test imported the Step 5 type before Step 5 created it, so no test body executed.
- **Intermediate TDD evidence**: the first Step 7 command, `uv run --python 3.12 pytest tests/integration/test_model_restart.py::test_recovery_binding_mismatch_releases_no_output -q`, exited 1 during collection with `E   ModuleNotFoundError: No module named 'tests'` because the plan imports a helper through a `tests.contract` package that does not exist. After the helper was made local, the same command exited 1 with `AttributeError: 'DurableModelClient' object has no attribute 'recover_committed'`. The Step 8 command, `uv run --python 3.12 pytest tests/integration/test_model_restart.py::test_committed_completion_with_downstream_intent_is_not_released_twice -q`, exited 1 with `AttributeError: 'SqliteStateStore' object has no attribute 'record_downstream_action_intent'` because that SQLite-backed test was prescribed before the SQLite method. After SQLite persistence was added, both the downstream selector and reopened-restart selector exited 1 only at the missing `recover_committed` method.
- **Focused green evidence**: `uv run --python 3.12 pytest tests/integration/test_model_restart.py::test_restart_releases_committed_completion_without_provider_redispatch -q` exited 0 with `.                                                                        [100%]`; `uv run --python 3.12 pytest tests/integration/test_model_restart.py::test_recovery_binding_mismatch_releases_no_output -q` exited 0 with `.                                                                        [100%]`; `uv run --python 3.12 pytest tests/integration/test_model_restart.py::test_committed_completion_with_downstream_intent_is_not_released_twice -q` exited 0 with `.                                                                        [100%]`. `uv run --python 3.12 pytest tests/integration/test_model_restart.py tests/unit/domain/test_model_retry.py tests/contract/test_state_store.py -q` exited 0 with `.........................................                                [100%]`.
- **Mechanical reconciliation**: the first `uv run --python 3.12 ruff format --check .` exited 1 with `3 files would be reformatted, 20 files already formatted`; only the four displayed line-layout changes were applied manually. The first `uv run --python 3.12 ruff check .` exited 1 with three `I001` import-order findings and one `FURB157` integer-string `Decimal` constructor; only the displayed import moves and `Decimal(0)` replacement were applied. Ruff was never run in write or fix mode, and no constraint, assertion, tested value, or field binding changed for lint.
- **Final mechanical green evidence**: `uv run --python 3.12 ruff format --check .` exited 0 with `23 files already formatted`; `uv run --python 3.12 ruff check .` exited 0 with `All checks passed!`; `uv run --python 3.12 pytest -q` exited 0 with `...............................................................          [100%]`; `uv run --python 3.12 mypy src` exited 0 with `Success: no issues found in 13 source files`.
- **No-redispatch boundary**: `recover_committed` performs one committed-turn journal read and has no call path to `ModelPort`. SQLite migration 4 stores the six-field recovery binding, normalized completion, dispatch result, ownership, committed Audit sequence, and downstream intent pointer. Completion settlement updates the attempt, counters, committed turn, and one Audit event through the existing transaction-owning mutation callback. Recovery returns output only for an exact binding with no recorded downstream intent; all other states return a zero-charge blocked result.
- **Plan interpretations and defects**: Steps 1-4 are internally misordered because the failing test imports the Step 5 type. The prescribed cross-test helper import cannot work because `tests/` and `tests/contract/` have no package markers; the helper was reproduced locally because adding package files would violate the exact stage. Step 8 runs a SQLite test after adding only in-memory behavior, so its predicted failure point is unreachable. Step 11's pasted downstream transaction uses a transaction shape incompatible with the repository's transaction-owning `_commit_state_and_event`; the implementation uses its mutation callback and one atomic commit. Task 4A already consumed migration 3, so Task 4B uses additive migration 4 even though the prompt mentions Task 4's migration 2 as the example. The plan calls `model_attempt_count` in Step 12 prose, but the actual API and test use `model_attempts` plus `len(...)`.
- **Open SPEC/debt boundary**: Task 4B does not validate or explicitly persist `ModelCompletion.requested_model_id`; its recovery binding covers the request and revision digests but not that completion field, so the SPEC 5.4 debt remains open. No new `uuid4` call site was added; the existing non-injectable durable ID sources and request-agnostic `ScriptedMockLLM` remain open. Budget revalidation remains Task 9 work. Task 3's empty repository receipt binding remains untouched for Task 8.
- **Changed paths**: `src/apexcrew/domain/model.py`, `src/apexcrew/domain/effects.py`, `src/apexcrew/adapters/state/memory.py`, `src/apexcrew/adapters/state/sqlite.py`, `tests/integration/test_model_restart.py`, and `AGENT_LOG.md`.
- **Commit subject**: `feat(model): recover committed model turns`.
- **Review boundary**: spec-compliance and code-quality reviews are deferred to the M1-02 module PR review. No push was performed.

## 2026-08-01 / TASK-005 - Canonical scopes and promotion hazards

- **Base and task**: Task 5 (`TASK-005`) started from `9ece93623b8644555d3946123eadf49f8bfb1dff` on `codex/m1-03-scope-policy`.
- **Implementing agent and skill**: OpenAI Codex (GPT-5), session `019fbda3-4403-73b1-9956-87ec8ee20d65`, used the `tdd` and `andrej-karpathy-skills:karpathy-guidelines` skills. No subagent, assisting agent, or human edited code.
- **TDD sequence**: literal unsafe path forms, glob grammar and noncanonical pattern forms, conservative glob proofs, W-to-R/W-to-Q hazards, and exact promotion-order selectors were observed red before each minimum implementation.
- **First red evidence**: `uv run --python 3.12 pytest tests/unit/domain/test_path_grammar.py::test_invalid_or_protected_paths_are_rejected -q` exited 1 during collection with `E   ModuleNotFoundError: No module named 'apexcrew.domain.plan'` and `ERROR: found no collectors for C:\Users\29119\Desktop\AI4SE\tests\unit\domain\test_path_grammar.py::test_invalid_or_protected_paths_are_rejected`.
- **Intermediate red evidence**: `uv run --python 3.12 pytest tests/unit/domain/test_path_grammar.py::test_glob_uses_segment_star_and_complete_double_star tests/unit/domain/test_path_grammar.py::test_glob_rejects_noncanonical_path_forms -q` exited 1 during collection with `E   ImportError: cannot import name 'GlobPattern' from 'apexcrew.domain.plan'`; `uv run --python 3.12 pytest tests/unit/domain/test_path_grammar.py::test_glob_proofs_are_conservative_and_explicit -q` exited 1 during collection with `E   ImportError: cannot import name 'GlobProof' from 'apexcrew.domain.plan'`; `uv run --python 3.12 pytest tests/unit/domain/test_plan_validation.py::test_writer_of_a_reader_input_creates_promotion_hazard tests/unit/domain/test_plan_validation.py::test_writer_of_a_check_input_creates_promotion_hazard tests/unit/domain/test_plan_validation.py::test_duplicate_promotion_entries_cannot_omit_a_hazard_writer -q` exited 1 during collection with `E   ImportError: cannot import name 'CheckDefinition' from 'apexcrew.domain.plan'`.
- **Focused green evidence**: the exact unsafe-path selector exited 0 with `...............                                                          [100%]`; the exact two-selector glob command exited 0 with `.............                                                            [100%]`; the exact proof selector exited 0 with `.                                                                        [100%]`; the exact three-selector plan command exited 0 with `...                                                                      [100%]`; `uv run --python 3.12 pytest tests/unit/domain/test_path_grammar.py tests/unit/domain/test_plan_validation.py -q` exited 0 with `................................                                         [100%]`; and `uv run --python 3.12 mypy src/apexcrew/domain/plan.py` exited 0 with `Success: no issues found in 1 source file`.
- **Mechanical reconciliation**: the first `uv run --python 3.12 ruff format --check .` exited 1 with `2 files would be reformatted, 24 files already formatted`; only its displayed line layouts were applied manually. The next formatter check exposed one missed recursive-return layout and exited 1 with `1 file would be reformatted, 25 files already formatted`; that displayed join was then applied. The first `uv run --python 3.12 ruff check .` exited 1 with one `I001` for the plan's incremental same-module imports; only Ruff's displayed consolidated import block was applied. Ruff was never run in write or fix mode, and no constraint, assertion, tested value, or field binding changed.
- **Final mechanical green evidence**: `uv run --python 3.12 ruff format --check .` exited 0 with `26 files already formatted`; `uv run --python 3.12 ruff check .` exited 0 with `All checks passed!`; `uv run --python 3.12 pytest -q` exited 0 with `........................................................................ [ 75%]` and `.......................                                                  [100%]`; `uv run --python 3.12 mypy src` exited 0 with `Success: no issues found in 14 source files`.
- **Plan interpretations and defects**: Step 14 predicts that collection fails on missing `PlanRevision`, but the prescribed import list encounters the earlier missing `CheckDefinition` name first. Task 5 has no separate execution-slicing amendment or `Exact stage` row; Step 22's `git add` command is its authoritative exact stage and matches the declared Files set plus `AGENT_LOG.md`. The plan's Step 8 claim that every noncanonical path form is proved denied in patterns is broader than its pattern parameter list, which omits the protected-segment, DOS-device, and trailing-space cases present in the path test; the implementation still routes pattern segments through the same validator. Steps 18-19 claim exact acyclic ordering is verified, but the prescribed tests exercise hazard ordering and duplicate entries, not the combined-graph cycle branch. The Task 5 `Consumes` line names Task 3 deterministic state diagnostics, but this pure domain slice neither imports nor uses Task 3 state behavior.
- **Changed paths**: `src/apexcrew/domain/plan.py`, `tests/unit/domain/test_path_grammar.py`, `tests/unit/domain/test_plan_validation.py`, and `AGENT_LOG.md`.
- **Commit subject**: `feat(plan): validate scopes and promotion hazards`.
- **Review boundary**: spec-compliance and code-quality reviews are deferred to the M1-03 module PR review. No push was performed.

## 2026-08-01 / TASK-007A - No-follow repository handles

- **Base and task**: Task 7A (`TASK-007A`) started from `488f09d8909e9e9c96c66a5e3beaf87cb8457ccc` on `codex/m1-04-git-safety` and implemented only source Steps 3-5 plus source Step 6's tests under the Slice 7A exact stage. Source Steps 1-2 and 8-20, including Step 14's later edit to `test_no_follow_handles.py`, remain untouched for Task 7B.
- **Implementing agent and skills**: OpenAI Codex (GPT-5), primary agent `/root`, used the `tdd`, `writing-plans`, and `andrej-karpathy-skills:karpathy-guidelines` skills. The existing approved `PLAN.md` was used instead of creating an additional planning document. No human edited code. Spec reviewer: `/root/task7a_spec_review`. Code-quality reviewer: `/root/task7a_quality_review`.
- **First TDD red evidence**: after writing `tests/unit/adapters/repository/test_no_follow_handles.py` and before creating any repository-adapter source, `uv run --python 3.12 pytest tests/unit/adapters/repository/test_no_follow_handles.py::test_every_component_is_opened_relative_to_its_held_parent -q` exited 1 during collection with exact text `ModuleNotFoundError: No module named 'apexcrew.adapters.repository'` and `ERROR: found no collectors`. The plan required that missing-module text but predicted collection exit code 4; the actual pytest/interpreter combination returned 1, which is recorded rather than rewritten.
- **Intermediate TDD evidence**: the first full slice run exited 1 with two test-support failures: ctypes function objects were unhashable in the signature fixture, and the duplicate-directory fixture omitted `NextEntryOffset`, so its second record was unreachable. Correcting only those fixtures made all then-present 18 cases pass. The spec review then proved that a backslash-containing Windows name let `NtCreateFile` resolve unchecked intermediate components; `test_backslash_cannot_hide_unchecked_windows_components` was added and exited 1 with all three `open`/`try_open`/`try_open_any` parameters failing to raise. After the three sibling guards rejected backslash with `INVALID_HANDLE_RELATIVE_PATH`, the selector exited 0 with three passes. The quality review then proved Python code-point length truncated an astral UTF-16 component before `NtCreateFile`; `test_windows_unicode_string_lengths_use_utf16_bytes` exited 1 with actual `[(4, 6, '<astral>')]` versus expected `[(6, 8, '<astral>x')]`. Explicit UTF-16LE storage and byte lengths made that selector exit 0.
- **Focused and final green evidence**: `uv run --python 3.12 pytest tests/unit/adapters/repository/test_no_follow_handles.py --collect-only -q` exited 0 with `23` tests. `uv run --python 3.12 pytest tests/unit/adapters/repository/test_no_follow_handles.py -q` exited 0 with all cases passing. `uv run --python 3.12 pytest -q` exited 0. `uv run --python 3.12 ruff format --check .` exited 0 with `38 files already formatted`; `uv run --python 3.12 ruff check .` exited 0 with `All checks passed!`; `uv run --python 3.12 mypy src` exited 0 with `Success: no issues found in 22 source files`. The exact-scope `git diff --check` and forbidden pathname-API scan exited 0.
- **Finding 1 - confirmed containment repair**: `StableHandleTree.try_open()` now copies `open()`'s exact `{"", ".", ".."}` rejection using `RepositoryUnsafeError("INVALID_HANDLE_RELATIVE_PATH")` and its cached-node kind rejection using `RepositoryUnsafeError("GIT_STORAGE_KIND_CHANGED")`. Tests cover `try_open("..")`, `try_open("")`, and cached kind change. The `try_open()` containment gap was found by attempting to implement the plan, not by any review; two independent document reviews and four correction rounds did not catch it. The owner-confirmed interpretation derives from SPEC line 519 and the two sibling methods, and the owner may reverse it if those sources are read differently.
- **Finding 2 - specified Windows helpers**: `_query_directory_handle_names` calls only `NtQueryDirectoryFile(FileIdBothDirectoryInformation)` on the supplied held directory handle, filters dot entries, and fails closed on malformed UTF-16, invalid offsets/lengths, duplicate names, or the requested maximum. `_last_status_is_object_name_not_found` accepts only `STATUS_OBJECT_NAME_NOT_FOUND` and `STATUS_OBJECT_PATH_NOT_FOUND`; `_last_status_is_not_a_directory` accepts only `STATUS_NOT_A_DIRECTORY`. Stale NT status is cleared before every new `NtCreateFile`, so a later reparse/identity failure cannot inherit file-fallback authority. No `Path.open`, `Path.exists`, `Path.iterdir`, `Path.resolve`, `lstat`, or pathname reopen/enumeration API was added.
- **Finding 3 - fully typed native wrappers**: every wrapped Win32/NT call has explicit `argtypes` and `restype`: `CreateFileW`, `GetFileInformationByHandle`, `SetFilePointerEx`, `ReadFile`, `CloseHandle`, `NtCreateFile`, and `NtQueryDirectoryFile`. `HANDLE` uses `wintypes.HANDLE`, a pointer-sized type, including `CreateFileW.restype`. `UNICODE_STRING` now uses explicit UTF-16LE bytes, preserves astral components, and rejects byte lengths that would overflow its `USHORT` fields.
- **Finding 4 - Step 6 test support**: `recording_backend`, `recording_windows_backend`, `recording_posix_backend`, `FILE_OPEN_REPARSE_POINT`, and `FILE_SHARE_DELETE` are defined inside the one authorized Task 7A test path. No support file outside the exact stage was required.
- **Finding 5 - coverage classification**: the retained parent-handle traversal and flag tests use simulated POSIX and Windows recording backends. The Windows-host tests load the real DLLs to inspect ctypes declarations but use a fake NT call for directory-buffer/status behavior; this is not host-native filesystem traversal coverage. The spec reviewer also ran a one-off real Windows traversal diagnostic to reproduce the hidden-backslash defect, but it is not a retained test. A real host-native Windows integration test is cheap and possible; it was not added unasked.
- **Ordered reviews**: `/root/task7a_spec_review` initially failed the diff for unchecked backslash traversal, then returned PASS after the red/green repair and found no remaining critical spec issue. Only afterward, `/root/task7a_quality_review` initially failed the diff for UTF-16 code-point/byte truncation, then returned PASS after the red/green repair and found no remaining critical correctness, ABI, status-lifecycle, cleanup, collection, or scope issue.
- **Changed paths**: `src/apexcrew/adapters/repository/__init__.py`, `src/apexcrew/adapters/repository/no_follow.py`, `src/apexcrew/adapters/repository/no_follow_posix.py`, `src/apexcrew/adapters/repository/no_follow_windows.py`, `tests/unit/adapters/repository/test_no_follow_handles.py`, and `AGENT_LOG.md`.
- **Commit subject**: `feat(git): add no-follow path handles`.
- **External state**: no push, publication, credential access, pathname-based repository enumeration, or Task 7B work was performed.

## 2026-08-01 / TASK-006 - Opaque secret and action policy

- **Base and task**: Task 6 (`TASK-006`) started from `a1663fa0d6a60ccd3ee7a2e3577a7055db90ce11` on `codex/m1-03-scope-policy`.
- **Implementing agent and skill**: OpenAI Codex (GPT-5), session `019fbdb1-8ce5-78b1-a14f-07f3f0ba7682`, used the `tdd`, `writing-plans`, and `andrej-karpathy-skills:karpathy-guidelines` skills. No subagent, assisting agent, or human edited code.
- **First red evidence**: `uv run --python 3.12 pytest tests/unit/domain/test_secret_policy.py::test_secret_policy_denies_mixed_case_default_without_returning_path -q` exited 1 during collection with `E   ModuleNotFoundError: No module named 'apexcrew.domain.policy'` and `ERROR: found no collectors for C:\Users\29119\Desktop\AI4SE\tests\unit\domain\test_secret_policy.py::test_secret_policy_denies_mixed_case_default_without_returning_path`.
- **First focused green**: rerunning the exact secret-policy selector exited 0 with `.                                                                        [100%]`.
- **Digest contract evidence**: `uv run --python 3.12 pytest tests/contract/test_secret_policy_digest.py::test_digest_changes_when_host_rule_changes -q` exited 0 with `.                                                                        [100%]`. Despite Step 6 calling this a failing contract, the digest implementation required by Steps 3-4 already existed, so the prescribed Step 7 result was immediately green.
- **Second red evidence**: `uv run --python 3.12 pytest tests/unit/domain/test_action_policy.py::test_default_action_policy_requires_approval_or_hard_denies -q` exited 1 during collection with `E   ImportError: cannot import name 'ActionPolicy' from 'apexcrew.domain.policy'` and `ERROR: found no collectors for C:\Users\29119\Desktop\AI4SE\tests\unit\domain\test_action_policy.py::test_default_action_policy_requires_approval_or_hard_denies`.
- **Second focused green**: rerunning the exact action-policy selector exited 0 with `.                                                                        [100%]`.
- **Aggregate focused green**: `uv run --python 3.12 pytest tests/unit/domain/test_secret_policy.py tests/unit/domain/test_action_policy.py tests/contract/test_secret_policy_digest.py -q` exited 0 with `...                                                                      [100%]`; `uv run --python 3.12 mypy src/apexcrew/domain/actions.py src/apexcrew/domain/policy.py src/apexcrew/adapters/credentials` exited 0 with `Success: no issues found in 4 source files`.
- **Mechanical reconciliation**: the first `uv run --python 3.12 ruff format --check .` exited 1 with `3 files would be reformatted, 30 files already formatted`; only its displayed layout changes were applied manually. The first `uv run --python 3.12 ruff check .` after layout reconciliation exited 1 with one `C401` finding; only Ruff's displayed set-comprehension rewrite was applied. Ruff was never run in write or fix mode, and no constraint, assertion, tested value, field binding, or disclosure behavior changed.
- **Final mechanical green evidence**: `uv run --python 3.12 ruff format --check .` exited 0 with `33 files already formatted`; `uv run --python 3.12 ruff check .` exited 0 with `All checks passed!`; `uv run --python 3.12 pytest -q` exited 0 with 99 passing tests (`........................................................................ [ 73%]` and `...........................                                              [100%]`); `uv run --python 3.12 mypy src` exited 0 with `Success: no issues found in 18 source files`.
- **Non-disclosure boundary**: secret inspection is lexical and performs no filesystem probe, so its result cannot disclose whether a path exists. A denial returns only `SECRET_PATH_DENIED` and `effective secret path`; the opaque digest projects only fixed version labels, an HMAC-SHA-256 value, and the operator-rule count. No raw operator rule is returned by the digest. The keyring adapter was added and type-checked, but no command in this task called `keyring.get_password`, `keyring.set_password`, or otherwise read or wrote the real OS credential store.
- **Plan interpretations and defects**: Step 6 is not a red stage because Steps 3-4 already create the imported module and digest implementation; its Step 7 expected result correctly says PASS. Task 6 has no separate Execution Slicing Amendment row or `Exact stage` row, so Step 18's `git add` list is the authoritative exact stage and matches the declared Files set plus `AGENT_LOG.md`. The prompt's instruction to stage “Task 4's declared path set” conflicts with this Task 6 slice and was interpreted as a numbering typo. The plan implements `SecretPathPolicy.inspect` and `ActionPolicy.classify` as separate primitives: `ActionPolicy.default()` has no secret-policy dependency and, by itself, does not classify an otherwise canonical secret path as `DENY`; composition into effect enforcement is not specified in this slice. The prescribed digest test proves only that different rules change the opaque value, not its stated no-raw-rule projection. The mixed-case denial test proves generic returned fields but does not independently compare timing or inspect any Audit, WebUI, or log projection; none of those integration surfaces exists in Task 6. Step 17 asks for an “exact entry” that is explicitly labeled as an example and omits the prompt-required base SHA, exact commands/output, changed paths, and commit subject, so this evidence-complete entry supersedes that abbreviated example.
- **Changed paths**: `SECURITY.md`, `src/apexcrew/domain/actions.py`, `src/apexcrew/domain/policy.py`, `src/apexcrew/adapters/credentials/__init__.py`, `src/apexcrew/adapters/credentials/keyring.py`, `tests/unit/domain/test_secret_policy.py`, `tests/unit/domain/test_action_policy.py`, `tests/contract/test_secret_policy_digest.py`, and `AGENT_LOG.md`.
- **Commit subject**: `feat(policy): bind opaque secret path denials`.
- **Review boundary**: spec-compliance and code-quality reviews are deferred to the M1-03 module PR review. No push was performed.

## 2026-08-02 / TASK-007B - Bounded Git preflight

- **Base and task**: Task 7B (`TASK-007B`) started from `17cd92d5ab0197a680f7681e09798f649f5c6e9e` on `codex/m1-04-git-safety` and implemented only source Steps 1-2 and 8-20 under the Slice 7B exact stage. Task 8 was not started.
- **Implementing agent and skills**: OpenAI Codex (GPT-5), primary agent and session identifier `/root`, used the `tdd`, `writing-plans`, and `andrej-karpathy-skills:karpathy-guidelines` skills. The signed-off `PLAN.md` supplied the implementation plan, so no duplicate plan or worktree was created. No subagent, assisting agent, or human edited code.
- **First TDD red evidence**: after creating `tests/helpers/git_repository.py` and the source Step 1 tests, `uv run --python 3.12 pytest tests/integration/test_git_preflight.py::test_byte_config_parser_supports_subsections_quotes_and_continuations tests/integration/test_git_preflight.py::test_preflight_rejects_include_and_include_if_before_git_runs tests/integration/test_git_preflight.py::test_preflight_rejects_sparse_and_split_index_bytes_before_git_runs -q` exited 1 during collection with `E   ModuleNotFoundError: No module named 'apexcrew.adapters.repository.git'` and three `ERROR: found no collectors` lines. This matched the replacement slice's missing-module red boundary; pytest returned exit code 1.
- **Parser/layout green evidence**: after the minimum Step 8 implementation, rerunning the exact replacement red selector exited 0 with `......                                                                   [100%]`.
- **Runner TDD evidence**: after adding the mandatory Step 14 replacement-race tests and raw-route tests, `uv run --python 3.12 pytest tests/unit/adapters/repository/test_no_follow_handles.py::test_replaced_bound_component_stops_before_git_spawn tests/integration/test_git_preflight.py::test_runner_rejects_raw_git_route_options_before_spawn -q` exited 1 during collection with `E   ImportError: cannot import name 'GitCommandRunner' from 'apexcrew.adapters.repository.git'` in both test modules. After Steps 12-13, the first full no-follow run exposed only a test-fixture error: simulated POSIX used `Path('/git')`, which is not host-absolute on Windows, so two cases raised `ValueError: ABSOLUTE_GIT_EXECUTABLE_REQUIRED`; using a host-absolute fixture path made `uv run --python 3.12 pytest tests/unit/adapters/repository/test_no_follow_handles.py -q` exit 0 with `...........................                                              [100%]`.
- **Focused green evidence**: the first complete integration run reached all tests but pytest attempted to use the 1 MiB `bytes` parameter as a Windows node ID and errored during setup; explicit descriptive IDs changed no value or assertion. `uv run --python 3.12 pytest tests/integration/test_git_preflight.py -q` then exited 0 with all 37 cases accounted for and two expected non-POSIX skips. `uv run --python 3.12 mypy src/apexcrew/adapters/repository tests/helpers/git_repository.py tests/integration/test_git_preflight.py` exited 0 with `Success: no issues found in 7 source files`.
- **Mechanical reconciliation**: the first focused mypy run found five snippet-era typing errors: one `Any` unpack result, two redundant casts, a non-explicit error re-export, and the runtime-only pytest helper import path. Narrow typing fixes resolved them while the helper itself remained directly checked. The first `ruff format --check .` reported two files requiring only displayed layout changes. The first `ruff check .` reported import ordering, the Python 3.12 `type` alias form, two assertion-shape findings, and one unused test import; only its displayed mechanical changes were applied. Ruff was never run in write or fix mode, and no constraint, assertion value, operation argument, or bound identity changed.
- **Final mechanical green evidence**: `uv run --python 3.12 ruff format --check .` exited 0 with `41 files already formatted`; `uv run --python 3.12 ruff check .` exited 0 with `All checks passed!`; `uv run --python 3.12 pytest -q` exited 0 with the suite reaching `[100%]` and two expected platform skips; `uv run --python 3.12 mypy src` exited 0 with `Success: no issues found in 23 source files`. The exact-scope `git diff --check` and forbidden pathname-API/shell scan exited 0.
- **Containment and command boundary**: config, index, directory inventory, identity revalidation, and complete `.git` storage snapshots use only already-open handles. Production code adds no `Path.open`, `Path.exists`, `Path.iterdir`, `Path.resolve`, `lstat`, pathname reopen, raw shell, or caller-controlled Git route option. `StableHandleTree.try_open()` retains Task 7A's invalid-component and cached-kind guards unchanged. The command runner accepts only the closed typed operation union, validates operands, installs its fixed routing-neutralizing arguments and environment, and calls `assert_stable_for` before `GitSpawner.run`.
- **Plan interpretations and defects**: source Step 1's `from tests.helpers.git_repository` is invalid because `tests/` is not a package; pytest can import the declared helper as `helpers.git_repository`, while strict mypy needs a targeted import-resolution suppression and checks the helper path directly. Step 8 calls `replace` without importing it, while Step 13 uses `TypeAlias` and `GitOid` without importing them; current Ruff also requires Python 3.12's `type` statement instead of the plan's `TypeAlias` spelling. Step 10 calls its linked-worktree test failing even though Step 8 already implements `if layout.worktree_admin_entries: raise ...`; Step 16 likewise adds hostile-storage tests after Step 8 already implements every listed guard, so neither can produce a genuine red in the prescribed source order. Step 14 references `bound_repository_from_backend`, `RecordingGitSpawner`, `replace_component_with_same_kind`, and `absolute_git` without defining any of them; local typed test support had to be supplied within the exact stage. The Step 1 over-limit parameter produces a roughly 1 MiB default pytest node ID and breaks Windows setup until short IDs are supplied. Step 19 claims bounded subprocess output, but the prescribed `SubprocessGitSpawner` uses unbounded `capture_output=True`; this slice implements the reviewed command shape and does not pretend that timeout alone bounds bytes. The prompt's commit sentence says “Task 4's declared path set”; this conflicts with the explicit Task 7B exact stage and was treated as a numbering typo. The amendment clearly replaces parent aggregate commands, but source Steps 8-20 still contain commands; the Slice 7B selector, full integration file, focused mypy command, global gates, and exact stage were treated as sole authority.
- **Changed paths**: `src/apexcrew/adapters/repository/git.py`, `tests/helpers/git_repository.py`, `tests/integration/test_git_preflight.py`, `tests/unit/adapters/repository/test_no_follow_handles.py`, and `AGENT_LOG.md`.
- **Commit subject**: `feat(git): reject unsafe repository layouts`.
- **Review and external-state boundary**: spec-compliance and code-quality reviews are deferred to the M1-04 module PR review as required. No push, publication, credential access, pathname-based production repository traversal, or Task 8 work was performed.

## 2026-08-02 / TASK-008 - Journaled pinned target reservation

- **Base and task**: Task 8 (`TASK-008`) started from `98b08f2545e5dc09576f88dff531ec7c10c2c46d` on `codex/m1-04-git-safety` and implemented PLAN Steps 1-12. No stage, commit, or push was performed by the implementing subagent.
- **Implementing agent and skills**: OpenAI Codex (GPT-5), implementing subagent `/root/task8_impl`, used the `tdd` and `andrej-karpathy-skills:karpathy-guidelines` skills. The human supplied authorization corrections and did not edit the retained Task 8 files during this implementation.
- **First red evidence**: `uv run --python 3.12 pytest tests/integration/test_target_reservation.py::test_creation_intent_is_committed_before_add tests/integration/test_target_reservation.py::test_post_add_wrong_registration_never_locks tests/integration/test_target_reservation.py::test_restart_after_add_reuses_one_intent_and_locks_once -q` exited 1 during collection with `E   ModuleNotFoundError: No module named 'apexcrew.domain.admission'` and no collector for each requested selector. No `worktree add` or `worktree lock` command was reachable.
- **Focused green evidence**: rerunning that exact selector exited 0 with `....                                                                     [100%]`; the two changed-registration parameters account for four cases. `uv run --python 3.12 pytest tests/integration/test_target_reservation.py -q` exited 0 with `.......                                                                  [100%]`.
- **Run-binding discovery and green evidence**: before criterion-authorized test seeding, `uv run --python 3.12 pytest tests/contract/test_state_store.py -q` exited 1 with exactly five parameter-instance failures, all `RUN_NOT_FOUND`, belonging to the identical-replay, request-ID-reuse, and SQLite restart-replay tests. After genuine Run/repository/reservation seeding and the explicit closure test, the four focused selectors exited 0 with seven parameter cases, and the complete contract file exited 0 with all 39 cases passing.
- **Debt #1 closure test**: `test_record_command_requires_existing_run_and_uses_real_repository_binding` proves both adapters reject a fresh store with exact `RUN_NOT_FOUND` and zero Audit append, then accept the same command only after a genuine Run, repository ID, and preallocated Target Reservation binding exists. `record_command()` persists and compares the Run row's repository ID; it never creates a Run.
- **Criterion-authorized contract test changes**: `test_identical_command_replay_returns_committed_outcome` now seeds its genuine binding, retains all three prior Audit events, and proves the same exact replay at the resulting shifted sequence. `test_request_id_reuse_conflicts_without_appending_audit` now seeds genuine and distinct bindings for both `run-command` and `run-other`, retains the exact `IDEMPOTENCY_KEY_REUSE` assertions, and proves neither Run receives a conflict Audit event. `test_sqlite_command_replay_survives_restart` now seeds a genuine binding before the first command and retains the exact restart replay and no-second-append assertions. No assertion was removed, generalized, or weakened.
- **New reservation tests**: `test_creation_intent_is_committed_before_add` proves the Task 3 intent and phase precede Git. `test_post_add_wrong_registration_never_locks` proves mixed/third post-add states stop before lock. `test_restart_after_add_reuses_one_intent_and_locks_once` proves restart loads the same intent and locks without another add. `test_restart_after_lock_settles_without_duplicate_git` proves exact locked recovery emits no Git operation. `test_unobservable_creation_result_is_durable_indeterminate` proves uncertainty is settled durably as `INDETERMINATE`. `test_known_git_error_is_observed_and_settled_without_escaping` proves a known adapter rejection is freshly observed and converted to durable uncertainty.
- **Path-set extension and reason**: the human extended Task 8 to `src/apexcrew/domain/types.py` solely for `RepositoryId`, and to `tests/contract/test_state_store.py` for every test whose sole failure was the newly enforced Run repository binding. The plan assigned debt #1 closure to Task 8 while its declared file list excluded the type and contract paths required to close it. `src/apexcrew/domain/types.py` received only `RepositoryId`; the contract file received only genuine binding support, the three necessary test updates, and the named closure test.
- **Plan interpretations and defects**: Step 1 references undefined `make_creation_intent` and `seeded_reservation_store`, so both typed helpers are local to the integration file because `tests/` is not a package. The prescribed positional `ReservationObservation(...)` calls conflict with Pydantic's keyword-only default, so the immutable document exposes the specified positional signature. Step 1 reads `intent.recorded_sequence` while Step 4 omits that field, so it is excluded local metadata populated from the sole persisted Task 3 effect sequence and does not enter the canonical operation payload. Step 5 references a nonexistent `_ids` source and a transaction shape incompatible with the transaction-owning `_commit_state_and_event`; the intent ID is deterministically bound to Run/reservation/next sequence and both adapters use the existing mutation callback for one state-plus-Audit commit. Migration 5 is additive from Task 4B's migration 4.
- **Locked-worktree inventory defect**: the plan modelled the locked worktree administration directory as three or four entries, while a real checked-out locked worktree from supported Git `2.47.1.windows.1` contains eight: `HEAD`, `commondir`, `gitdir`, `index`, `locked`, `logs`, `refs`, and `ORIG_HEAD`. The mismatch was found by the concrete `test_real_git_adapter_reserves_symbolic_pinned_target_and_refreshes_guard` regression on a real repository, not by either document or code review. The adapter's intentional `--no-checkout` form produced the observed six-entry subset without `index` or `ORIG_HEAD`; all four bookkeeping names are therefore optional, unknown top-level entries still fail closed, and the bounded enumeration cap is 16. Removing that overflow exposed and the same regression caught Git for Windows' `/` spelling for both absolute back-references; exact byte comparison now derives those two trusted expected paths with `Path.as_posix()` rather than accepting alternate paths.
- **Post-review red evidence and critical fixes**: `/root/task8_quality_review` reproduced two high-severity defects. `uv run --python 3.12 pytest tests/integration/test_target_reservation.py::test_conflict_settlement_allows_a_fresh_creation_intent -q` exited 1 for both adapters with exact `EFFECT_INTENT_DUPLICATE` after `CONFLICT` settlement because the plan's fixed reservation idempotency key could not represent its promised fresh intent. `uv run --python 3.12 pytest tests/integration/test_target_reservation.py::test_real_git_adapter_reserves_unicode_direct_ref_without_untyped_failure -q` exited 1 with an untyped `UnicodeEncodeError` after real Git had created the worktree. Creation-intent idempotency keys are now bound to their unique recorded sequence in both adapters, and exact `HEAD` comparison uses the same filesystem encoding as the accepted Git ref operand. Rerunning both selectors exited 0 with three parameter cases, and the complete reservation plus state-store contract selection exited 0 with 53 cases.
- **Ordered reviews**: spec-compliance reviewer `/root/task8_spec_review` returned `PASS` after the inventory, fresh-intent, Unicode-ref, and evidence corrections. Code-quality reviewer `/root/task8_quality_review` first returned `FAIL` with the two high-severity findings above, then returned `PASS` after their focused regressions and fixes; no critical or high-severity finding remains. Human changes to retained files: none.
- **Known bounded limitation**: `logs` and `refs` are allowed top-level Git worktree administration directories, but their contents are not inventoried. The tamper check therefore proves the exact top-level admin-name set and the exact required record/lock bindings, but does not prove anything about entries beneath those two directories; deep inspection is explicitly outside Task 8.
- **Typed Git and containment boundary**: production reservation code uses only closed `GitWorktreeAddNoCheckout` and `GitWorktreeLock` operations plus Task 7 no-follow handle traversal. No `Path.open`, `Path.exists`, `Path.iterdir`, `Path.resolve`, `lstat`, pathname reopen, raw shell, Win32/NT wrapper change, or modification to `try_open()` traversal/cached-kind guards was added.
- **Type and mechanical evidence**: the exact Step 11 mypy command exited 0 with `Success: no issues found in 4 source files`. The first global format check listed only five Task 8 files; its displayed layout was applied manually, never with a write-format/fix command. After review fixes, final `uv run --python 3.12 ruff format --check .` exited 0 with `43 files already formatted`; `uv run --python 3.12 ruff check .` exited 0 with `All checks passed!`; `uv run --python 3.12 mypy src` exited 0 with `Success: no issues found in 24 source files`; `uv run --python 3.12 pytest -q` exited 0, and `uv run --python 3.12 pytest --collect-only` confirmed `176 tests collected`; the full run contained two expected platform skips.
- **Changed paths**: `src/apexcrew/domain/effects.py`, `src/apexcrew/domain/admission.py`, `src/apexcrew/domain/types.py`, `src/apexcrew/adapters/state/memory.py`, `src/apexcrew/adapters/state/sqlite.py`, `src/apexcrew/adapters/repository/git.py`, `tests/integration/test_target_reservation.py`, `tests/contract/test_state_store.py`, and `AGENT_LOG.md`. The pre-existing Task 7B-only `PLAN.md` ledger change was preserved and not edited by Task 8.
- **Commit subject**: `feat(admission): journal target reservation creation`.
- **External state**: no push, publication, credential access, provider request, or model-selected Git operation was performed.

## 2026-08-02 / M1-04 - Required Ubuntu CI portability repair

- **Base and scope**: this repair started from `aa15d1915c6b7b633568cd64c4d2f6730cfaf4f4` on `codex/m1-04-git-safety` and is attributable to `TASK-007A` and `TASK-007B`. OpenAI Codex (GPT-5), primary session `/root`, used the `diagnosing-bugs` and `andrej-karpathy-skills:karpathy-guidelines` skills. No subagent or human edited code.
- **Observed CI failures**: GitHub Actions `quality` failed on Ubuntu because Linux mypy treated the runtime `WinDLL = None` suppression as unused and could not infer `_kernel32` or `_ntdll`; `unit-ubuntu` failed four replacement-race parameter cases because `Path("C:/git.exe")` is relative on POSIX, plus two Windows simulations because Linux's four-byte `wchar_t` tried to interpret explicit UTF-16LE storage as native wide characters. The production `ABSOLUTE_GIT_EXECUTABLE_REQUIRED` denial was not relaxed, and no test or assertion was skipped or removed.
- **Typing red and focused green evidence**: before the repair, `uv run mypy --platform linux src/apexcrew/adapters/repository/no_follow_windows.py` exited 1 with 24 errors matching CI: one unused `type: ignore` and 23 unresolved `_kernel32`/`_ntdll` uses. After the repair, that exact command and its `--platform win32` counterpart each exited 0 with `Success: no issues found in 1 source file`; `uv run --python 3.12 mypy --platform linux src` exited 0 with `Success: no issues found in 24 source files`.
- **Runtime regression evidence**: `uv run --python 3.12 pytest tests/unit/adapters/repository/test_no_follow_handles.py::test_replaced_bound_component_stops_before_git_spawn tests/unit/adapters/repository/test_no_follow_handles.py::test_windows_unicode_string_lengths_use_utf16_bytes tests/unit/adapters/repository/test_no_follow_handles.py::test_windows_stale_not_directory_status_cannot_enable_file_fallback -q` exited 0 with seven parameter cases at `[100%]`. The fixture now resolves its executable path on the host, and the Windows fake reads the stored pointer as raw memory before strict UTF-16LE decoding, independent of host `wchar_t` width.
- **Final Windows mechanical gates**: `uv run --python 3.12 ruff format --check .` exited 0 with `43 files already formatted`; `uv run --python 3.12 ruff check .` exited 0 with `All checks passed!`; `uv run --python 3.12 pytest -q` exited 0 at `[100%]` with the two existing platform skips; `uv run --python 3.12 mypy src` exited 0 with `Success: no issues found in 24 source files`. Ubuntu is pending real CI.
- **Platform evidence limitation and lesson**: the Linux runtime failures could not be reproduced locally because this host is Windows and uses Windows' two-byte `wchar_t`; only the Linux mypy target could be reproduced locally. This is the second time M1-04 has shown that green local Windows gates are not evidence of a green required Windows-and-Ubuntu build. Cross-platform fixtures must use host-neutral paths and explicit wire encodings, and required-platform CI remains the authoritative portability evidence.
- **Preserved boundaries**: `StableHandleTree.try_open()` traversal and cached-kind guards, all native `argtypes`/`restype` declarations, the reservation inventory repair, debt #1 closure, and the absolute Git executable guard are unchanged. No push was performed.
- **Changed paths**: `src/apexcrew/adapters/repository/no_follow_windows.py`, `tests/unit/adapters/repository/test_no_follow_handles.py`, and `AGENT_LOG.md`.
- **Commit subject**: `fix(git): make Windows no-follow checks portable`.
- **Review boundary**: spec-compliance and code-quality reviews are deferred to the M1-04 module PR review.

## 2026-08-02 / TASK-009 - Lease and hard budget authority

- **Base and task**: Task 9 (`TASK-009`) started from `ab8ca2e956b01b33015b619608ef431a25f193ab` on `codex/m1-05-authority` and implemented PLAN Steps 1-25. OpenAI Codex (GPT-5), primary session `/root`, used the `tdd`, `writing-plans`, and `andrej-karpathy-skills:karpathy-guidelines` skills. No subagent or human edited the retained Task 9 files.
- **First lease red**: `uv run --python 3.12 pytest tests/unit/domain/test_leases.py::test_overlapping_write_leases_are_denied -q` exited 1 during collection with `E   ModuleNotFoundError: No module named 'apexcrew.domain.authority'` and `ERROR: found no collectors`; this matched Step 2.
- **Lease green and expiry red/green**: the overlap selector exited 0 with `. [100%]`. The exact Step 10 selector then exited 1 with `AttributeError: 'AuthorityService' object has no attribute 'authorize_write'`; after the state-plus-Audit expiry transition it exited 0 with `. [100%]`.
- **Tranche reds and corrected proof**: the exact Step 14 selector first exited 1 during collection with `ImportError: cannot import name 'CheckSet' from 'apexcrew.domain.authority'`, not the plan's later `NO_PROGRESS` symbol. After those symbols existed it exposed the second collection error `ModuleNotFoundError: No module named 'tests'` from the prescribed `tests.unit.domain.test_leases` import. The sibling test import was corrected without changing production behavior. The prescribed test also allocated tranche 2 while tranche 1 still had eight unconsumed calls, directly contradicting Step 16's one-active-tranche rule. The final test consumes each tranche through eight real worker `reserve_model_attempt` transactions before renewal, and `test_active_tranche_must_be_consumed_before_another_allocation` proves early replacement fails with `TASK_TRANCHE_STILL_ACTIVE` and no Audit advance.
- **SQLite reservation red/green**: the exact Step 19 selector never reached the predicted missing adapter method; it exited 1 during collection with `ModuleNotFoundError: No module named 'tests'` from the same invalid plan import. A local declared-file helper replaced that import. The plan's request also retained Task 4's placeholder Budget digest while installing a different approved digest, which contradicts Step 20's exact revision revalidation; the helper now binds the request to `current_approved_budget`. The final selector exited 0 with `. [100%]`, deriving the trusted reservation as `calls=1`, `input_tokens=1000`, `output_tokens=200`, and `cost_usd=0.0055`, while stale Run B retained zero counters and sequence.
- **Runtime-account red/green**: Step 21 referenced undefined `seeded_authority_store` and `_transaction`. A local helper now seeds a genuine Run/Target Reservation, and the first selector exited 1 with `AttributeError: 'SqliteStateStore' object has no attribute '_transaction'`. The narrow fixture transaction context was added without wrapping `_commit_state_and_event`; the selector then exited 0 with `. [100%]` and proved exact values after reopen plus clock-regression rejection.
- **Focused final green evidence**: `uv run --python 3.12 pytest tests/contract/test_state_store.py::test_sqlite_authority_model_reservation_is_run_bound_and_stale_safe tests/unit/domain/test_leases.py tests/unit/domain/test_authority.py -q` exited 0 with `...... [100%]`. `uv run --python 3.12 pytest tests/contract/test_state_store.py::test_active_runtime_account_survives_restart_and_rejects_clock_regression -q` exited 0 with `. [100%]`. `uv run --python 3.12 pytest tests/unit/domain/test_authority.py tests/unit/domain/test_leases.py -q` exited 0 with `..... [100%]`. `uv run --python 3.12 mypy src/apexcrew/domain/authority.py` exited 0 with `Success: no issues found in 1 source file`.
- **Hard-authority evidence**: `test_global_model_call_ceiling_pauses_before_second_intent` proves a one-call approved ceiling creates no second intent/counter reservation and appends the exact pause, while a caller's `reserved_cost_usd=9.99` is ignored and approved pricing derives `0.0055`. Worker reservations consume exactly one current active-tranche call. Both adapters re-read sequence, approved Budget, counter snapshots, optional Task/tranche ownership, current Run revisions/target/dispatch state when a production Run exists, credential presence, pricing, and every global reservation ceiling inside the commit boundary. `V01_MECHANISM_LIMITS` alone supplies Task tranche/call and action/model timeout limits. No caller override exists.
- **Lease/runtime persistence**: additive migration 6 owns authority counters, Task counters/tranches, Workspace Leases, authorization denials, model-attempt bindings, Run dispatch/runtime columns, Audit runtime stamps, and the temporary approved-Budget hook required until Task 10. Lease IDs are deterministic digests of the Run/Task/Attempt/generation/expected-sequence binding; Task 9 adds no new `uuid4` site. Memory validates copied maps and publishes only after every check succeeds. SQLite uses `_commit_state_and_event` directly for state-plus-Audit writes. Open runtime intervals receive only adapter-clock Audit stamps and reject missing/regressed clocks.
- **Objective-progress interpretation**: Step 16's adapter signature accepted only a precomputed boolean while its prose required the canonical digest of complete before/after pass sets, failure sets, and lifecycle pair. The boolean cannot prove that claim. The retained internal interface therefore passes immutable `ProgressEvidence`; each adapter independently recomputes progress and stores/digests the full canonical evidence. This is the minimum interpretation that makes the promised authority real.
- **Task 4A debt #6**: not closed in this slice. Task 9's declared/Exact Stage path set excludes `src/apexcrew/domain/model.py`, where `DurableModelClient.complete` still invokes the legacy reservation-owning journal methods on initial dispatch and retry. Task 9B also excludes that file and owns global threshold/producer closure, not client composition. PLAN Task 12 explicitly introduces `AuthorityModelClient` and states that every known-closed retry repeats Task 9's full Authority reservation; Task 12 is therefore the first declared slice that can close the live retry path. Task 9 supplies and tests the authority but does not claim the legacy Task 4A path is protected.
- **Plan defects and interpretations**: Step 24 claims red/green action-authorization and hard-ceiling selectors that no preceding step defines; focused authority tests were added for the concrete global call ceiling and active-tranche gate, but no invented action fixture was attributed to the plan. The model deadline contract supplies both timestamps from the request while claiming a trusted deadline; the retained domain validation binds the exact two-minute window to `V01_MECHANISM_LIMITS` and rejects naive timestamps, but current-time expiry cannot be evaluated until the trusted dispatch clock is composed. Step 7's new durable `uuid4` lease ID conflicts with the prompt's explicit no-new-durable-`uuid4` rule and was replaced by a binding digest. Step 18's `tests.unit...` import conflicts with the repository's non-package tests layout. Step 21 omits both helper definitions it invokes. The prompt's commit paragraph says “Task 8's declared path set” and its PLAN edit boundary says “Task 4”; both conflict with the explicit Task 9 Files/Step 25 and were treated as copy errors.
- **Mechanical reconciliation**: the first focused authority mypy run found two snippet-era literal-widening errors. The first check-only Ruff formatter reported three Task 9 files needing only displayed layout changes. Ruff check then reported import order, paired-branch simplification, two `Decimal` constructor simplifications, and one unused plan-snippet import; only the displayed mechanical changes were applied. Ruff was never run in write/fix mode, and no assertion value, ceiling, deadline, or binding changed for lint.
- **Final mechanical green evidence**: `uv run --python 3.12 ruff format --check .` exited 0 with `46 files already formatted`; `uv run --python 3.12 ruff check .` exited 0 with `All checks passed!`; `uv run --python 3.12 pytest -q` exited 0 at `[100%]` with the two existing platform skips; `uv run --python 3.12 mypy src` exited 0 with `Success: no issues found in 25 source files`. The exact-scope `git diff --check` exited 0.
- **Changed paths**: `src/apexcrew/domain/authority.py`, `src/apexcrew/domain/effects.py`, `src/apexcrew/adapters/state/memory.py`, `src/apexcrew/adapters/state/sqlite.py`, `tests/unit/domain/test_authority.py`, `tests/unit/domain/test_leases.py`, `tests/contract/test_state_store.py`, and `AGENT_LOG.md`.
- **Commit subject**: `feat(authority): enforce leases and hard budgets`.
- **Review and external-state boundary**: spec-compliance and code-quality reviews are deferred to the M1-05 module PR review as required. No push, publication, credential access, provider call, or external repository action was performed.

## 2026-08-02 / TASK-009A - Stop repeated Task behavior

- **Base and task**: Task 9A (`TASK-009A`) started from `a24c10b009d5b9e773cc5be565246014173b3f22` on `codex/m1-05-authority`. OpenAI Codex (GPT-5), primary session `/root`, used the `tdd` and `andrej-karpathy-skills:karpathy-guidelines` skills. No subagent, assisting agent, or human edited the retained Task 9A files.
- **Actual first red**: `uv run --python 3.12 pytest tests/unit/domain/test_task_stopping.py::test_second_identical_checkpoint_pauses_task_and_closes_dispatch -q` exited 1 during collection with `ImportError: cannot import name 'CheckpointKey' from 'apexcrew.domain.authority'` and no collector. The plan's predicted missing-method `AttributeError` was unreachable because `CheckpointKey` and `TaskStopDecision` were imported before the test body. After those types and the Authority route existed, the selector next failed at the real storage gap with `AttributeError: 'SqliteStateStore' object has no attribute 'install_running_attempt_for_test'`, then at the absent `record_task_checkpoint` method.
- **Lifecycle-storage red/green**: `uv run --python 3.12 pytest tests/contract/test_state_store.py::test_task_and_attempt_lifecycle_storage_matches -q` first exited 1 for both adapters because `install_running_attempt_for_test` was absent, then exited 0 with both cases green. Task 9A creates storage the plan never allocated: additive migration 7 introduces authoritative `tasks` and `attempts`, plus required `task_checkpoints` and `task_invalid_actions` histories. The in-memory adapter owns matching records. The retained state vocabulary is deliberately minimal: Task `ACTIVE`, `READY`, `PAUSED` and Attempt `RUNNING`, `FAILED`; later slices must extend it additively.
- **Stop red/green evidence**: the checkpoint selector next exited 1 with missing `record_task_checkpoint`, then exited 0 at the fixed second identical observation. The invalid-action selector exited 1 with missing `record_invalid_action`, then exited 0 with three distinct, correctly Task-bound Attempt IDs becoming `FAILED` and Task successors `READY`, `READY`, `PAUSED`. SQLite restart, memory/SQLite parity, post-pause rejection, terminal-Attempt replay rejection, lease revocation, and injected state/Audit rollback selectors all pass.
- **Mechanism-limit authority**: both stop adapters read `V01_MECHANISM_LIMITS.repeated_checkpoint_ceiling` and `V01_MECHANISM_LIMITS.repeated_invalid_action_ceiling` directly. Their public signatures have no `ceiling` parameter or default. `test_task_stop_cap_is_not_caller_selectable` proves both SQLite and memory reject a caller-supplied cap for both methods before lifecycle, history, or Audit mutation.
- **Legal transitions**: `_set_task_state` compare-and-sets only the slice-required SPEC transitions `ACTIVE -> READY`, `ACTIVE -> PAUSED`, and the Task 9D-required `PAUSED -> READY`; `_finish_attempt` accepts only `RUNNING -> FAILED`. Invalid Task/Attempt source states raise `StateConflict` and transaction rollback preserves Task, Attempt, lease, history count, and Audit sequence. An `ACTIVE` Task now denies a second Attempt as `TASK_NOT_READY`, while a Run-wide dispatch closure retains reason precedence.
- **Runtime-boundary evidence**: the first exact active-runtime selector exited 1 with missing `AuthorityService.evaluate_active_run_time_boundary`, then with the missing SQLite method. Its green result proves ten observed owned seconds close dispatch, preserve the four-second cumulative value and open interval, and deny another Attempt as `ACTIVE_RUN_TIME_CEILING`. Memory and SQLite decisions match. A below-ceiling check returns `CONTINUE` without appending Audit. The adapter reads the monotonic clock inside the write boundary and uses that same reading for the stop event.
- **Protocol ownership**: stop and runtime-boundary methods were added to `AuthorityState`, not `EffectJournal`, because they compare-and-set authoritative lifecycle, budget, lease, dispatch, and counter state together with Audit. `AuthorityService` was already typed against `AuthorityState`; `EffectJournal` remains the narrower generic effect-intent journal. No `cast` or `Any` was added.
- **Counter semantics**: checkpoint and invalid-action histories are Task-lifetime records keyed independently by exact checkpoint/action identity. Alternating action kinds or identities does not reset either history, so a Worker cannot create an unbounded loop by alternating them.
- **Ordered reviews**: `/root` spec-compliance check returned `PASS` against SPEC sections 6.2 and 10.2 after confirming fixed adapter-owned caps, exact legal transitions, durable restart behavior, atomic invalid-action settlement, and trusted runtime timing. `/root` code-quality check first found the `ACTIVE`-Task scheduling edge and noncanonical SQLite dispatch-cause acceptance; focused regression/validation fixes were applied. The final code-quality check returned `PASS` with no critical or high-severity finding.
- **Mechanical reconciliation**: the first `uv run --python 3.12 mypy src` found two literal-widening errors in dispatch reasons. The first Ruff checks found import ordering, three formatting changes, and a callable dataclass default. Only mechanical import/layout/default ordering changes were applied; no cap, transition, digest, time, or assertion value changed. Ruff import organization and formatting were run in write mode for those displayed mechanical changes.
- **Final green evidence**: `uv run --python 3.12 pytest tests/unit/domain/test_task_stopping.py tests/unit/domain/test_authority.py tests/contract/test_state_store.py -q` exited 0 at `[100%]`; `uv run --python 3.12 ruff format --check .` exited 0 with `47 files already formatted`; `uv run --python 3.12 ruff check .` exited 0 with `All checks passed!`; `uv run --python 3.12 mypy src` exited 0 with `Success: no issues found in 25 source files`; `uv run --python 3.12 pytest -q` exited 0 at `[100%]` with the two existing platform skips, and collection confirmed `197 tests collected`; `git diff --check` exited 0.
- **Changed paths**: `src/apexcrew/domain/authority.py`, `src/apexcrew/adapters/state/memory.py`, `src/apexcrew/adapters/state/sqlite.py`, `tests/unit/domain/test_task_stopping.py`, `tests/contract/test_state_store.py`, and `AGENT_LOG.md`. `src/apexcrew/domain/effects.py` is intentionally unchanged. The pre-existing Task 9-only `PLAN.md` ledger change was preserved and is not part of Task 9A's implementation commit.
- **Commit subject**: `feat(authority): stop repeated task behavior`.
- **External state**: no push, publication, credential access, provider request, or external repository action was performed.

## 2026-08-02 / TASK-009B - Global budget thresholds

- **Base and task**: Task 9B (`TASK-009B`) started from `efd54d59cdb16d94856d88fb027f02ddc1cd1995` on `codex/m1-05-authority`. OpenAI Codex (GPT-5), primary session `/root`, used the `tdd` and `andrej-karpathy-skills:karpathy-guidelines` skills. No subagent or human edited the retained Task 9B files.
- **Threshold authority**: the total `GlobalBudgetMetric` mapping covers all eight SPEC metrics and reads each enforced ceiling from the transactionally revalidated approved Budget. The fixed warning threshold comes only from `V01_MECHANISM_LIMITS.warning_percent`. The journal has no warning-percent or ceiling input, `AtomicAction` carries neither value, and lease issuance passes only the Budget digest so each adapter re-reads `concurrent_worker_ceiling` inside the write transaction. The exact interface regression covers both adapters and fails if any of those three caller-selected cap surfaces returns.
- **Warning and stop behavior**: memory and SQLite persist at most one warning per Run/Budget revision/metric/threshold. At a ceiling, the admitted action or producer mutation commits before `BUDGET_STOP_REQUESTED`; new dispatch closes under the canonical `BUDGET_EXHAUSTED` cause while an admitted model intent or lease remains available to settle. Active-runtime threshold evaluation uses the trusted monotonic reading, persists exact Decimal seconds, leaves the interval open, and preserves cumulative nanoseconds for Task 11B.
- **Producer coverage**: Task 9B covers exactly six production producers available in M1-05: `PLANNING_REQUESTS`, `MODEL_CALLS`, `INPUT_TOKENS`, `OUTPUT_TOKENS`, and `COST_RESERVE_USD` through authorized model reservation/settlement, plus `CONCURRENT_WORKERS` through Workspace Lease issue/expiry. It does not claim eight-producer coverage. The missing concrete producers are `ACTIVE_RUN_SECONDS`, owned by Task 11B, and `TASKS`, owned by Task 12A.
- **Authorized Task 9A test correction**: `test_active_runtime_below_ceiling_does_not_append_audit` was renamed to `test_active_runtime_threshold_appends_exact_warning_audit`. Its only stale premise was Task 9B's legitimate 80-percent crossing behavior; the replacement assertions are stronger and exact: one sequence advance, `GLOBAL_BUDGET_USAGE_SETTLED`, the complete `BudgetWarning` payload, and dispatch still open. No other test in `tests/unit/domain/test_task_stopping.py` changed.
- **Unblock red evidence**: `uv run --python 3.12 pytest tests/unit/domain/test_task_stopping.py::test_active_runtime_below_ceiling_does_not_append_audit -q` exited 1 with `AssertionError: assert 2 == 1`. `uv run --python 3.12 ruff format --check .` exited 1 and named exactly `memory.py`, `sqlite.py`, and `test_global_budget_producers.py`; `uv run --python 3.12 ruff check .` exited 1 with 13 findings. These were the three owner-confirmed red gates. The exact renamed selector and the four parameter cases covering the new cap-interface plus existing journal-cap regression then exited 0.
- **Focused green evidence**: `uv run --python 3.12 pytest tests/unit/domain/test_budget_thresholds.py tests/integration/test_global_budget_producers.py tests/unit/domain/test_authority.py tests/unit/domain/test_model_retry.py tests/unit/domain/test_task_stopping.py -q` exited 0 with all 36 displayed cases green. `git diff --check` exited 0.
- **Ordered reviews**: `/root` performed the SPEC-compliance check first against SPEC section 10.2 and the approved Task 9B producer allocation; it returned `PASS` with no critical finding. `/root` then performed the code-quality check over transactionality, Budget binding, caller cap surfaces, warning uniqueness, metric monotonicity/gauge behavior, persistence, typing, and tests; it returned `PASS` with no critical or high-severity finding.
- **Mechanical reconciliation**: the initial Ruff formatter requested layout changes in three files. Ruff check reported three import-order findings, nine Decimal-constructor simplifications, and one exception-type preference. Only those displayed mechanical changes were applied; no metric, threshold, ceiling, action ordering, or assertion value changed for lint.
- **Final green evidence**: `uv run --python 3.12 ruff format --check .` exited 0 with `49 files already formatted`; `uv run --python 3.12 ruff check .` exited 0 with `All checks passed!`; `uv run --python 3.12 pytest -q` exited 0 at `[100%]` with the two existing platform skips; `uv run --python 3.12 mypy src` exited 0 with `Success: no issues found in 25 source files`.
- **Changed paths**: `src/apexcrew/domain/authority.py`, `src/apexcrew/domain/effects.py`, `src/apexcrew/adapters/state/memory.py`, `src/apexcrew/adapters/state/sqlite.py`, `tests/unit/domain/test_budget_thresholds.py`, `tests/integration/test_global_budget_producers.py`, `tests/unit/domain/test_task_stopping.py`, and `AGENT_LOG.md`. The pre-existing Task 9/9A `PLAN.md` ledger changes are preserved outside this implementation commit; Task 9B's row will be updated immediately after the commit under global Step 8.
- **Commit subject**: `feat(authority): enforce global budget thresholds`.
- **External state**: no push, publication, credential access, provider request, or external repository action was performed.

## 2026-08-02 / TASK-009C - Persist and classify action deadlines

- **Base and task**: Task 9C (`TASK-009C`) started from `75fb5765c7290faf96dfa1cf21cb90794084ca67` on `codex/m1-05-authority`. OpenAI Codex (GPT-5), primary session `/root`, used the `tdd`, `writing-plans`, and `andrej-karpathy-skills:karpathy-guidelines` skills. No subagent, assisting agent, or human edited the retained Task 9C files.
- **Actual first red**: `uv run --python 3.12 pytest tests/unit/domain/test_action_deadlines.py::test_ordinary_and_check_deadlines_use_fixed_v01_limits -q` exited 1 during collection with `ImportError: cannot import name 'ActionDeadline' from 'apexcrew.domain.authority'` and `ERROR: found no collectors`. The plan's predicted missing-`open_action_deadline` `AttributeError` was unreachable because the test imports `ActionDeadline` before its body runs.
- **In-memory red/green**: after the value types and Authority route existed, the same exact selector exited 1 with `AttributeError: 'InMemoryStateStore' object has no attribute 'record_action_deadline'`, then exited 0 with one displayed pass after the copied-map transaction implementation. `uv run --python 3.12 pytest tests/unit/domain/test_action_deadlines.py::test_unobservable_ordinary_timeout_is_indeterminate tests/unit/domain/test_action_deadlines.py::test_declared_check_timeout_has_no_passing_receipt -q` exited 0 with two displayed passes.
- **SQLite red/green**: `uv run --python 3.12 pytest tests/unit/domain/test_action_deadlines.py::test_deadline_and_timeout_decision_survive_sqlite_restart -q` first exited 1 with `AttributeError: 'SqliteStateStore' object has no attribute 'record_action_deadline'`. After additive migration 9 alone, the required rerun produced the identical `AttributeError`; the plan's predicted missing-bound-parameter insert was impossible because no method performed an insert. After the methods were added through `_commit_state_and_event`, the first run exposed `StateConflict: CURRENT_BUDGET_BINDING_MISMATCH`; using the transactionally revalidated approved-budget record, which is the same source read by Authority and by the test seeding API, made the selector green. Its final parameterized run exited 0 with two displayed passes covering ordinary and check decisions after reopen.
- **Closed timeout outcomes**: ordinary timeout settlement records `INDETERMINATE`, charges the full reservation classification, and removes the intent from recoverable unsettled work. Declared-check timeout settlement records only `INFRASTRUCTURE_UNCERTAINTY`, leaves the exact check intent and frozen snapshot binding recoverable, and carries neither semantic result nor Receipt. `TimeoutDecision` rejects runtime `PASS`, `FAILED`, or any other non-closed outcome shape. The focused value-object selector first failed with `Failed: DID NOT RAISE <class 'ValueError'>` for `FAILED`, then exited 0 after the closed-outcome validation.
- **Trusted deadline authority**: the plan exposed five caller-controlled authority inputs: `action_class`, `started_at`, current `now`, `check_id`, and `snapshot_digest`. Retained `open_action_deadline` accepts only Run, durable intent, and expected sequence. Authority obtains time from its UTC clock, derives class and check bindings from the canonical durable effect intent, and reads 120/600 seconds only from `V01_MECHANISM_LIMITS`. `deadline_state` and `settle_timeout` read the same trusted clock internally; settlement before expiry raises `ACTION_DEADLINE_NOT_EXPIRED` with zero state or Audit mutation. Both adapters revalidate the intent, current revisions, approved Budget, stored deadline, retry gate, and settle-once condition inside their state-plus-Audit callback.
- **Boundary green**: `uv run --python 3.12 pytest tests/unit/domain/test_action_deadlines.py tests/unit/domain/test_budget_thresholds.py -q` exited 0 at `[100%]` with 23 displayed passing cases. Exact 120/600-second boundaries, early-settlement denial, in-memory and SQLite classification, restart persistence, no passing Receipt, and the Task 9B dispatch gate all pass.
- **Plan defects and interpretations**: Step 10 names undefined `AuthorityError`; the existing closed rejection is `AuthorityDenied`. Step 11 calls undefined `_settle_timeout_successor`, which is not in PLAN's exhaustive permitted undefined-private-helper list; its semantics were implemented explicitly in each adapter. Steps 14-15 prescribe explicit SQLite transactions, but `_commit_state_and_event` owns the transaction and the standing constraint forbids nesting, so both writes use its mutation callback. Step 14's predicted insert failure cannot occur after tables alone. The plan treats uppercase `DECLARED_CHECK` as a caller argument, while the durable Task 15 `ToolIntent.kind` is lowercase `check`; deriving from the durable intent resolves both the naming mismatch and the caller-selectable 600-second ceiling. The plan does not check expiry in `settle_timeout`, so it would allow an early timeout settlement; retained code denies it. The plan's in-memory model had no representation for an `INDETERMINATE` intent, so a separate copied/published set was required to match SQLite's existing effect-intent state.
- **Debt disposition**: no listed debt item is closed by Task 9C. Debt #6 remains open because Task 12's `AuthorityModelClient` owns dispatch/retry wiring. `ModelCompletion.requested_model_id`, injectable durable IDs/`ScriptedMockLLM`, promotion-order dependency validation, secret-policy composition/HMAC/non-disclosure tests, bounded subprocess output, and recursive reservation tamper inspection are outside Task 9C's declared paths and must be closed by their named future owners or before M1 close as recorded in `AI4SE-ops/DEBT.md`.
- **Mechanical reconciliation and final gates**: the first format check named only four owned files; `uv run --python 3.12 ruff format` was applied only to those paths. Final `uv run --python 3.12 ruff format --check .` exited 0 with `50 files already formatted`; `uv run --python 3.12 ruff check .` exited 0 with `All checks passed!`; `uv run --python 3.12 pytest` exited 0 with `223 passed, 2 skipped in 14.87s`; `uv run --python 3.12 mypy src` exited 0 with `Success: no issues found in 25 source files`; `git diff --check` exited 0. Windows is observed green; Ubuntu remains pending CI.
- **Changed paths**: `src/apexcrew/domain/authority.py`, `src/apexcrew/domain/effects.py`, `src/apexcrew/adapters/state/memory.py`, `src/apexcrew/adapters/state/sqlite.py`, `tests/unit/domain/test_action_deadlines.py`, and `AGENT_LOG.md`. The pre-existing Task 9/9A/9B `PLAN.md` ledger edit is preserved outside this implementation commit; Task 9C's row will be updated immediately after commit.
- **Commit subject**: `feat(authority): classify bounded action timeouts`.
- **Review and external-state boundary**: spec-compliance and code-quality reviews are deferred to the M1-05 module PR review as required. No push, publication, credential access, provider call, or external repository action was performed.

## 2026-08-02 / TASK-009D - Resume only the exact recorded Task pause

- **Base and task**: Task 9D (`TASK-009D`) started from `0e6bc7800fad1059c47aceccff95121bc3703742` on `codex/m1-05-authority`. OpenAI Codex (GPT-5), primary session `/root`, used the `tdd` and `andrej-karpathy-skills:karpathy-guidelines` skills. No subagent, assisting agent, or human edited the retained Task 9D files.
- **Actual first red**: `uv run --python 3.12 pytest tests/unit/domain/test_task_resume.py::test_resume_requires_exact_pause_and_preserves_all_history -q` exited 4 during collection with `ImportError: cannot import name 'ResumeTaskRequest' from 'apexcrew.domain.authority'` and `ERROR: found no collectors`. The plan's predicted missing-`resume_task` `AttributeError` was unreachable because the test imports later-created value types before its body runs.
- **Further red evidence**: `uv run --python 3.12 pytest tests/unit/domain/test_task_resume.py::test_memory_and_sqlite_exact_resume_preserve_the_same_counters -q` exposed `AttributeError: 'InMemoryStateStore' object has no attribute 'install_task_pause_for_test'`. The exact-binding selector later exposed `AssertionError: assert True is False` after the dispatch-closed assertion was added, proving ordinary Task pauses had not closed Run dispatch. Both gaps were fixed before the next slice.
- **Exact resume authority**: a resume is stale unless its Run, Task, active pause sequence, reason, pause-time Plan/Policy/Model revisions, current approved revisions, and canonical counter digest all match. Only the Budget digest may change, and only an exact exhausted lower-ceiling pause can use a genuinely higher approved Budget with headroom for every recorded exhausted metric. The fixed administrative maximum is evaluated first and always requires cancel/new Run. Directly resumable, infrastructure-repair, non-resumable, and unknown causes remain a closed decision table.
- **Preservation and allocation**: resume increments only the lifetime `manual_resumes` count. Calls, tokens, cost, attempts, stale refreshes, failure digests, checkpoints, invalid-action history, warnings, and lease generation are preserved. The reserved Attempt and allocation IDs are deterministic digests of the exact resume binding; no durable `uuid4` was added. Both adapters revalidate pause, counters, global usage, revisions, Budget, allocation bounds, Task state, and dispatch cause inside the state-plus-Audit commit, then remove only the exact pause's dispatch-close cause.
- **Trusted repair boundary**: the plan's `TrustedRepairObservation` argument was a caller-supplied assertion of its own authority and was rejected. `resume_task` accepts no repair value; both adapters read an exact pause-bound observation from adapter-owned state. Task 9C did not actually create the claimed production repaired-infrastructure producer, so production safely denies infrastructure resume unless trusted state exists. The future check-settlement owner must wire that producer; the current plan names no concrete closing step.
- **Focused green evidence**: the exact-binding selector exited 0 with one displayed pass; the closed-cause/manual-resume/attempt/stale-refresh selector exited 0 with seven displayed passes; the infrastructure-repair selector exited 0 with one displayed pass; the lower-ceiling/fixed-maximum/persistence selector exited 0 with five displayed passes; and the memory/SQLite parity selector exited 0 with one displayed pass. `$env:MYPYPATH='src'; uv run --python 3.12 mypy tests/unit/domain/test_task_resume.py` exited 0 with `Success: no issues found in 1 source file`.
- **Boundary and final green evidence**: `uv run --python 3.12 pytest tests/unit/domain/test_task_resume.py tests/unit/domain/test_task_stopping.py tests/unit/domain/test_budget_thresholds.py tests/unit/domain/test_action_deadlines.py tests/unit/domain/test_authority.py tests/unit/domain/test_leases.py -q` exited 0 at `[100%]`. Final `uv run --python 3.12 ruff format --check .` exited 0 with `51 files already formatted`; `uv run --python 3.12 ruff check .` exited 0 with `All checks passed!`; `uv run --python 3.12 pytest` exited 0 with `238 passed, 2 skipped in 12.20s`; `uv run --python 3.12 mypy src` exited 0 with `Success: no issues found in 25 source files`. Windows is observed green; Ubuntu remains pending CI.
- **Plan defects and interpretations**: the first predicted red was impossible as described. Step 4 let the caller manufacture a trusted repair. The claimed Task 9C repair producer does not exist. The prose alternates between nonexistent `task_counters` storage and existing `task_budget_counters`. Failure and warning history stores did not exist, so additive migration 10 adds exact resume metadata persistence. A plain insert into a `(run_id, task_id)` pause primary key prevented a second lifetime pause, so inactive rows are updated while active rows fail closed. The planned loader discarded the stored pause-time revisions. The snippets closed dispatch only for Budget pauses and left ordinary paused Tasks dispatchable. `_ids.new_attempt_id()` and `_ids.new_allocation_id()` do not exist and would have introduced another durable random-ID authority. The explicit SQLite transaction conflicts with transaction-owning `_commit_state_and_event`, so the write uses its mutation callback. Task 9D has no `Exact stage` row; Step 19's `git add` path set is authoritative.
- **Debt disposition**: no listed debt item is closed. Debt #6 now has the Task 9/9A-9D authority but still requires Task 12's `AuthorityModelClient` to wire Task 4A dispatch/retry through it. Requested-model-ID validation/persistence belongs to the future model-request validation owner. Injectable durable IDs and request-sensitive `ScriptedMockLLM` remain due before the M1-02 PR decision. Promotion-order dependency validation, secret-policy runtime composition/HMAC/non-disclosure coverage, bounded subprocess output, and recursive reservation tamper inspection remain with their recorded future owners or must be decided before M1 close.
- **Changed paths**: `src/apexcrew/domain/authority.py`, `src/apexcrew/domain/effects.py`, `src/apexcrew/adapters/state/memory.py`, `src/apexcrew/adapters/state/sqlite.py`, `tests/unit/domain/test_task_resume.py`, and `AGENT_LOG.md`. The pre-existing Task 9-9C `PLAN.md` ledger edit remains outside this implementation commit; Task 9D's row will be updated immediately after commit.
- **Commit subject**: `feat(authority): resume exact task pauses`.
- **Review and external-state boundary**: spec-compliance and code-quality reviews are deferred to the M1-05 module PR review as required. No push, publication, credential access, provider call, or external repository action was performed.

## 2026-08-02 / TASK-010 - A-Hybrid control and query interfaces

- **Base and task**: Task 10 (`TASK-010`) started from `011a5d0bf0dc8a97777f5d0631f4fb6e9957b969` on `codex/m1-06-application-runtime`. OpenAI Codex (GPT-5), primary session `/root`, used the `tdd` and `andrej-karpathy-skills:karpathy-guidelines` skills. The signed-off `PLAN.md` supplied the implementation plan; no duplicate plan, subagent, worktree, or human edit was used.
- **Actual first red**: `uv run --python 3.12 pytest tests/acceptance/test_application_interfaces.py tests/acceptance/test_revision_lifecycle.py::test_bootstrap_revisions_and_permit_survive_sqlite_restart -q` exited 1 during collection with `ModuleNotFoundError: No module named 'helpers.application'` and `ERROR: found no collectors for ...test_bootstrap_revisions_and_permit_survive_sqlite_restart`. The plan's predicted missing `apexcrew.application.control` failure was unreachable because Step 1 imports the helper that Step 7 creates later.
- **Focused green evidence**: `uv run --python 3.12 pytest tests/acceptance/test_revision_lifecycle.py::test_bootstrap_revisions_and_permit_survive_sqlite_restart -q` exited 0 with `. [100%]`; `uv run --python 3.12 pytest tests/acceptance/test_application_interfaces.py tests/acceptance/test_revision_lifecycle.py -q` exited 0 at `[100%]`; the Task boundary selector `uv run --python 3.12 pytest tests/acceptance/test_revision_lifecycle.py::test_bootstrap_revisions_and_permit_survive_sqlite_restart tests/contract/test_state_store.py -q` exited 0 at `[100%]`; `uv run --python 3.12 pytest tests/acceptance/test_application_interfaces.py -q` exited 0 at `[100%]`; and `$env:MYPYPATH='src'; uv run --python 3.12 mypy tests/helpers/application.py` exited 0 with `Success: no issues found in 1 source file`.
- **Control and query boundary**: `apexcrew.application` exports only `CrewControl` and `RunQueries`; root exports remain limited to `__version__` until Task 11 adds `CrewRuntime`. Unknown Runs return closed control/query models. Queries use one read transaction and do not reconcile, issue or expose a Permit, or change Audit sequence. Task 26A still owns exact Tier 1 prefix folding and sanitization.
- **Bootstrap and revision authority**: create-run compares the caller's repository root/direct target/expected OID with an injected mechanism-owned preflight binding and persists only its repository identity and instance digest. Bootstrap inputs, three current revision documents, reservation, receipt, Run, and Audit event commit atomically. Approval uses the payload command kind, constant-time code comparison, exact current proposal state, exact applicable bindings, fixed Budget validation, and current Budget pricing. Policy/Plan stay frozen across all execution phases; active Budget/Model replacement either applies immediately or closes dispatch and records one barrier-pending replacement.
- **Runtime Permit authority**: issuance is restricted to accepted delivery-authorizing Task 10 commands, derives the source envelope digest, checks the mechanism-derived phase, current full revision set, canonical target authority, owner absence, source uniqueness, and one-unconsumed rule. Consumption checks and consumes in one transaction, increments progress only on success, installs one owner, records `RUNTIME_PERMIT_CONSUMED`, and records `RUNTIME_PERMIT_INVALIDATED` on phase/binding mismatch. Replays, a second source command, an accepted revision command repurposed as authority, wrong phase, rollback faults, and a second consume cannot restart work.
- **Plan defects and interpretations**: the restart snippet reopens `tmp_path/state.db` although the fixture creates `tmp_path/application/state.db`. Its helper wraps transaction-owning `_commit_state_and_event` in `_transaction`, which necessarily raises `NESTED_STATE_TRANSACTION`; retained helpers use mutation callbacks. `model_call_ceiling=241` is rejected by Pydantic before a handler can return the planned `INVALID` outcome. Approval examples hash request IDs such as `approve-policy`, while the prose requires payload command kinds such as `approve_policy`. The stale and corrected resume examples reuse one request ID, which correctly yields `IDEMPOTENCY_KEY_REUSE`; retained commands use distinct IDs. The pause seed omits Task 9D's required pause-time Budget and revision bindings. Root-export timing conflicts between the Files summary and explicit Task 10/11 prose; the explicit prose wins. The plan names `apply_or_defer_revision_replacement` but gives no signature, and a standalone transaction would break atomic approval; the operation is retained inside approval's mutation callback. Its public `issue_runtime_permit` signature exposes a caller-selected phase and accepts any accepted command unless narrowed; retained adapters derive/validate phase and source class. The supplied `ControlCommandService` has no repository-preflight dependency despite requiring create-run preflight; a required internal authority port was added, and Task 27C must compose the real Git preflight implementation. Task 10's minimal `at_sequence` model can select a retained sequence but pairs it with current lifecycle state; Task 26A is explicitly assigned exact historical event-prefix projection. Task 11A's slicing row also claims Permit consumption that this task's prompt explicitly requires now; Task 11A should extend the existing contract instead of reimplementing it. Task 10 has no `Exact stage` row, so Step 8's `git add` list is authoritative.
- **Debt disposition**: no listed debt item is closed. Debt #6 still belongs to Task 12's `AuthorityModelClient`. Requested-model-ID validation/persistence, injectable durable IDs and request-sensitive `ScriptedMockLLM`, dependency promotion ordering, secret-policy composition/HMAC/non-disclosure coverage, bounded subprocess output, and recursive reservation tamper inspection remain with their recorded owners. Task 27C must supply the production repository bootstrap-authority composition; Task 26A must close exact historical projection.
- **Final mechanical green evidence**: `uv run --python 3.12 ruff format --check .` exited 0 with `58 files already formatted`; `uv run --python 3.12 ruff check .` exited 0 with `All checks passed!`; `uv run --python 3.12 pytest` exited 0 with `259 passed, 2 skipped in 13.22s`; `uv run --python 3.12 mypy src` exited 0 with `Success: no issues found in 29 source files`; `git diff --check` exited 0. Windows is observed green; Ubuntu remains pending CI.
- **Changed paths**: `src/apexcrew/__init__.py`, `src/apexcrew/application/__init__.py`, `src/apexcrew/application/control.py`, `src/apexcrew/application/queries.py`, `src/apexcrew/domain/commands.py`, `src/apexcrew/domain/projection.py`, `src/apexcrew/adapters/state/memory.py`, `src/apexcrew/adapters/state/sqlite.py`, `tests/acceptance/test_application_interfaces.py`, `tests/acceptance/test_revision_lifecycle.py`, `tests/helpers/application.py`, `tests/contract/test_state_store.py`, and `AGENT_LOG.md`.
- **Commit subject**: `feat(application): add control and query seams`.
- **Review and external-state boundary**: spec-compliance and code-quality reviews are deferred to the M1-06 module PR review as required. No push, publication, credential access, provider call, repository mutation, or external effect was performed.

## 2026-08-02 / TASK-011A - Persist Runtime Permit consumption and reads

- **Base and task**: Task 11A (`TASK-011A`) started from `f69148b8a577a445d3e7bca76c44b7adabc846c3` on `codex/m1-06-application-runtime`. OpenAI Codex (GPT-5), primary session `/root`, used the `tdd`, `writing-plans`, and `andrej-karpathy-skills:karpathy-guidelines` skills. No subagent, assisting agent, or human edited the retained Task 11A files.
- **Observed red**: `uv run --python 3.12 pytest tests/contract/test_runtime_permit_store.py::test_unconsumed_and_consumed_permits_survive_restart -q` exited 1 with `AssertionError: assert None == 1`; after the consumed Permit and owner survived the second SQLite reopen, `ActiveRunTimeState.open_owner_generation` was still `None`. This was the real missing persistence boundary, not an import or missing-method failure.
- **Focused green evidence**: the exact red selector rerun exited 0 with `. [100%]`. `uv run --python 3.12 pytest tests/contract/test_runtime_permit_store.py::test_unconsumed_and_consumed_permits_survive_restart tests/contract/test_state_store.py -q` exited 0 at `[100%]`. `uv run --python 3.12 pytest tests/contract/test_runtime_permit_store.py tests/contract/test_state_store.py::test_runtime_permit_consumption_is_atomic_and_one_use tests/contract/test_state_store.py::test_runtime_permit_is_bound_to_its_issued_phase -q` exited 0 with `..... [100%]`. `$env:MYPYPATH='src;tests'; uv run --python 3.12 mypy --strict --explicit-package-bases tests/helpers/application.py tests/contract/test_runtime_permit_store.py` exited 0 with `Success: no issues found in 2 source files`.
- **Atomic Permit persistence**: both adapters retain Task 10's accepted-command, mechanism-derived phase, current revisions, target authority, progress generation, compare-and-set, invalidation, and one-use replay checks. A successful consume now reads the injected monotonic clock once after those checks and uses that one value for owner/progress installation, the opened active interval, and the `RUNTIME_PERMIT_CONSUMED` Audit stamp. SQLite persists every member in the existing state-and-event transaction; memory mutates copied maps and publishes only after Audit succeeds. The existing injected fault proves Permit, owner interval, and Audit all roll back together in both adapters, while the non-rollback clock observation is counted explicitly.
- **Plan defects and interpretations**: source Step 1 references an undefined `seeded_permit_store`; the retained restart test issues a real Permit through control instead. Source Step 3 opens its own `_transaction`, which conflicts with the standing `NESTED_STATE_TRANSACTION` denial and the current transaction-owning commit helper; the retained implementation uses that helper's mutation callback and a cached trusted-time factory. The Step 3 snippet also assumes helper names and signatures absent from the current adapters. Task 10 already supplied the Permit table and consume/read APIs, so Task 11A needed no migration and no domain type/effect change despite those paths appearing in Exact Stage. “Permit restart/rollback tests” is ambiguous because source Step 1's other rollback test closes a `RunStop` interval, behavior explicitly assigned to 11B; 11A retained that exclusion and extended Task 10's existing consume rollback contract instead. “Matching memory/SQLite restart” is literally impossible for the in-memory adapter; parity is proven for consume/rollback, while restart is SQLite-only. The slice selector's doubled quiet configuration suppresses the normal pass-count summary.
- **Task 10 compatibility**: Task 10's successful one-use and phase-binding assertions remain unchanged. Task 11A necessarily adds a configured monotonic clock as a precondition for successful consumption because consuming without opening and stamping the interval would violate this slice; tests now inject that mechanism dependency. Invalid phase/binding consumption still invalidates without consulting the clock, and replay still returns `None` with zero mutation and zero additional clock reads.
- **Debt disposition**: no listed open debt item was closed or deepened. Requested-model validation/persistence remains with the slice that owns model-request validation; injectable durable IDs and request-sensitive `ScriptedMockLLM` remain a decision required before the M1-02 PR; Task 4A retry wiring remains Task 12's `AuthorityModelClient`; promotion-order checks remain with the coordination/plan-graph owner; secret-policy composition belongs to a later runtime slice, keyed-HMAC coverage to M1 close, and non-disclosure coverage to each Audit/WebUI/logging owner; bounded subprocess output and recursive reservation tamper inspection remain required decisions before M1 close.
- **Final mechanical green evidence**: `uv run --python 3.12 ruff format --check .` exited 0 with `59 files already formatted`; `uv run --python 3.12 ruff check .` exited 0 with `All checks passed!`; `uv run --python 3.12 pytest` exited 0 with `260 passed, 2 skipped in 13.18s`; `uv run --python 3.12 mypy src` exited 0 with `Success: no issues found in 29 source files`; `git diff --check` exited 0. Windows is observed green; Ubuntu remains pending CI.
- **Changed paths**: `src/apexcrew/adapters/state/memory.py`, `src/apexcrew/adapters/state/sqlite.py`, `tests/contract/test_runtime_permit_store.py`, `tests/contract/test_state_store.py`, and `AGENT_LOG.md`. The pre-existing Task 10 `PLAN.md` ledger change was preserved and is not part of this commit. Exact Stage's unchanged `src/apexcrew/domain/types.py` and `src/apexcrew/domain/effects.py` add no staged content.
- **Commit subject**: `feat(runtime): persist runtime permits`.
- **Review and external-state boundary**: spec-compliance and code-quality reviews are deferred to the M1-06 module PR review as required. No push, publication, credential access, provider call, repository mutation, or external effect was performed.

## 2026-08-02 / TASK-011B - Runtime ownership and composition

- **Base and task**: Task 11B (`TASK-011B`) started from `fe6b1bc9589bcf3b8a170b2db6925f1c350ea19d` on `codex/m1-06-application-runtime`. OpenAI Codex (GPT-5), primary session `/root`, used `andrej-karpathy-skills:karpathy-guidelines`; no subagent or assisting agent edited the retained files.
- **Human correction**: the owner corrected a reversed pytest interpretation and authorized two exact compatibility changes. `apexcrew.application.__all__` now remains an exact equality over `CrewControl`, `CrewRuntime`, and `RunQueries`; the root equality includes those three plus `__version__`. A direct `AuthorityService.reserve_model_attempt` test now expects the truthful explicit barrier state `IDLE` while retaining its exact five-metric atomicity assertion.
- **Focused evidence**: the two corrected selectors first exited 1 with the observed `['CrewControl', 'CrewRuntime', 'RunQueries']` versus the stale two-interface list and `assert 'IDLE' == 'IN_FLIGHT'`; after the exact assertion changes they exited 0 with `.. [100%]`. The complete Task 11B selector exited 0 with 17 displayed passes.
- **Owned-time and recovery boundary**: Task 11B retains proposal 0001 Gap 3 accounting: durable monotonic owned intervals, action-boundary evaluation, atomic Permit/open and RunStop/close, and crash sealing at the old generation's latest committed Audit stamp without reading the current clock. The deterministic binding digest, detached main-worktree handling, and portable path comparison remain covered by the unchanged implementation and regression suite.
- **Barrier dispatch debt**: Task 11B does not guarantee that a barrier is open for every production model reservation. `RuntimeService._drive_phase` calls `Coordinator.run_planning_turn` and `Coordinator.schedule` without opening a barrier; current in-flight tests open one inside fixture coordinators, while direct Authority reservation correctly leaves it `IDLE`. Task 12's `AuthorityModelClient`, already owner of debt #6 and every authority-mediated provider attempt, must ensure model reservation/dispatch occurs within the runtime barrier so SPEC line 299's settled-in-flight conjunct cannot become true during a live call.
- **Global producer coverage**: `ACTIVE_RUN_SECONDS` now has a real Task 11B owned-interval producer, bringing production coverage to seven of eight global metrics. Only `TASKS` remains, owned by Task 12A's immutable Plan transaction; no all-eight claim is made here.
- **Final mechanical green evidence**: `uv run --python 3.12 ruff format --check .` exited 0 with `62 files already formatted`; `uv run --python 3.12 ruff check .` exited 0 with `All checks passed!`; `uv run --python 3.12 pytest` exited 0 with `279 passed, 2 skipped in 18.64s`; `uv run --python 3.12 mypy src` exited 0 with `Success: no issues found in 31 source files`. Windows is observed green; Ubuntu remains pending CI.
- **Tests touched by the unblock**: `tests/acceptance/test_application_interfaces.py` and `tests/integration/test_global_budget_producers.py`; no assertion was changed to a subset check and the five-metric set equality remains exact.
- **Changed paths**: `src/apexcrew/application/runtime.py`, `src/apexcrew/application/__init__.py`, `src/apexcrew/__init__.py`, `src/apexcrew/adapters/system.py`, `src/apexcrew/domain/admission.py`, `src/apexcrew/domain/model.py`, `src/apexcrew/adapters/repository/git.py`, `src/apexcrew/adapters/state/memory.py`, `src/apexcrew/adapters/state/sqlite.py`, `tests/helpers/application.py`, `tests/integration/test_runtime_permits.py`, `tests/integration/test_target_reservation.py`, `tests/integration/test_global_budget_producers.py`, `tests/acceptance/test_application_interfaces.py`, `tests/contract/test_state_store.py`, and `AGENT_LOG.md`. The pre-existing Task 10/11A `PLAN.md` ledger edit is preserved outside this implementation commit; Task 11B's row is updated immediately after the commit.
- **Commit subject**: `feat(runtime): consume one-use permits under lock`.
- **Review and external-state boundary**: local spec-compliance and code-quality checks found no new critical issue beyond the Task 12 barrier-dispatch debt recorded above; module-level independent reviews remain at the M1-06 PR boundary. No push, publication, credential access, provider call, external repository action, or human code edit was performed.

## 2026-08-02 / TASK-012A - Persist the immutable Plan graph and Task counts

- **Base and task**: Task 12A (`TASK-012A`) started from `62997b25b63132622138ddcf9b48534b5ebb2a10` on `codex/m1-07-planning-start`. OpenAI Codex (GPT-5), primary session `/root`, used the `tdd` and `andrej-karpathy-skills:karpathy-guidelines` skills. No subagent, assisting agent, worktree change, or human edit was used.
- **Observed red**: `uv run --python 3.12 pytest tests/contract/test_plan_graph.py::test_plan_graph_and_planning_count_survive_restart tests/contract/test_plan_graph.py::test_plan_task_count_producer_warns_and_stops_at_budget_thresholds -q` exited 1 during collection with `ModuleNotFoundError: No module named 'apexcrew.domain.coordination'`; pytest also reported `found no collectors` for both requested node IDs. This matched the slice's actual missing boundary.
- **Focused green evidence**: rerunning the exact red selector exited 0 with `... [100%]`. The authoritative boundary command `uv run --python 3.12 pytest tests/contract/test_plan_graph.py::test_plan_graph_and_planning_count_survive_restart tests/contract/test_plan_graph.py::test_plan_task_count_producer_warns_and_stops_at_budget_thresholds tests/contract/test_state_store.py -q` exited 0 at `[100%]`. `$env:MYPYPATH='src'; uv run --python 3.12 mypy --strict --explicit-package-bases tests/contract/test_plan_graph.py` exited 0 with `Success: no issues found in 1 source file`.
- **Immutable graph persistence**: additive SQLite migration 13 creates only `plans`, `task_contracts`, `task_dependencies`, `hazard_edges`, and `run_checks`; the in-memory adapter mirrors them in copied maps and publishes only after every validation and Audit append succeeds. Both adapters reject a non-planning Run, stale base/revision binding, noncanonical or graph-mismatched Plan document, incomplete/duplicate checks, a non-authoritative planning count, closed dispatch, duplicate graph, or current-Budget Task overflow before any graph or usage becomes visible. SQLite reconstructs the exact proposal from Task/check rows and verifies row digests plus dependency/hazard edges after reopen.
- **Task-count producer**: Plan persistence inserts the complete Task graph first, derives `TASKS` from the persisted Task-row/map count, and settles it with literal `GlobalBudgetMetric.TASKS`, the current approved Budget digest/ceiling, and Task 9B's fixed `V01_MECHANISM_LIMITS.warning_percent`. Ten of twelve records one warning and enters `AWAITING_PLAN_APPROVAL`; twelve of twelve persists and journals the Plan first, closes dispatch, enters `PAUSED`, and appends `BUDGET_STOP_REQUESTED` immediately after `PLAN_PROPOSED`. A proposal above a lowered current ceiling retains zero graph, zero Task usage, and the prior Audit sequence. `TASKS` completes production coverage at eight of eight global budget metrics.
- **Dependency-order gap**: the persisted-Plan correctness hole is closed at `PlanProposal.from_validated_plan`: every dependency edge must point forward in the proposed promotion order, independently of hazards, and the contract test uses a hazard-free reversed dependency to prove rejection. `domain/plan.py` is outside Task 12A's exact path set, so its standalone Task 5 `validate_plan` API still lacks the same check; the owning domain-plan review should fold the rule down before declaring that older API debt fully closed.
- **Plan defects and interpretations**: source Step 3 includes `planning_returned_bytes` and `settle_planning_read`, but the binding slice amendment assigns both to 12B, so migration 13 excludes that column and those methods. Source Step 4 omits the mandatory Python 3.12 selector; the slice command was used. The source tests reference undefined `seeded_planning_store` and `valid_plan_proposal`; local contract fixtures seed a real Run, approved Budget, revision bindings, and durable authority planning count. `task_revision` has no corresponding Task 5 domain field, so the v1 row is fixed to literal `1`; `hazard_class` has no specified value, so the sole closed class is `PROMOTION`. The proposal's planning count is caller-shaped in the snippet; both adapters additionally require it to equal the durable Task 9 authority counter. The first implementation rerun exposed two test-fixture mistakes, `REGISTERED_LOCKED` at draft creation and a reservation pathname not equal to its reservation ID; both were corrected without weakening the existing reservation guard. The stronger Plan-document binding then exposed the test's use of `str(GlobPattern)` rather than its canonical `.value`; the fixture was corrected and the rejection stayed in production.
- **Debt disposition**: no unrelated debt was changed. Debt #6 cannot be closed in 12A because `AuthorityModelClient`, planning dispatch/retry, and runtime-barrier ownership are explicitly 12B work. Requested-model persistence/validation, injectable durable IDs and request-sensitive `ScriptedMockLLM`, secret-policy composition/HMAC/non-disclosure coverage, bounded subprocess output, and recursive reservation tamper inspection remain with their recorded owners. The dependency-order mechanism is closed for all persistable Plans, with the standalone `validate_plan` limitation stated above.
- **Final mechanical green evidence**: `uv run --python 3.12 ruff format --check .` exited 0 with `64 files already formatted`; `uv run --python 3.12 ruff check .` exited 0 with `All checks passed!`; `uv run --python 3.12 pytest` exited 0 with `282 passed, 2 skipped in 17.91s`; `uv run --python 3.12 mypy src` exited 0 with `Success: no issues found in 32 source files`. Windows is observed green; Ubuntu remains pending CI.
- **Changed paths**: `src/apexcrew/domain/coordination.py`, `src/apexcrew/adapters/state/memory.py`, `src/apexcrew/adapters/state/sqlite.py`, `tests/contract/test_plan_graph.py`, and `AGENT_LOG.md`. Exact Stage's unchanged `tests/contract/test_state_store.py` adds no staged content.
- **Commit subject**: `feat(planning): persist immutable plan graph`.
- **Review and external-state boundary**: spec-compliance and code-quality reviews are deferred to the M1-07 module PR review as required. No push, publication, credential access, provider call, external repository action, or human code edit was performed.

## 2026-08-02 / TASK-012B - Run the bounded read-only planning loop

- **Base and task**: Task 12B (`TASK-012B`) started from `08bda573114debf1de5728aec67afe76014c76e1` on `codex/m1-07-planning-start`. OpenAI Codex (GPT-5), primary session `/root`, used `writing-plans` and `andrej-karpathy-skills:karpathy-guidelines`. No subagent, assisting agent, worktree change, or human edit was used.
- **Observed red**: `uv run --python 3.12 pytest tests/unit/domain/test_planning_loop.py::test_planning_never_dispatches_before_bootstrap_approval tests/unit/adapters/repository/test_planning_snapshot_reader.py::test_snapshot_reader_rejects_nonregular_tree_entry_before_blob_read -q` exited 4 during collection with `ImportError: cannot import name 'BoundedPlanningReadGateway' from 'apexcrew.domain.coordination'`, `ModuleNotFoundError: No module named 'apexcrew.adapters.repository.planning'`, and `ERROR: found no collectors` for both node IDs. The plan's prediction that `apexcrew.domain.coordination` was absent was stale because Task 12A had already created that module.
- **Bounded planning behavior**: the Coordinator accepts exactly one closed planning action per RuntimeService boundary. Reads are pinned to the authorized Run/repository/base/scope snapshot, use structured Git operations, reject nonregular and noncanonical entries before blob content, enforce manifest/file/search/total-return caps, settle canonical returned bytes durably, and retain no newly exposed content on overflow. Additive SQLite migration 14 and copied-map memory state remain behaviorally matched. Recovered reads, failures, invalid actions, and Plan submissions settle the exact journaled marker under the exact consumed planning Permit in the same state-plus-Audit transaction and do not redispatch the model.
- **Authority and barrier closure**: `AuthorityModelClient` opens a deterministic runtime barrier before every reservation, refreshes sequence and counters, reserves every first attempt and known-closed retry through `Authority.reserve_model_attempt`, uses only `V01_MECHANISM_LIMITS.ordinary_action_timeout_seconds`, and settles the barrier after the attempt. A denial makes zero provider calls. Retry coverage observes `IN_FLIGHT` at both provider calls and proves two provider attempts consume one logical planning request. Debt #6(a), retry budget revalidation, and #6(b), an open barrier before every reservation, are closed by production code plus tests.
- **Caller-trust corrections**: the plan's caller-derived two-minute retry window was rejected; the fixed mechanism deadline is read from `V01_MECHANISM_LIMITS`. Repository identity, pinned base, scope, snapshot, returned bytes, planning count, recovered marker, Permit, and revision bindings are revalidated by the mechanism. Retry attempts do not increment `PLANNING_REQUESTS` or trip the logical-turn ceiling reached by their own first attempt. Recovery required a separate `current_for_recovery` authorization view because the plan's sole `current(run_id)` view observes the already-incremented count and cannot process an eighth committed turn.
- **Subprocess debt**: Task 7B's production `capture_output=True` path was replaced with concurrent bounded stdout/stderr drains, a fixed 30-second deadline, fail-closed overflow, and an already-exited-process kill guard. The 2,000,001-byte test is cross-platform. This closes the implementation and test substance of debt #10 for Git capture, but `AI4SE-ops/DEBT.md` is outside the exact stage and remains unchanged.
- **Focused green evidence**: `uv run --python 3.12 pytest tests/unit/domain/test_planning_loop.py tests/unit/adapters/repository/test_planning_snapshot_reader.py tests/integration/test_planning_authorization.py -q` exited 0 with 17 displayed passes. `uv run --python 3.12 pytest tests/unit/domain/test_planning_loop.py tests/unit/adapters/repository/test_planning_snapshot_reader.py tests/integration/test_planning_authorization.py tests/contract/test_state_store.py -q` exited 0 at `[100%]` with 70 displayed passes before the final three safety regressions were added; all three are included in the final full-suite gate.
- **Plan defects and interpretations**: the red prediction was stale; the source snippets omit the runtime barrier required by SPEC line 299; the retry snippet reuses a caller-selected attempt window; `credential_profile=None` is described as valid for `ScriptedMockLLM` even though the existing Authority correctly returns `CREDENTIAL_UNAVAILABLE`; the read-gateway snippet catches an adapter exception from the domain layer; the subprocess snippet uses unbounded `capture_output=True`; and the recovery prose requires marker settlement for Plan submission while the initial split implementation path had no recovery-aware graph transaction. The plan also failed to distinguish logical planning requests from provider retries, making a retry consume another planning turn and potentially deny itself. The retained code resolves each issue without weakening an existing denial.
- **Debt disposition**: debt #6 is closed in both requested halves. Git bounded-capture debt #10 is substantively closed but its external ledger remains for the owning module/M1 close. Requested-model-ID persistence/validation, injectable durable IDs and request-sensitive `ScriptedMockLLM`, standalone dependency-order validation, secret-policy default composition/HMAC/non-disclosure coverage, and recursive reservation tamper inspection remain outside Task 12B and were not deepened.
- **Final mechanical green evidence**: `uv run --python 3.12 ruff format --check .` exited 0 with `68 files already formatted`; `uv run --python 3.12 ruff check .` exited 0 with `All checks passed!`; `uv run --python 3.12 pytest` exited 0 with `299 passed, 2 skipped in 18.62s`; `uv run --python 3.12 mypy src` exited 0 with `Success: no issues found in 33 source files`; `git diff --check` exited 0. Windows is observed green; Ubuntu remains pending CI.
- **Changed paths**: `src/apexcrew/domain/coordination.py`, `src/apexcrew/domain/model.py`, `src/apexcrew/domain/policy.py`, `src/apexcrew/adapters/repository/planning.py`, `src/apexcrew/adapters/repository/git.py`, `src/apexcrew/adapters/state/memory.py`, `src/apexcrew/adapters/state/sqlite.py`, `tests/unit/domain/test_planning_loop.py`, `tests/unit/adapters/repository/test_planning_snapshot_reader.py`, `tests/integration/test_planning_authorization.py`, and `AGENT_LOG.md`. Exact Stage's unchanged `tests/contract/test_state_store.py` adds no staged content. The pre-existing Task 12A `PLAN.md` ledger edit is preserved outside this implementation commit.
- **Commit subject**: `feat(planning): run bounded planning loop`.
- **Review and external-state boundary**: spec-compliance and code-quality reviews are deferred to the M1-07 module PR review as required. No push, publication, credential access, provider request, or external repository action was performed.

## 2026-08-02 / TASK-013 - Gate Plan approval and private Run-ref initialization

- **Base and task**: Task 13 (`TASK-013`) started from `8eec9f9b1293ee4d9220966dba4271da9bbb97aa` on `codex/m1-07-planning-start`. OpenAI Codex (GPT-5), primary session `/root`, used the `tdd` and `andrej-karpathy-skills:karpathy-guidelines` skills. No subagent, assisting agent, worktree change, or human edit was used.
- **Observed red**: `uv run --python 3.12 pytest tests/integration/test_start_guard.py -q` exited 1 during collection with `ImportError: cannot import name 'PrivateRefInitializer' from 'apexcrew.application.runtime'`. This was the actual first missing public boundary. A later fail-closed regression, `uv run --python 3.12 pytest tests/integration/test_start_guard.py::test_unobservable_private_ref_result_preserves_recoverable_intent -q`, exited 1 with `AssertionError: assert 'ABSENT_EXPECTED' == 'INIT_INTENT_RECORDED'`; the adapters had incorrectly treated an unobservable post-CAS state as proof of absence.
- **Focused green evidence**: `uv run --python 3.12 pytest tests/integration/test_start_guard.py -q` exited 0 with nine displayed passes at `[100%]`. `uv run --python 3.12 pytest tests/integration/test_start_guard.py::test_plan_approval_and_private_ref_state_survive_restart tests/contract/test_state_store.py -q` exited 0 with 58 displayed passes at `[100%]`. The uncertainty regression alone exited 0 with `. [100%]` after both adapters retained `INIT_INTENT_RECORDED` while moving the Run to `INDETERMINATE`.
- **Exact approval and start ownership**: Plan approval looks up the exact immutable Task 12 proposal by Run and Plan digest, verifies its current pre-Plan Policy/Budget/Model binding, compares the exact confirmation-code input, and atomically persists the approval, approved Plan/Run states, absent private-ref record, command receipt, and Audit event. Start requires that same approved/current Plan digest and current revision set, derives repository identity, pinned target OID, target-safety digest, reservation identity, and ref/reflog storage observations from owned state and the repository guard, and atomically persists that binding with the one-use `READY_TO_START` Permit.
- **Private-ref boundary**: the sole new mutating Git operation accepts only `refs/apexcrew/runs/<run-id>`, creates it from absence at the pinned base OID, and rejects `refs/heads/*` before spawn. The deterministic intent ID binds Run ID plus consumed Permit generation and introduces no durable `uuid4`. The intent and owner-bound guard are committed before Git. Success updates only the private ref record and Run Head; known failure pauses without adoption; conflict records conflict without overwrite; unobservable observation remains recoverable as `INIT_INTENT_RECORDED` with an `INDETERMINATE` Run. A process crash leaves the unsettled intent durable for Task 24C.
- **Plan defects and interpretations**: Steps 2, 4, and 6 omit the mandatory `--python 3.12`; every selector used it. The generic approval design elsewhere in the plan assumes revision documents, but Task 12 persists Plans in the dedicated graph tables, so Task 13 required a Plan-specific exact proposal lookup and approval record. Step 3's `run_refs` DDL omits `guard_binding_json` even though Steps 5 and its prose require the complete inspected binding to survive until Permit consumption; migration 15 adds that nullable column. The proposed `PrivateRefIdSource.next_intent_id(run_id)` neither binds a retry generation nor prevents another caller-shaped durable-ID source; the retained ID is the SHA-256 binding of Run ID and Permit generation. The Step 5 `RefCasIntent` snippet predeclares Task 19 target/private promotion variants and IDs outside this slice; Task 13 retains only `private_ref_init`. The prescribed crash test claims fresh-continue recovery before Task 24C exists; this slice can and does prove durable crash state, but cannot implement or honestly claim Task 24C reconciliation. The plan does not state what `run_refs.state` should be after a typed `PRIVATE_REF_UNOBSERVABLE` result; treating uncertainty as `ABSENT_EXPECTED` was unsafe, so it remains `INIT_INTENT_RECORDED` while the effect result and Run become indeterminate.
- **Debt disposition**: debt #7 is not closed. Task 13 never constructs `ActionPolicy.default()` and its start path has no path-bearing Worker effect; Task 14 is the first declared tool-effect slice that consumes Task 6's secret policy and must prove end-to-end secret denial, with a plan/path-set amendment if literal `ActionPolicy.default()` composition requires modifying `domain/policy.py`. Debt #8 is not closed because Task 13 adds only generic Audit event fields and no path-bearing Audit/WebUI/log projection. Task 14 must prevent secret paths from entering tool-effect Audit data, and Task 26A must prove the final sanitized Audit projection does not disclose them. Requested-model persistence/validation, injectable durable IDs and request-sensitive `ScriptedMockLLM`, the standalone dependency-order check, keyed-HMAC enforcement, and recursive reservation tamper inspection remain outside this slice. Debt #6 and bounded Git output remain closed and were not routed around.
- **Final mechanical green evidence**: `uv run --python 3.12 ruff format --check .` exited 0 with `69 files already formatted`; `uv run --python 3.12 ruff check .` exited 0 with `All checks passed!`; `uv run --python 3.12 pytest` exited 0 with `310 passed, 2 skipped in 20.55s`; `uv run --python 3.12 mypy src` exited 0 with `Success: no issues found in 33 source files`. `git diff --check` exited 0. Windows is observed green; Ubuntu remains pending CI.
- **Changed paths**: `src/apexcrew/application/control.py`, `src/apexcrew/application/runtime.py`, `src/apexcrew/domain/admission.py`, `src/apexcrew/domain/effects.py`, `src/apexcrew/adapters/state/memory.py`, `src/apexcrew/adapters/state/sqlite.py`, `src/apexcrew/adapters/repository/git.py`, `tests/integration/test_start_guard.py`, `tests/contract/test_state_store.py`, and `AGENT_LOG.md`. The pre-existing Task 12A/12B `PLAN.md` ledger edits are preserved outside this implementation commit; Task 13's ledger row will be updated immediately after commit.
- **Commit subject**: `feat(runtime): guard private run ref initialization`.
- **Review and external-state boundary**: spec-compliance and code-quality reviews are deferred to the M1-07 module PR review as required. No push, publication, credential access, provider request, target-ref mutation, or external repository action was performed.

## 2026-08-03 / M1-R3 - Independent review, historical disposition, and owner GO

- **Reviewed candidate**: `13d8ea45d9dc3f60a4d8d60879d80a8d67c90db9` on `codex/m1-r3-closeout-plan`; `PLAN.md` SHA-256 `04487B354CCB8C6ECB295E800543E353D4083830491B9905E92DE97B6C53367A`; normalized execution-authorization SHA-256 `E110B6F27128F5450F2080AA662A665A432A2334821E5E3ED3BD69728C62A19B`; frozen `SPEC.md` SHA-256 `97E9652D874B606C1673867923C97C29834F63B43ADB3F3E89779B13183E26D6`.
- **Independent review**: APPROVED with zero remaining plan-level blockers. The reviewer verified the exact candidate, unique authorization markers/header, the 14-row EOF ledger, allowed-cell-only normalization, the frozen SPEC digest, and that PR #8 remains outside `main` and is not M1 acceptance evidence.
- **Historical-deviation disposition**: the repository owner and course authority/instructor acceptance required by the M1-R3 gate were explicitly confirmed in this session for every listed historical deviation. That acceptance does not relabel historical reviews, accept PR #8, or waive any R3 task/review requirement.
- **Owner decision**: the repository owner explicitly records `M1 GO` and authorizes creation of the R3-01 implementation worktree after the documentation-only reviewed base is observed on `main`.
- **Scope boundary**: this GO authorizes only R3-01 under the reviewed M1-R3 route. It does not mark M1 complete, authorize M2-M4, authorize PR #8 merge, or authorize a push/publication/credential/Runtime Grant.

## 2026-08-03 / M1-FIX-001 - Bind requested model provenance

- **Pre-task record**: fresh implementation subagent OpenAI Codex (GPT-5), session `/root/r3_01_fix001`, starts from base `f4f75e68eaee751873d698de82ad56b9519df1fa` on `codex/m1-r3-model-provenance`.
- **Exact red command**: `uv run --python 3.12 pytest tests/unit/domain/test_model_requests.py tests/contract/test_state_store.py tests/integration/test_model_restart.py -q`.
- **Intended closed path set**: `src/apexcrew/domain/model.py`, `src/apexcrew/adapters/state/memory.py`, `src/apexcrew/adapters/state/sqlite.py`, `tests/unit/domain/test_model_requests.py`, `tests/contract/test_state_store.py`, `tests/integration/test_model_restart.py`, and `AGENT_LOG.md` only.
- **Observed red**: after correcting one omitted `ModelUsage` test import, the exact selector exited 1 with four target failures. The in-memory `DurableModelClient`, paired memory/SQLite state-store test, and SQLite restart test each observed `COMPLETED` where `REQUESTED_MODEL_MISMATCH` is required for a response whose `requested_model_id` is `gpt-5.6-mini`, persisted request is `gpt-5.6-terra`, and returned model remains allowlisted.
- **Implementation continuity**: the original fresh implementation session was interrupted after recording the red result and beginning the closed-set diff; no commit or review occurred. At the owner's instruction, the primary Codex session resumed only that uncommitted diff, completed the required memory/SQLite response-ID persistence and migration, and preserved this handoff fact for the required reviews.
- **Focused green evidence**: `uv run --python 3.12 pytest tests/unit/domain/test_model_requests.py tests/contract/test_state_store.py tests/integration/test_model_restart.py -q` exited 0 with `66 passed`; `uv run --python 3.12 mypy src` exited 0 with `Success: no issues found in 33 source files`; `git diff --check` exited 0 with no output.
- **Behavior delivered**: `DurableModelClient` compares the response requested-model ID with the exact intent request before releasing an action. A mismatch is `REQUESTED_MODEL_MISMATCH`, carries no normalized action, and uses the same conservative charge as a returned-model mismatch. Memory persists the result object; SQLite migration 16 independently stores response requested-model IDs for attempts and committed turns, validates them against serialized dispatch data on read, and restores reported provider usage after restart without inferring the response value from request JSON.
- **Changed paths**: `src/apexcrew/domain/model.py`, `src/apexcrew/adapters/state/memory.py`, `src/apexcrew/adapters/state/sqlite.py`, `tests/unit/domain/test_model_requests.py`, `tests/contract/test_state_store.py`, `tests/integration/test_model_restart.py`, and `AGENT_LOG.md` only. Human code changes: none.
- **Review status**: pending ordered fresh spec-compliance review followed by fresh code-quality review. Any code/test change after either review restarts the complete red/green/spec/quality sequence.
- **Intended commit subject**: `fix(model): bind requested model provenance`.
- **First spec review**: fresh reviewer `/root/r3_01_fix001_spec_fast` returned REJECTED with one P1 finding: migration 16 left the new response-ID columns null for already committed SQLite completions, causing recovery to reject those legacy rows. No quality review ran after that rejection.
- **Required restart after review finding**: added `test_legacy_committed_completion_without_response_requested_id_recovers`. With the legacy SQLite compatibility branch intentionally disabled, `uv run --python 3.12 pytest tests/integration/test_model_restart.py::test_legacy_committed_completion_without_response_requested_id_recovers -q` exited 1 with `StateConflict: COMMITTED_MODEL_TURN_INCOMPLETE`. The repaired reader preserves a legacy missing response value as unknown, verifies only the values that exist, and never derives it from request JSON. The full focused selector rerun exited 0 with `67 passed`; `uv run --python 3.12 mypy src` again exited 0 with `Success: no issues found in 33 source files`; `git diff --check` again exited 0 with no output.
- **Current review status**: restarted spec-compliance review pending; quality review is forbidden until that fresh spec review passes.
- **Second spec review**: fresh reviewer `/root/r3_01_fix001_spec_recheck` returned REJECTED with one P1 finding: a new committed row whose response-ID column and serialized field were both changed to null could be reclassified as legacy and released. No quality review ran after that rejection.
- **Required restart after second review finding**: added `test_new_committed_completion_cannot_be_reclassified_as_legacy`. Before the new binding check, `uv run --python 3.12 pytest tests/integration/test_model_restart.py::test_new_committed_completion_cannot_be_reclassified_as_legacy -q` exited 1 because the expected `StateConflict` was not raised. The repaired reader accepts legacy only when the serialized response-ID key is absent and the added column is null; it rejects a present-but-null key or any column/serialized value mismatch. The full focused selector rerun exited 0 with `68 passed`; `uv run --python 3.12 mypy src` again exited 0 with `Success: no issues found in 33 source files`; `git diff --check` again exited 0 with no output.
- **Current review status**: restarted spec-compliance review pending for a third fresh reviewer; quality review remains forbidden until it passes.
- **Final spec review**: fresh reviewer `/root/r3_01_fix001_spec_final` returned PASS with no findings. It verified exact task scope, pre-release requested-model comparison, closed mismatch settlement, conservative charge, memory/SQLite ID/outcome/usage persistence, legacy absent-key compatibility, present-null tamper rejection, and the focused `68 passed` plus `mypy src` evidence. Code-quality review is now authorized and pending.
- **Final quality review**: fresh reviewer `/root/r3_01_fix001_quality` returned PASS with no correctness, migration, typing, serialization, or maintainability findings. It confirmed the closed seven-path scope and a clean `git diff --check`; review was read-only and did not replace execution evidence.
- **Final regression and commit readiness**: `uv run --python 3.12 pytest tests/unit/domain/test_model_requests.py tests/unit/domain/test_model_retry.py tests/contract/test_state_store.py tests/integration/test_model_restart.py -q` exited 0 with `70 passed`; `uv run --python 3.12 mypy src` exited 0 with `Success: no issues found in 33 source files`; `git diff --check` exited 0 with no output. Ordered review and verification are complete; the task is ready for the planned implementation commit.
## M1-R3 ledger-audit M1-FIX-001
- PLAN before SHA-256: 04487B354CCB8C6ECB295E800543E353D4083830491B9905E92DE97B6C53367A
- PLAN after SHA-256: 13BB0887AAEDA4365C4F49C783E0E0A76F85354EC938A664DD723C3B8FD70E47
- Cells: Status, Implementation commit
- Implementation commit: 65d545bc3734bd1703d5b0793b52401da6f3b9cd

## 2026-08-04 / M1-R3A - M1-FIX-002 scope-correction review and owner GO

- **Reviewed candidate**: `aaeeef46068884fc939078bfb87b1ce234cbc701` on `codex/m1-r3-fix002-scope-plan`; `PLAN.md` SHA-256 `45CAAA148B43EBF452A8C2F4A03362D2870263875C36198F6EBCEACFBDE3D9A6`; normalized execution-authorization SHA-256 `AC4D72F80F31BE8ED7739DB89C4256CA1ACE1466AEE589061F9494160EABF005`; frozen `SPEC.md` SHA-256 `97E9652D874B606C1673867923C97C29834F63B43ADB3F3E89779B13183E26D6`.
- **Independent review**: fresh reviewer `/root/r3_fix002_scope_plan_review` returned APPROVED with zero blockers. It verified that the candidate-parent diff is one `PLAN.md` line adding only `tests/contract/test_state_store.py` to the M1-FIX-002 closed set, the authorization markers and EOF ledger remain valid, all legacy nonempty `ScriptedMockLLM` inputs are within that set, and M1-FIX-001 remains completed with implementation SHA `65d545bc3734bd1703d5b0793b52401da6f3b9cd`.
- **Owner decision**: the repository owner explicitly renews `M1 GO` for the reviewed M1-R3A scope correction and authorizes resuming only M1-FIX-002 in the existing R3-01 worktree.
- **Scope boundary**: this decision does not mark M1 complete, authorize M2-M4, authorize PR #8 merge, or authorize push, publication, credential access, or a Runtime Grant.

## 2026-08-04 / M1-FIX-008 - Close the model provenance format regression

- **Pre-task record**: fresh implementation subagent OpenAI Codex (GPT-5), session `/root/r3_01_fix008_impl`, starts from base `8631c52200c84d466a6de4709f0dbee12e01baff` on `codex/m1-r3-model-provenance`.
- **Exact red command**: `uv run --python 3.12 ruff format --check .`.
- **Intended closed path set**: `src/apexcrew/domain/model.py`, `src/apexcrew/adapters/state/memory.py`, `src/apexcrew/adapters/state/sqlite.py`, and `AGENT_LOG.md` only.
- **Observed red**: the exact command exited `1` and reported only `src/apexcrew/adapters/state/memory.py:3321`, `src/apexcrew/adapters/state/sqlite.py:5040`, and `src/apexcrew/domain/model.py:370`; each report was the intended single condition-layout replacement. It reported `3 files would be reformatted, 66 files already formatted`.
- **Mechanical implementation**: applied only Ruff's displayed one-line layout at those three conditions. No operand, branch, type, persisted value, test, or other production path changed.
- **Green evidence**: `uv run --python 3.12 ruff format --check .` exited `0` with `69 files already formatted`; `uv run --python 3.12 ruff check .` exited `0` with `All checks passed!`; `uv run --python 3.12 pytest -q` exited `0` with the full suite completing and two expected skips; `uv run --python 3.12 mypy src` exited `0` with `Success: no issues found in 33 source files`; `git diff --check` exited `0` with no output.
- **Changed paths and human boundary**: `src/apexcrew/domain/model.py`, `src/apexcrew/adapters/state/memory.py`, `src/apexcrew/adapters/state/sqlite.py`, and `AGENT_LOG.md` only. Human changes: none.
- **Commit subject**: `style(model): normalize provenance bindings`.
- **Process correction**: preliminary local commit `93e1a0759ccf64962e1c1dab2d027b90616abfca` was created before the mandatory ordered review evidence and therefore was not ledger-recorded or accepted. The branch was returned with `git reset --soft` to its verified parent `8631c52200c84d466a6de4709f0dbee12e01baff`, preserving this exact staged diff without a code or test edit. All preliminary review results are discarded; the fresh staged-diff reviews recorded below are the only final task review evidence.
- **Spec-compliance review**: fresh reviewer OpenAI Codex (GPT-5), session `/root/r3_01_fix008_spec_rebuild`, returned PASS. It verified the authorized base, exact four-path staged scope, three Ruff-only condition-layout hunks, frozen SPEC preservation, red/green evidence, and clean Ruff-format plus cached/working-tree diff checks. No finding or code/test fix was required.
- **Code-quality review**: fresh reviewer OpenAI Codex (GPT-5), session `/root/r3_01_fix008_quality_rebuild`, returned PASS. It verified the final staged four-path scope, no unstaged or untracked change, behavior-preserving Ruff-only source layout, task evidence quality, and passing Ruff format/check, full pytest, mypy, and cached/working-tree diff checks. No finding or code/test fix was required.

## 2026-08-04 / M1-FIX-002 - Injectable model identities and request-sensitive scripted responses

- **Pre-task record**: fresh implementation subagent OpenAI Codex (GPT-5), session `/root/r3_01_fix002_impl`, starts from base `c62298400c80dc525ed0f53de7f679f4055995ab` on `codex/m1-r3-model-provenance`.
- **Exact red command**: `uv run --python 3.12 pytest tests/contract/test_scripted_model.py tests/unit/domain/test_model_retry.py tests/integration/test_model_restart.py -q`.
- **Intended closed path set**: `src/apexcrew/domain/model.py`, `src/apexcrew/adapters/model/__init__.py`, `src/apexcrew/adapters/model/scripted.py`, `tests/helpers/application.py`, `tests/contract/test_scripted_model.py`, `tests/integration/test_global_budget_producers.py`, `tests/integration/test_model_restart.py`, `tests/integration/test_planning_authorization.py`, `tests/integration/test_runtime_permits.py`, `tests/unit/domain/test_model_requests.py`, `tests/unit/domain/test_model_retry.py`, and `AGENT_LOG.md` only.
- **Scope correction and restart point**: the observed raw nonempty fixture in `tests/contract/test_state_store.py` made the original closed set unable to satisfy the strict per-step binding contract. The independently reviewed M1-R3A candidate added that exact test path only and the owner renewed M1 GO before further task work. The task resumes from reviewed base `efd6f1b1fdb924e688f5735111da27cb3d790346` with the corrected path set in `PLAN.md`; no earlier prototype result is treated as green evidence.
- **Continuation handoff**: the original fresh session `/root/r3_01_fix002_impl` stopped after recording the scope conflict and leaving an uncommitted prototype. Fresh continuation `/root/r3_01_fix002_resume_impl` inspected that diff, retained only changes required by the renewed path set, and restarted TDD; it does not claim the original session's prototype as green evidence.
- **Restarted red preparation**: the first rerun of the exact selector stopped at collection with `NameError: name 'ScriptedModelStep' is not defined` from the prototype's unquoted self return annotation. The continuation changed that annotation to a quoted forward reference so the red assertion could execute; no contract result was taken from that collection failure.
- **Restarted red**: after collection was repaired, `uv run --python 3.12 pytest tests/contract/test_scripted_model.py tests/unit/domain/test_model_retry.py tests/integration/test_model_restart.py -q` produced `1 failed, 11 passed`. `test_scripted_model_rejects_unbound_nonempty_input` expected `TypeError: SCRIPTED_MODEL_STEP_REQUIRED`, but the temporary `ScriptedMockLLM` raw `ProviderAttemptResult` compatibility branch did not raise.
- **Implementation and migration**: `ScriptedMockLLM` now accepts only `Sequence[ScriptedModelStep]`, rejects every non-step entry at construction with `SCRIPTED_MODEL_STEP_REQUIRED`, compares request digest, idempotency key, and requested model ID before deque consumption, and returns only the bound result. Raw nonempty fixtures were migrated in `tests/contract/test_state_store.py`, `tests/helpers/application.py`, `tests/integration/test_model_restart.py`, `tests/integration/test_planning_authorization.py`, `tests/unit/domain/test_model_requests.py`, and `tests/unit/domain/test_model_retry.py`; empty scripts remain unchanged. The contract test proves each of the three binding mismatches leaves the step unconsumed. The domain's `ModelIdentitySource`/`RandomModelIdentitySource` seam preserves random UUID production IDs and the finite-source retry test proves deterministic turn and provider-attempt IDs.
- **Green evidence**: the exact selector now reports `12 passed`; `uv run --python 3.12 pytest -q` exits `0` with two expected skips; `uv run --python 3.12 mypy src` reports `Success: no issues found in 33 source files`; `uv run --python 3.12 ruff check .` reports `All checks passed!`; and `git diff --check` has no output.
- **Formatting baseline observation**: `uv run --python 3.12 ruff format --check .` still exits `1` solely because three pre-existing, out-of-task hunks would be reformatted: `src/apexcrew/adapters/state/memory.py:3321`, `src/apexcrew/adapters/state/sqlite.py:5040`, and `src/apexcrew/domain/model.py:370`. None is in this task's diff; the first two are outside the exact allowed paths. They were not changed under this task's closed scope.
- **Final recheck after task-local formatting correction**: `tests/integration/test_planning_authorization.py` was reduced to Ruff's required one-line `ScriptedModelStep.for_request(...)` layout before any review. The exact selector exited `0` with `12 passed`; `uv run --python 3.12 pytest -q` exited `0` with two expected skips; `uv run --python 3.12 mypy src` exited `0` with `Success: no issues found in 33 source files`; `uv run --python 3.12 ruff check .` exited `0` with `All checks passed!`; and `git diff --check` exited `0` with no output. The format check still reports exactly the three baseline lines above and 66 already formatted files.
- **Changed paths and human boundary**: `src/apexcrew/domain/model.py`, `src/apexcrew/adapters/model/scripted.py`, `tests/contract/test_scripted_model.py`, `tests/contract/test_state_store.py`, `tests/helpers/application.py`, `tests/integration/test_model_restart.py`, `tests/integration/test_planning_authorization.py`, `tests/unit/domain/test_model_requests.py`, `tests/unit/domain/test_model_retry.py`, and `AGENT_LOG.md` only. Human code changes: none.
- **Spec-compliance review**: fresh reviewer `/root/r3_01_fix002_spec_review` returned PASS with no M1-FIX-002 blockers. It verified the exact ten-file corrected scope; `ScriptedModelStep`-only nonempty inputs; raw-item rejection; three-field pre-`popleft` comparison; empty-script behavior; migration of every tracked nonempty fixture; and direct finite-source injection at the two actual ID factories while preserving production UUID defaults. It independently observed the focused 12-pass selector, full suite with two expected skips, mypy, Ruff check, and diff check. The three existing Ruff format hunks are R3-01 module-closeout debt, not this task's SPEC blocker. A fresh quality review is now authorized and pending.
- **Quality review**: fresh reviewer `/root/r3_01_fix002_quality_review` returned PASS with no task-local blockers. It confirmed exact corrected scope, nonempty-step migration, pre-`popleft` request binding, production-random/default plus finite-ID-source behavior, and coverage of raw rejection and each binding dimension. It independently observed the focused 12-pass selector, full suite with two expected skips, mypy, Ruff check, and diff check. The three pre-existing Ruff format hunks remain module-closeout risk only and this diff does not worsen them.
## M1-R3 ledger-audit M1-FIX-002
- PLAN before SHA-256: 45CAAA148B43EBF452A8C2F4A03362D2870263875C36198F6EBCEACFBDE3D9A6
- PLAN after SHA-256: 9FD18C973C664B8A9ABB1F553FFFA2F038FD13637000E8A498601127FB229D65
- Cells: Status, Implementation commit
- Implementation commit: 205bd422bbd84a995882ceac50d597e4ef9c0f92

## 2026-08-04 / M1-R3B - M1-FIX-008 format-remediation review and owner GO

- **Reviewed candidate**: `7d8a2615d9a394026cc329e19733b97f2f49329e` on `codex/m1-r3-fix008-format-plan`; `PLAN.md` SHA-256 `27F48E06AE0F40C95CA5C1C0E875333CCFEFF6EA74348AADF27941FA329D058E`; normalized execution-authorization SHA-256 `B578F4A16D8E47705DD264D175329D40E8F266386C795B75909795DBB838C77A`; frozen `SPEC.md` SHA-256 `97E9652D874B606C1673867923C97C29834F63B43ADB3F3E89779B13183E26D6`.
- **Independent review**: fresh reviewer `/root/r3_fix008_plan_review` returned APPROVED with zero blockers. It verified the candidate-only `PLAN.md` change, authorization-marker and 15-row EOF-ledger invariants, the exact three-line Ruff remediation scope, the narrow R3-01 regression exception, and the explicit seven-command post-`M1-FIX-008` module regression.
- **Owner decision**: the repository owner explicitly renews `M1 GO` for the reviewed M1-R3B format-remediation correction and authorizes resuming only `M1-FIX-008` in the existing R3-01 worktree.
- **Scope boundary**: this decision does not mark M1 complete, authorize M2-M4, authorize PR #8 merge, or authorize push, publication, credential access, or a Runtime Grant.
## M1-R3 ledger-audit M1-FIX-008
- PLAN before SHA-256: 27F48E06AE0F40C95CA5C1C0E875333CCFEFF6EA74348AADF27941FA329D058E
- PLAN after SHA-256: 3215973D002AFAF8208278C7426E39645531AD6A15F6CC5F6498BEB985B3BB6E
- Cells: Status, Implementation commit
- Implementation commit: 1bbd0f3a1d89d2c46cec6790e4515aa4dadc6a9a

## 2026-08-04 / R3-01 - Post-M1-FIX-008 local module regression

- **Regression revision**: `3e3f668083a6a769aace47e0b528b5528a86ec09`; implementation map `M1-FIX-001=65d545bc3734bd1703d5b0793b52401da6f3b9cd`, `M1-FIX-002=205bd422bbd84a995882ceac50d597e4ef9c0f92`, and `M1-FIX-008=1bbd0f3a1d89d2c46cec6790e4515aa4dadc6a9a`.
- **Frozen inputs**: `SPEC.md` SHA-256 `97E9652D874B606C1673867923C97C29834F63B43ADB3F3E89779B13183E26D6`; `PLAN.md` SHA-256 `3215973D002AFAF8208278C7426E39645531AD6A15F6CC5F6498BEB985B3BB6E`; normalized execution authorization `B578F4A16D8E47705DD264D175329D40E8F266386C795B75909795DBB838C77A`.
- **1. Dependency sync**: `uv sync --frozen --all-groups` exited `0` in 2.4 s: `Audited 46 packages in 8ms`.
- **2. R3-01 focused selector**: the exact eight-file model selector in `PLAN.md` exited `0` in 28.5 s at `[100%]`.
- **3. Full offline suite**: `uv run --python 3.12 pytest -q` exited `0` in 54.5 s at `[100%]` with two expected skips on this Windows host; no other platform or hosted-CI result is claimed.
- **4-6. Format, lint, typing**: Ruff format exited `0` in 18.7 s with `69 files already formatted`; Ruff check exited `0` in 1.8 s with `All checks passed!`; `mypy src` exited `0` in 2.6 s with `Success: no issues found in 33 source files`.
- **7. Diff integrity**: `git diff --check main...HEAD` exited `0` in 1.5 s with no output. No credential-like material was printed or observed in the exact changed-path review.
- **Boundary**: all local task reviews and the R3-01 regression are complete. Push, PR creation, hosted CI observation, module-PR reviews, merge, M2-M4, publication, credentials, and Runtime Grants remain unauthorized.

## 2026-08-04 / M1-FIX-001 review-remediation - SQLite legacy/bound provenance

- **Pre-task record**: fresh implementation subagent OpenAI Codex (GPT-5), session `/root/r3_01_fix001_legacy_repair_impl`, starts from base `1a92d8eefb9a0bb0180f165f5efc4a2ca369c732` on `codex/m1-r3-model-provenance`.
- **Quality-review P1**: a newly completed SQLite turn could be reclassified as legacy by deleting both the `response_requested_model_id` SQL value and the serialized dispatch key, allowing restart recovery to release its action.
- **Exact red command**: `uv run --python 3.12 pytest tests/unit/domain/test_model_requests.py tests/contract/test_state_store.py tests/integration/test_model_restart.py -q`.
- **Allowed path set**: `src/apexcrew/domain/model.py`, `src/apexcrew/adapters/state/memory.py`, `src/apexcrew/adapters/state/sqlite.py`, `tests/unit/domain/test_model_requests.py`, `tests/contract/test_state_store.py`, `tests/integration/test_model_restart.py`, and `AGENT_LOG.md` only. The public seam is `DurableModelClient` restart recovery; direct SQLite manipulation is limited to constructing a physical schema-v15 migration input.
- **Observed red**: the exact selector exited `1` with `67 passed, 1 failed`. `test_new_committed_completion_cannot_be_reclassified_as_legacy` deleted both the current completion's SQLite response-requested ID and serialized dispatch key; `DurableModelClient.recover_committed()` returned `COMPLETED` rather than raising the expected closed storage-binding conflict.
- **Implementation**: migration 16 now adds constrained `response_requested_model_binding` columns to `model_attempts` and `model_turns`, defaulting pre-migration rows to `LEGACY`. Every new attempt settlement writes `BOUND`; every new committed completion writes `BOUND`. Reads accept `LEGACY` only when both the SQL value is null and the dispatch key is absent. `BOUND` requires an existing dispatch key whose value matches the SQL column, and a committed bound completion additionally requires a non-null response-requested ID. No value is inferred from request JSON.
- **Behavior evidence**: the legacy test physically removes both migration-16 response-ID and binding columns, removes migration `16` from the schema ledger, and removes both serialized keys before reopening through `SqliteStateStore`; the resulting migrated legacy completion recovers through `DurableModelClient`. The current-row tamper test deletes both representations on attempts and the committed turn; `model_attempts()` and public restart recovery each reject, and the empty scripted model remains uncalled.
- **Green evidence**: `uv run --python 3.12 pytest tests/unit/domain/test_model_requests.py tests/contract/test_state_store.py tests/integration/test_model_restart.py -q` exited `0` with `68 passed`; `uv run --python 3.12 mypy src` exited `0` with `Success: no issues found in 33 source files`; `uv run --python 3.12 ruff format --check .` exited `0` with `69 files already formatted`; `uv run --python 3.12 ruff check .` exited `0` with `All checks passed!`; and both cached and working-tree `git diff --check` exited `0` with no output.
- **Changed paths and human boundary**: `src/apexcrew/adapters/state/sqlite.py`, `tests/integration/test_model_restart.py`, and `AGENT_LOG.md` only. Human code changes: none. Ordered spec-compliance and quality reviews remain intentionally unrun for the parent workflow.

### 2026-08-04 / M1-FIX-001 review-remediation continuation - published v16 compatibility

- **Review invalidation and P1**: the preceding remediation candidate's spec-compliance evidence is invalid after this code/test change. Its quality review rejected the candidate because it rewrote published migration `16` instead of preserving its exact two response-requested-ID columns; an already-applied v16 database without the new binding columns then fails recovery with `OperationalError`.
- **New red**: after adding physical v16 fixtures, the exact selector exited `1` with `67 passed, 2 failed`. A valid applied-v16 completion and an applied-v16 completion with both response-ID representations deleted each failed on `sqlite3.OperationalError: no such column: response_requested_model_binding`. This is the required proof that the rejected candidate could not upgrade a historical v16 database.
- **Final migration contract**: migration `16` is restored to its published two `response_requested_model_id` ALTER statements. New migration `17` adds constrained, non-null `LEGACY`/`BOUND` columns with `LEGACY` defaults, then marks rows `BOUND` whenever the v16 SQL value or top-level serialized key is present. New settlements and new committed completions continue to write `BOUND`.
- **Legacy/tamper distinction**: historical pre-v16 attempts use the earlier persisted `result_digest` format, which excludes response and returned model IDs. A `LEGACY` read must have both response-ID representations absent and match that historical digest. A v16 row with both representations deleted has a v16 digest containing those fields and therefore closes; no request JSON is consulted. A legacy committed turn additionally requires a matching verified legacy completed attempt before recovery can release it.
- **Green evidence**: the exact selector exited `0` with `69 passed`, including: physical v15 to v16/v17 migration recovery; physical applied-v16-without-binding-columns recovery; and applied-v16 deletion of both response representations closing both attempt read and public `DurableModelClient` recovery without a model call. `uv run --python 3.12 mypy src` exited `0` with `Success: no issues found in 33 source files`; Ruff format/check exited `0` with `69 files already formatted` and `All checks passed!`; cached and working-tree `git diff --check` exited `0` with no output.
- **Supersession and boundary**: the prior log statement that binding columns were added to migration `16` describes the now-rejected staged candidate and is superseded by this migration-17 contract. Actual changed paths remain `src/apexcrew/adapters/state/sqlite.py`, `tests/integration/test_model_restart.py`, and `AGENT_LOG.md`; no commit, push, review, or M2-M4 change occurred.
- **Spec-compliance review**: fresh reviewer OpenAI Codex (GPT-5), session `/root/r3_01_fix001_legacy_repair_spec`, returned PASS. It verified constrained migration-16 discriminators, BOUND writes at both state transitions, strict legacy/bound read validation, real v15-to-v16 migration behavior, public recovery closure after current-row deletion, and the red/green evidence. No finding or code/test fix was required.
- **Superseded review evidence**: that preceding spec PASS covered the earlier candidate before the migration-17 compatibility correction and is not used for this final remediation. A fresh reviewer subsequently identified that a `BOUND` completed attempt with an explicitly stored null response-requested ID was still readable through `model_attempts()`.
- **Final red/green slice**: `uv run --python 3.12 pytest tests/integration/test_model_restart.py::test_new_attempt_with_explicit_null_requested_model_id_is_closed -q` first exited `1` because no `StateConflict` was raised. The read contract now requires a non-null persisted response-requested ID only for `COMPLETED` bound attempts; legitimate `KNOWN_CLOSED_REJECTION` attempts retain their no-response representation. The same exact command then exited `0`, and the task selector `uv run --python 3.12 pytest tests/unit/domain/test_model_requests.py tests/contract/test_state_store.py tests/integration/test_model_restart.py -q` exited `0`.
- **Spec-compliance review**: fresh reviewer OpenAI Codex (GPT-5), session `/root/r3_01_fix001_final_spec_review`, returned PASS against the final staged candidate. It confirmed the exact permitted three-path scope, unchanged migration 16, additive applied-v16-safe migration 17, strict `LEGACY` digest validation, `BOUND` writes, explicit-null completed-attempt closure, legitimate known-closed recovery, and public no-call closure for a tampered committed completion. It independently observed the task selector, focused upgrade/tamper/retry selector, mypy, Ruff format/check, and cached/working-tree diff checks passing.
- **Code-quality review**: fresh reviewer OpenAI Codex (GPT-5), session `/root/r3_01_fix001_final_quality_review`, returned PASS after the spec review. It found no blocker in the three-path staged scope, migration compatibility, SQLite row indexing, conservative tamper closure, or focused physical-migration tests, and independently reran the task selector, mypy, Ruff format/check, and cached diff check successfully. No code or test changed after either final review.
## M1-R3 ledger-audit M1-FIX-001
- PLAN before SHA-256: 3215973D002AFAF8208278C7426E39645531AD6A15F6CC5F6498BEB985B3BB6E
- PLAN after SHA-256: EDC3E664BC9E307A56B09C6B9C7D288A4876BC14B62900DFBC2849C7C5B99E31
- Cells: Status, Implementation commit
- Implementation commit: 567ab10ec5601aedddca4e149a1a68f54e50e25e

## 2026-08-04 / R3-01 - Post-remediation module regression

- **Regression revision**: `ead4f3b01f89f57cd696759346805135313423d2`; M1-FIX-001 repair `567ab10ec5601aedddca4e149a1a68f54e50e25e`; unchanged companion task commits `M1-FIX-002=205bd422bbd84a995882ceac50d597e4ef9c0f92` and `M1-FIX-008=1bbd0f3a1d89d2c46cec6790e4515aa4dadc6a9a`.
- **Supersession**: the earlier R3-01 regression at `1a92d8e` predates the M1-FIX-001 review remediation and is not final module evidence.
- **Frozen inputs**: `SPEC.md` SHA-256 `97E9652D874B606C1673867923C97C29834F63B43ADB3F3E89779B13183E26D6`; `PLAN.md` SHA-256 `EDC3E664BC9E307A56B09C6B9C7D288A4876BC14B62900DFBC2849C7C5B99E31`; normalized execution authorization `B578F4A16D8E47705DD264D175329D40E8F266386C795B75909795DBB838C77A`.
- **1. Dependency sync**: `uv sync --frozen --all-groups` exited `0` in 0.8 s: `Audited 46 packages in 7ms`.
- **2. R3-01 focused selector**: the exact eight-file model selector in `PLAN.md` exited `0` in 11.1 s.
- **3. Full offline suite**: `uv run --python 3.12 pytest -q` exited `0` in 27.5 s with two expected skips on this Windows host.
- **4-6. Format, lint, typing**: Ruff format exited `0` with `69 files already formatted`; Ruff check exited `0` with `All checks passed!`; `mypy src` exited `0` with `Success: no issues found in 33 source files`.
- **7. Diff integrity**: `git diff --check main...HEAD` exited `0` with no output. The reviewed R3-01 change set contains no credential-like material; this records only an inspection result and no suspected value.
- **Boundary**: local module regression is green. Module spec review, module quality review, same-revision hosted CI, PR merge, and every later R3 module remain pending.
- **Module spec-compliance review**: fresh reviewer OpenAI Codex (GPT-5), session `/root/r3_01_module_final_spec_review`, returned PASS against `main...92a5ed1`. It verified requested-model mismatch closure and conservative charge before output release; memory/SQLite persistence and restart behavior; unchanged migration 16 plus verified migration-17 legacy/bound classification; request-sensitive scripted steps with no mismatch consumption; finite injected identities; the exact format-only M1-FIX-008 boundary; declared R3-01 scope; historical PR #8 non-acceptance; and credential-like-material inspection. It independently reran the eight-file R3-01 selector, complete offline suite, Ruff format/check, mypy, and `git diff --check main...HEAD` successfully.

### 2026-08-04 / M1-FIX-001 review-remediation - bound digest and request binding

- **Pre-task record**: the module quality reviewer `/root/r3_01_module_final_quality_review` rejected `74e19c5` because a coherent edit of both response-requested-model ID representations could evade duplicate-only comparison and release an action after restart. This is an existing M1-FIX-001 contract failure, not new scope.
- **Exact red command**: `uv run --python 3.12 pytest tests/integration/test_model_restart.py::test_bound_completion_with_coherent_requested_model_id_tamper_is_closed -q`.
- **Allowed path set**: `src/apexcrew/domain/model.py`, `src/apexcrew/adapters/state/memory.py`, `src/apexcrew/adapters/state/sqlite.py`, `tests/unit/domain/test_model_requests.py`, `tests/contract/test_state_store.py`, `tests/integration/test_model_restart.py`, and `AGENT_LOG.md` only. The public seam is `DurableModelClient.recover_committed`; direct SQLite edits construct persistence-corruption input only.
- **Observed red**: the exact command exited `1` because `model_attempts()` did not raise after both SQL and JSON response-requested-model IDs were changed coherently from `gpt-5.6-terra` to `gpt-5.6-mini`.
- **Implementation**: SQLite now recomputes the full current `BOUND` attempt result digest during read and, for a dispatch whose outcome is `COMPLETED`, requires its response-requested-model ID to equal the exact persisted request ID. A committed turn, both legacy and bound, additionally requires a matching completed attempt returned by that validated read. Provider-complete records with a closed `REQUESTED_MODEL_MISMATCH` outcome retain their legitimate mismatched response ID and remain readable without action release.
- **Green evidence**: the exact node exited `0`; `uv run --python 3.12 pytest tests/unit/domain/test_model_requests.py tests/contract/test_state_store.py tests/integration/test_model_restart.py -q` exited `0`; `uv run --python 3.12 mypy src` exited `0` with `Success: no issues found in 33 source files`; Ruff format/check both exited `0`; and cached plus working-tree `git diff --check` exited `0` with no output.
- **Review restart and boundary**: this code/test change invalidates the prior task and module review conclusions. Fresh ordered task spec-compliance and code-quality reviews are required before this remediation implementation commit; module reviews and same-revision CI restart after it. Changed paths remain `src/apexcrew/adapters/state/sqlite.py`, `tests/integration/test_model_restart.py`, and `AGENT_LOG.md`; human changes: none.
- **Spec-compliance review**: fresh reviewer OpenAI Codex (GPT-5), session `/root/r3_01_fix001_bound_integrity_spec_review`, returned PASS. It verified full BOUND digest recomputation, persisted-request equality only for action-releasing `COMPLETED`, legitimate closed mismatch/rejection/indeterminate reads, validated completed-attempt linkage before recovery, migration-17 null/missing classification, public zero-call tamper closure, exact allowed scope, and the observed red/green/static evidence.
- **Code-quality review P1 and restart**: fresh reviewer `/root/r3_01_fix001_bound_integrity_quality_review` rejected the candidate because a coherent request-JSON rewrite could evade comparison while retaining the persisted request/recovery digests. The new exact red node is `tests/integration/test_model_restart.py::test_bound_completion_with_rewritten_request_json_is_closed`; no conclusion from the preceding task spec review remains valid after its code/test change.
- **Observed restarted red**: the exact node exited `1` because the current candidate allowed a request JSON rewrite, matching response-ID rewrites, and recomputed result digest through `model_attempts()` without `StateConflict`.
- **Implementation and fixture correction**: additive migration 18 adds `request_requested_model_id` to `model_attempts` and backfills it from historical request JSON. Both initial and retry reservations write the original requested model ID. Readback now compares the parsed request digest to both persisted digest columns and its requested model ID to the independent attempt anchor. The historic-v16 fixture now removes migrations 17 and 18 plus their new columns, so reopen actually exercises their ordered upgrades. An initial focused rerun exposed two query-projection qualification errors from the new join; both were corrected before a green result.
- **Final green evidence**: `uv run --python 3.12 pytest tests/integration/test_model_restart.py::test_bound_completion_with_rewritten_request_json_is_closed -q` exited `0`; the physical v16 and v15 upgrade nodes both exited `0`; `uv run --python 3.12 pytest tests/unit/domain/test_model_requests.py tests/contract/test_state_store.py tests/integration/test_model_restart.py -q` exited `0`; `mypy src`, Ruff format/check, and cached plus working-tree `git diff --check` all exited `0`.
- **Review restart**: this migration-18 code/test change invalidates the preceding task spec PASS and quality REJECTED candidate; fresh ordered task spec-compliance then code-quality review are pending. Module regression and both module reviews restart after the resulting implementation commit.
- **Spec-compliance review**: fresh reviewer OpenAI Codex (GPT-5), session `/root/r3_01_fix001_request_anchor_spec_review`, returned PASS against the final staged migration-18 candidate. It verified unchanged migration 16, bounded migration 18 backfill, initial/retry anchor writes, parsed-request/attempt-digest/turn-digest/anchor read checks, BOUND digest validation, completed-attempt linkage, public recovery closure, physical v15/v16 upgrade fixtures, normal legacy and closed-mismatch behavior, exact allowed scope, and green evidence. Its independent coherent-rewrite exercise changed both request digests, both response representations, and the result digest; both `model_attempts()` and public recovery closed before action release.
- **Code-quality review P1 and restart**: fresh reviewer `/root/r3_01_fix001_request_anchor_quality_review` rejected the candidate because `reserve_authorized_model_attempt()` omitted the new anchor used by the production Authority reservation route. The new exact red node is `tests/contract/test_state_store.py::test_sqlite_authorized_model_attempt_retries_preserve_requested_model_anchor`; both preceding task review results are invalid after its code/test change.
- **Observed Authority red**: the exact node exited `1` at `model_attempts()` with `MODEL_REQUEST_STORAGE_BINDING_MISMATCH` after an Authority-reserved initial attempt, known-closed result, Authority-reserved retry, and completion. This proved both production reservations left the new anchor null.
- **Implementation**: the Authority SQL insert now writes `request.model_request.requested_model_id` alongside request JSON, both digest columns, and idempotency key. A source scan confirms all three `INSERT INTO model_attempts` statements now write the anchor: initial lightweight reservation, lightweight retry reservation, and the Authority production reservation used by `AuthorityModelClient`.
- **Final green evidence**: the exact Authority node exited `0`, proving both initial and retry attempts round-trip their request model and a reopened `DurableModelClient.recover_committed()` returns the committed action without a model call. The full M1-FIX-001 selector `uv run --python 3.12 pytest tests/unit/domain/test_model_requests.py tests/contract/test_state_store.py tests/integration/test_model_restart.py -q` exited `0`; mypy, Ruff format/check, and cached plus working-tree diff checks exited `0`.
- **Review restart**: this Authority-path code/test change invalidates the preceding task spec PASS and quality REJECTED candidate. Fresh ordered task spec-compliance then code-quality review are required before the repair commit. The exact changed paths are `src/apexcrew/adapters/state/sqlite.py`, `tests/contract/test_state_store.py`, `tests/integration/test_model_restart.py`, and `AGENT_LOG.md`; human changes: none.
- **Spec-compliance review**: fresh reviewer OpenAI Codex (GPT-5), session `/root/r3_01_fix001_all_writes_spec_review`, returned PASS against this final staged candidate. It independently confirmed unchanged migration 16; additive 17/18 upgrade behavior; all three initial/retry production inserts persisting the independent anchor; parsed request plus attempt/turn digest, anchor, response-binding and BOUND-digest validation; completed-attempt linkage before recovery; valid legacy/v16/mismatch/rejection paths; Authority retry zero-redispatch recovery; exact four-path scope; and green tests/static checks.
- **Code-quality review**: fresh reviewer OpenAI Codex (GPT-5), session `/root/r3_01_fix001_all_writes_quality_review`, returned PASS after the final spec review. It found no blocking issue in the three insert paths, migration 18 ordering/backfill, SQL projections or placeholder counts, cross-check conditions, legacy/mismatch/retry behavior, Authority initial/retry test coverage, scope, or evidence integrity. It independently observed full pytest, the focused model/state selector, mypy, Ruff format/check, and cached/working-tree diff checks passing. No code or test changed after either final review.
## M1-R3 ledger-audit M1-FIX-001
- PLAN before SHA-256: EDC3E664BC9E307A56B09C6B9C7D288A4876BC14B62900DFBC2849C7C5B99E31
- PLAN after SHA-256: 66C432A5526E5D58A775C9CD4341362E22D9CACAFE5049F99BDE2D22445BBA2C
- Cells: Status, Implementation commit
- Implementation commit: 99bd398eac413119670c3dce2c97ab8271e30b6f

## 2026-08-04 / R3-01 - Final post-anchor module regression

- **Regression revision**: `b3dcb864d79301f1826e622f5c3902eb1348b801`; final M1-FIX-001 anchor repair `99bd398eac413119670c3dce2c97ab8271e30b6f`; companion task commits remain `M1-FIX-002=205bd422bbd84a995882ceac50d597e4ef9c0f92` and `M1-FIX-008=1bbd0f3a1d89d2c46cec6790e4515aa4dadc6a9a`.
- **Supersession**: all earlier R3-01 regression and module-review evidence predates the final M1-FIX-001 anchor repair and is not final evidence for this PR.
- **Frozen inputs**: `SPEC.md` SHA-256 `97E9652D874B606C1673867923C97C29834F63B43ADB3F3E89779B13183E26D6`; `PLAN.md` SHA-256 `66C432A5526E5D58A775C9CD4341362E22D9CACAFE5049F99BDE2D22445BBA2C`; normalized execution authorization `B578F4A16D8E47705DD264D175329D40E8F266386C795B75909795DBB838C77A`.
- **1. Dependency sync**: `uv sync --frozen --all-groups` exited `0` in 0.6 s: `Audited 46 packages in 4ms`.
- **2. R3-01 focused selector**: the exact eight-file model selector in `PLAN.md` exited `0` in 12.3 s.
- **3. Full offline suite**: `uv run --python 3.12 pytest -q` exited `0` in 30.4 s with two expected skips on this Windows host.
- **4-6. Format, lint, typing**: Ruff format exited `0` with `69 files already formatted`; Ruff check exited `0` with `All checks passed!`; `mypy src` exited `0` with `Success: no issues found in 33 source files`.
- **7. Diff integrity**: `git diff --check main...HEAD` exited `0` with no output. The reviewed R3-01 diff contains no credential-like material; no suspected value is recorded.
- **Boundary**: final local module regression is green. Fresh module spec review, then fresh module quality review, hosted CI on the final head, and PR merge remain pending.
- **Final module spec-compliance review**: fresh reviewer OpenAI Codex (GPT-5), session `/root/r3_01_final_module_spec_review`, returned PASS against clean `2dfb8898a9329bedd6773000b15bc435135dc6fe` and `main...HEAD`. It verified early requested-model mismatch closure; response binding/digest and independent request-anchor validation across all three reservation paths including Authority retry; byte-identical migration 16 plus additive 17/18 legacy/v16 compatibility; coherent-rewrite closure before public recovery; scripted request binding and injectable identity behavior; task/ledger scope, normalized PLAN digest, PR #8 non-acceptance, credential scan, and independent seven-command module regression.

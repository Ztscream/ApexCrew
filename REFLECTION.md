# ApexCrew 反思报告

> 范围：本文根据 `SPEC.md`、`PLAN.md`、`SPEC_PROCESS.md` 和 `AGENT_LOG.md` 中截至 2026-08-10 的记录撰写。ApexCrew 仍是预发布实现：R4.3-00 至 R4.3-03 已完成双评审，R4.3-04 至 R4.3-06 的本地绿色实现尚待同样的评审和账本关闭，R4.3-07 的同版本发布证据尚未完成。因此，下面反思的是一套受证据约束的实施过程，而不是对“已生产发布”的复盘。

## 结论

这次项目最有价值的不是让智能体写得更快，而是把“它自称完成”转换为可以重放、审查和推翻的工程主张。最有效的组合是：先用 `brainstorming` 和领域建模收窄决策，再用 `writing-plans` 把决策写成带文件边界、red/green 命令和停止条件的任务，实施时用 TDD 取得可观察的失败，最后由独立的 SPEC review 和 quality review 反向挑战实现。只要其中任何一环被当作表单，收益就会迅速消失。

## 哪些技能真正有效

`writing-plans` 的作用最大，但不是因为它生成了很长的计划，而是因为它迫使计划提供可执行的细节：精确文件、依赖顺序、失败选择器、最小 green、提交边界和后续评审门。早期冷启动和 M1-R1 评审正是靠这些细节暴露了未定义字段、前向依赖、错误的任务拆分和过早宣称的发布门禁。R4.3 又把这一经验收敛成“一个任务、一个工作树、一个实现提交、两次独立评审”的闭环。

TDD 是第二个贡献最大的技能。它不只是“先写测试”，而是要求在实现前留下失败的观察。在 R4.3-02，嵌入式 secret path、歧义的新文件创建和零旧行插入都先以失败选择器出现；在 M1-FIX-004，跨连接请求 ID 竞争不是靠阅读代码猜出的，而是由确定性的竞态测试逼出来的。这样的 red evidence 能限制智能体把当前实现误认为既有正确行为。

`brainstorming`、`domain-modeling` 和架构层面的深模块思考也很重要。它们帮助项目在编码前明确了 Coordinator/WorkerLoop 必须自有、CLI 是唯一写控制面、最终 target CAS 与私有候选推进分离等不可妥协的边界。没有这些共同词汇，后面的测试会变成各模块各自解释“安全”的集合。

subagent-driven 工作流的有效部分不是“多开几个智能体”，而是隔离：新工作树、冻结 base、最小上下文、允许修改的路径集，以及实现者之外的审查者。R4.3-01 和 R4.3-02 的质量审查分别找到了大小写冲突/清理界限，以及不确定写入与嵌入路径泄露；这些问题都不是实现者的绿色选择器自动覆盖的。独立性让复审有机会挑战错误的前提。

## 哪些部分形式大于实质

技能名称、固定模板和冗长日志本身不构成证据。只有当日志能指向实际的失败输出、修复 diff、复跑结果和审查结论时，它才有价值。单纯在提交中写 `Subagent:` 或 `TDD`，不能证明实施者真的看到了 red，也不能证明审查者独立。

早期计划过度追求“所有事情都预先列完”。M1-R1 已有数万行和数百个细动作，但独立审查仍找到了十个阻塞项，包括未定义 helper、跨任务前向依赖、错误的 CI 时序和 Budget 的解释冲突。问题不在于计划不够长，而在于它把某些接口、状态和依赖写成了叙述，没有写成可验证的契约。对这种安全敏感项目，过大的计划还会让智能体更容易复制陈旧片段或遗漏一条限定语。

“每项工作都新建 subagent”同样不是魔法。若给审查者的提示词含糊，或者把实现者的解释一并带入，独立性会退化为复述。M1-R2 的一次外部审查有三项指控在交叉核验后未成立，其中两项源于审查提示措辞不精确。这说明独立审查必须有精确的审查对象、版本和判定问题，而不是只要求“看看有没有问题”。

## TDD：阻碍还是放大器

在 AI 协作中，TDD 是放大器，但前提是测试描述外部可观察的不变量，而非实现者刚写下的内部结构。它把一次自然语言交接变成了“先复现失败，再证明修复”的共同坐标，尤其适合 replay、SQLite 事务、Git 后置状态和路径安全这些人眼不易穷尽的边界。

它也会暴露并放大输入质量差的问题。Task 3 的计划给出了不存在的 `CommandOutcome.sequence`、缺失的辅助构造器和未说明的 repository binding；Task 8 对真实 Git worktree 管理目录的枚举也与 Git for Windows 的实际结果不一致。此时为了得到 red 而机械补齐断言，反而会把错误解释固化。正确做法是停下，把“测试为何无法表达需求”当作计划或规格缺陷，记录解释、补充真实环境回归，再恢复 green。TDD 不是禁止澄清的流程，而是及早触发澄清的装置。

## 自主运行时长与最佳任务颗粒度

本项目没有可靠的“智能体可自主运行 N 小时”的实测结论，也不应该用一个时间数字掩盖风险。可持续的自治单位是一个可独立验收的垂直切片：一个明确的领域结果或适配器边界、一个可运行的 red 选择器、一组最小的 green 验证、固定的文件/禁止路径集合，以及一个可审查的提交。对本项目，这通常相当于约 20 到 60 分钟的专注实现；遇到 schema、跨适配器事务、外部 Git/Docker 或最终 CAS 时，应更短并在每个可观察状态转换后停下。

最优颗粒度由“能否在同一次审查中完整说清行为”决定，而不是行数。一个任务可以跨少量生产和测试文件，但不应同时发明领域类型、迁移、运行时编排和发布流程。好的例子是 R4.3-01 的 context/check 工作区物化，或 R4.3-02 的原子 patch 与 Worker context：各自有清楚的不变量和后续接线任务。Task 8 则说明工作太宽时会混入类型、状态存储、Git 适配器和集成测试修正，评审与纠错成本明显上升。我的默认切分规则是：一个主状态转换或一个端口契约；超过一个新持久化 schema 加一个外部副作用时，再拆一次。

自治不偏题依赖的是输出门禁，而非时钟：每次效果前后要么结算、要么进入类型化的不确定状态；任务结束必须经过独立 SPEC review、quality review 和账本更新。若发现额外前提、计划中的未定义名、超出文件范围的依赖或要访问凭据/远端，智能体应暂停并升级，而不是“顺手补齐”。

## SPEC 与 PLAN 如何决定实现质量

SPEC 提供不能违反的语义边界，PLAN 把它翻译为可执行的局部上下文；两者任一模糊，智能体都会在局部作出貌似合理但不可审计的猜测。高质量的 SPEC 要给出状态机、数据归属、失败语义和安全否定项；高质量的 PLAN 要给出版本化输入、任务前置条件、精确选择器、文件所有权和交接点。二者之间还需要实施面对的独立冷读，而不是只做散文一致性检查。

一个具体案例是 M1-R1 对 Budget Revision 边界的错误收敛。原 SPEC 同时使用了“可降低的 Budget 字段”和“同样不可提高的每 Task 限制”两组表述；计划子智能体据此采用“不可提高就不属于 Budget Revision”的窄解释。独立计划审查指出，SPEC 对表格字段和 per-Task 限制均保留了可相容但不同的读法，不能由实施者自行选边。流程在写源代码前停止，随后由所有者批准 SPEC revision 2，明确了字段边界和 active-run time 的规则。这个案例的关键不是“审查救了一个 bug”，而是规约不清已经把子智能体带向了错误的实现前提；如果没有冻结输入、cold review 和停工权，它会变成一组很完整但语义错误的测试与代码。

同一教训也出现在计划层：Task 3 和 Task 8 的未定义 helper、暂时 repository binding 与真实 Git 目录清单差异，说明计划必须在派发前通过最小的可执行探针验证。以后应为每个任务增加一页“契约卡”：输入版本、拥有的类型/表、允许新建的名字、外部可观察结果、拒绝条件、唯一 red 命令和不属于本任务的决策。它比继续增加自然语言步骤更能防止偏离。

## 最有效的 prompt 与 context 策略

最有效的方式是把上下文压缩为不可替代的证据包，而不是把整个项目历史塞给实现者。实现 prompt 应包含：任务 ID、冻结的 base/SPEC/PLAN 版本、只相关的规格段落、允许与禁止的路径、既有公共接口、red 命令及预期失败、green/静态检查、不可执行的外部操作，以及固定格式的结果报告。审查 prompt 则只给规范、任务契约、精确 diff、测试证据和要反驳的主张，不附带实现者的自我解释。

这种策略有效是因为它同时减少两种失真：上下文过载造成的旧设计回填，以及信息不足造成的“按惯例补全”。冻结 hash 和 base 让智能体无法把别的分支的成果当成前提；路径范围和明确的停工条件让它知道何时不该继续；先 red 后 green 让结果报告以观察为中心。R4.3 对每个任务在前一个最终评审提交上新开工作树，就是这套策略的工程化版本。

## 凭据与分发带来的工程认识

凭据要求迫使我把“配置”视为信任边界，而不是普通字符串。真正要设计的是来源优先级、何时读取、能否缓存、是否会进入日志/`repr`/模型 payload/子进程环境、缺失时如何失败，以及不可信目标仓库能否影响它。由此形成了 OS keyring 优先、唯一受控 CI 环境变量、拒绝加载仓库 `.env`、默认不跑 live smoke、任何缺失都返回类型化失败的策略。它还说明 secret scan 不是凭据治理的替代品：扫描可以发现泄露，不能证明运行时没有把值传到了错误边界。

分发要求则迫使我区分“本地通过”与“可交付”。wheel、受限执行器镜像、静态只读 WebUI、Windows/Ubuntu CI、GitLab `unit-test`、可达历史 secret scan、性能和浏览器可用性，必须绑定到同一个 source SHA；旧 PR 的绿色 CI 不能覆盖新的本地改动。发布还涉及 owner-only 的 push、受信任发布、Pages 与真实 smoke，这些不能被智能体因为本地绿灯而擅自执行。R4.3-07 尚未完成正是这条原则的现实检验：宁可报告证据缺失，也不把 build 成功写成已发布。

## 如果重做

我会在首轮规格冻结前先实现一个更小的端到端“真实链路”：单 Worker、单 fixture、一次受限 check、一次私有候选推进和一次拒绝的 target CAS，然后再扩展并发、恢复、purge 和 UI。这样能更早发现哪些安全语义在文本中不可执行，避免先形成巨大的计划再做多轮拆解。

我会把计划验证自动化：检查任务间引用的类型/测试文件是否已存在或明确由当前任务创建，检查 red 命令在形式 base 上确实失败，检查 stage set 覆盖所有写路径，检查提交/PR/评审账本是否闭合。很多 M1-R1 的问题属于这类机械一致性，不该占用昂贵的人或模型审查。

我也会更早建立交付证据矩阵和 owner handoff：哪些门禁只能在远端、哪些需要 Docker/凭据、谁有权限执行、产物与 SHA 如何绑定。把这些事项放到最后会使“实现完成”和“课程/发布可验证”之间留下很大的断层。

## 对 Superpowers 方法论的批判

这套方法论隐含了几个前提：规格足够稳定且可写成局部任务；任务之间的依赖可以线性化；工作树、测试和审查的成本足够低；独立 agent 真正独立；以及离线 red/green 能代表关键风险。在 ApexCrew 中，这些前提只部分成立。

它们在确定性的领域逻辑、SQLite 持久化、类型化命令和 fixture 上大多成立，所以小任务、TDD 与独立复审显著提高了可靠性。但它们在安全边界、Git 的平台差异、真实 Docker 观察、凭据、远端 CI 和发布授权上不完全成立：真实环境会引入计划外状态，外部证据并不能由本地 agent 伪造，过细的串行任务还会拖慢对系统级不变量的理解。

因此，我会把 Superpowers 当成一套“把主张变成可反驳证据”的默认协议，而不是一套可凭勾选获得正确性的仪式。它需要由三个补充条件支撑：规格可以被实施者反驳并允许修订；每个 gate 有可观察且不可伪造的证据；人保留对范围、凭据、远端副作用和最终质量声明的决定权。满足这些条件时，技能是在放大工程判断；不满足时，它们只是在更快地产生格式正确的错误。
